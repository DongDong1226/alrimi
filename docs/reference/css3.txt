/* ============================================================
   EIASS    李⑥꽭    섍꼍 곹뼢 됯   듯빀 뺣낫  뚮옯  
   Design tokens + base + component system (KRDS-grounded)
   Font: Pretendard / Pretendard GOV  쨌  Primary base #1c47d4
   ============================================================ */

@import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.css");

/* ---------- TOKENS ---------- */
:root{
  /* primary scale (KRDS blue)    overridable by Tweaks */
  --p10:#b9c6f2; --p20:#97aaeb; --p30:#6784e2; --p40:#496cdd;
  --p50:#1c47d4; --p60:#1941c1; --p70:#143297; --p80:#0f2775; --p90:#0c1e59;
  --navy:#003675;
  --navy-2:#0a274a;
  --navy-3:#02234f;
  --accent:#0d95fa;
  --live:#de3412;

  --primary:var(--p50);
  --primary-strong:var(--p80);
  --primary-deep:var(--navy);
  --primary-soft:#eaf0fe;
  --primary-tint:#f3f6fd;

  /* eco / state */
  --eco:#1f8a5b; --eco-soft:#e7f4ee;
  --teal:#3f9aae;
  --orange:#fb4e0c;
  --warn:#e8730c; --danger:#de3412; --success:#1f8a5b; --info:#1c47d4;

  /* grayscale */
  --ink:#17181c; --ink-2:#42484e; --ink-3:#6d6d6d; --ink-4:#8e8e8e;
  --line:#e4e7ec; --line-2:#d8dde3; --line-strong:#c7ccd3;
  --bg:#ffffff; --bg-1:#f6f8fb; --bg-2:#fafbfc; --bg-blue:#eef3fb;

  /* type scale (px 쨌 횞--tscale) */
  --tscale:1;
  --fs-display-l:calc(56px*var(--tscale));
  --fs-display-m:calc(44px*var(--tscale));
  --fs-display-s:calc(34px*var(--tscale));
  --fs-h-l:calc(32px*var(--tscale));
  --fs-h-m:calc(24px*var(--tscale));
  --fs-h-s:calc(20px*var(--tscale));
  --fs-h-xs:calc(17px*var(--tscale));
  --fs-h-xxs:calc(15px*var(--tscale));
  --fs-body-l:calc(18px*var(--tscale));
  --fs-body-m:calc(16px*var(--tscale));
  --fs-body-s:calc(14px*var(--tscale));
  --fs-body-xs:calc(13px*var(--tscale));

  /* radius (횞--rscale) */
  --rscale:1;
  --r-xs:calc(4px*var(--rscale));
  --r-sm:calc(8px*var(--rscale));
  --r-md:calc(12px*var(--rscale));
  --r-lg:calc(18px*var(--rscale));
  --r-xl:calc(26px*var(--rscale));
  --r-pill:999px;

  /* density (횞--dscale) */
  --dscale:1;

  /* shadows */
  --sh-sm:0 1px 2px rgba(16,28,54,.06),0 1px 3px rgba(16,28,54,.05);
  --sh-md:0 6px 18px rgba(16,28,54,.08),0 2px 6px rgba(16,28,54,.05);
  --sh-lg:0 18px 50px rgba(16,28,54,.14),0 6px 16px rgba(16,28,54,.07);
  --sh-xl:0 30px 80px rgba(10,25,60,.22);

  --maxw:1280px;
  --ease:cubic-bezier(.4,.7,.3,1);

  --font:"Pretendard Variable","Pretendard GOV",Pretendard,-apple-system,
         BlinkMacSystemFont,system-ui,"Segoe UI","Apple SD Gothic Neo","Malgun Gothic",sans-serif;
}

/* ---------- RESET ---------- */
*,*::before,*::after{box-sizing:border-box}
html{-webkit-text-size-adjust:100%;scroll-behavior:smooth}
body{margin:0;font-family:var(--font);color:var(--ink-2);background:var(--bg);
  font-size:var(--fs-body-m);line-height:1.6;letter-spacing:-.01em;word-break:keep-all;
  -webkit-font-smoothing:antialiased;text-rendering:optimizeLegibility}
h1,h2,h3,h4,h5,p,figure,ul,ol{margin:0}
ul,ol{padding:0;list-style:none}
a{color:inherit;text-decoration:none}
img{display:block;max-width:100%}
button{font-family:inherit;cursor:pointer}
:focus-visible{outline:3px solid color-mix(in srgb,var(--primary) 55%,#fff);outline-offset:2px;border-radius:4px}
::selection{background:color-mix(in srgb,var(--primary) 22%,#fff)}

/* ---------- LAYOUT ---------- */
.wrap{width:min(100% - 48px,var(--maxw));margin-inline:auto}
.section{padding-block:calc(84px*var(--dscale))}
.section--tight{padding-block:calc(60px*var(--dscale))}
.bg-1{background:var(--bg-1)} .bg-blue{background:var(--bg-blue)}
.row{display:flex} .col{display:flex;flex-direction:column}
.between{justify-content:space-between} .center{align-items:center}
.gap-8{gap:8px}.gap-12{gap:12px}.gap-16{gap:16px}.gap-24{gap:24px}

/* ---------- TYPOGRAPHY HELPERS ---------- */
.kicker{display:inline-flex;align-items:center;gap:8px;
  font-size:var(--fs-body-s);font-weight:700;letter-spacing:.02em;color:var(--primary)}
.kicker svg{width:18px;height:18px}
.kicker--light{color:#bcd0fb}
.display{font-size:var(--fs-display-m);font-weight:700;line-height:1.18;
  letter-spacing:-.025em;color:var(--ink)}
.h-l{font-size:var(--fs-h-l);font-weight:700;line-height:1.28;letter-spacing:-.02em;color:var(--ink)}
.h-m{font-size:var(--fs-h-m);font-weight:700;line-height:1.32;letter-spacing:-.02em;color:var(--ink)}
.h-s{font-size:var(--fs-h-s);font-weight:600;line-height:1.4;color:var(--ink)}
.lead{font-size:var(--fs-body-l);color:var(--ink-3);line-height:1.6;font-weight:400}
.muted{color:var(--ink-3)} .strong{color:var(--ink);font-weight:600}
.em{color:var(--primary)} .text-pretty{text-wrap:pretty}

.section-head{display:flex;flex-direction:column;gap:12px;margin-bottom:40px}
.section-head .display,.section-head .h-l{margin-top:2px}
.section-head--row{flex-direction:row;align-items:flex-end;justify-content:space-between;gap:24px}

/* ---------- BUTTONS ---------- */
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;
  font-weight:600;font-size:var(--fs-body-m);line-height:1;border:1px solid transparent;
  border-radius:var(--r-sm);padding:14px 22px;transition:.18s var(--ease);white-space:nowrap}
.btn svg{width:18px;height:18px}
.btn--primary{background:var(--primary);color:#fff;box-shadow:var(--sh-sm)}
.btn--primary:hover{background:var(--p60);transform:translateY(-1px);box-shadow:var(--sh-md)}
.btn--deep{background:var(--primary-strong);color:#fff}
.btn--deep:hover{background:var(--p90)}
.btn--line{background:#fff;color:var(--primary-strong);border-color:var(--line-2)}
.btn--line:hover{border-color:var(--primary);color:var(--primary);background:var(--primary-tint)}
.btn--ghost{background:transparent;color:var(--ink-2)}
.btn--ghost:hover{background:#fff;color:var(--primary)}
.btn--text-danger{background:transparent;color:var(--danger,#de3412);border-color:transparent}
.btn--text-danger:hover{background:#fdf3f1;border-color:#f3c9c0}
.btn--lg{padding:17px 30px;font-size:var(--fs-body-l)}
.btn--sm{padding:9px 14px;font-size:var(--fs-body-s);border-radius:var(--r-xs)}
.btn--pill{border-radius:var(--r-pill)}

/* ---------- BADGES / CHIPS ---------- */
.badge{display:inline-flex;align-items:center;gap:5px;font-size:var(--fs-body-xs);
  font-weight:700;line-height:1;padding:5px 9px;border-radius:6px;letter-spacing:0}
.badge--blue{background:var(--primary-soft);color:var(--p70)}
.badge--navy{background:#e7eef9;color:var(--navy)}
.badge--eco{background:var(--eco-soft);color:var(--eco)}
.badge--teal{background:#e6f3f6;color:#2c7e90}
.badge--orange{background:#ffeede;color:#c2410c}
.badge--gray{background:#eef0f3;color:var(--ink-3)}
.badge--line{background:#fff;border:1px solid var(--line-2);color:var(--ink-3)}
.badge--live{background:var(--danger);color:#fff}
.badge--dday{background:var(--ink);color:#fff}
.badge--dot::before{content:"";width:5px;height:5px;border-radius:50%;background:currentColor}

.chip{display:inline-flex;align-items:center;gap:6px;padding:8px 14px;border-radius:var(--r-pill);
  font-size:var(--fs-body-s);font-weight:500;color:var(--ink-2);
  background:#fff;border:1px solid var(--line-2);transition:.16s var(--ease)}
.chip:hover{border-color:var(--primary);color:var(--primary);background:var(--primary-tint);transform:translateY(-1px)}

/* ---------- CARD ---------- */
.card{background:#fff;border:1px solid var(--line);border-radius:var(--r-md);transition:.2s var(--ease)}
.card--hover:hover{transform:translateY(-4px);box-shadow:var(--sh-lg);border-color:transparent}

/* ---------- ICON CIRCLE ---------- */
.ico-tile{display:flex;align-items:center;justify-content:center;width:64px;height:64px;
  border-radius:var(--r-md);background:var(--primary-tint);color:var(--primary);transition:.2s var(--ease)}
.ico-tile svg{width:30px;height:30px}

/* ============================================================
   HEADER / GNB
   ============================================================ */
.topbar{background:var(--primary-deep);color:#cdd9ec}
.topbar .wrap{display:flex;justify-content:flex-end;align-items:center;gap:2px;height:38px;
  font-size:var(--fs-body-xs)}
.topbar a{display:inline-flex;align-items:center;gap:6px;padding:6px 12px;color:#cdd9ec;
  font-weight:500;border-radius:var(--r-xs);transition:.15s}
.topbar a:hover{color:#fff;background:rgba(255,255,255,.08)}
.topbar .sep{width:1px;height:12px;background:rgba(255,255,255,.18)}
.topbar svg{width:15px;height:15px}

.site-header{position:sticky;top:0;z-index:200;background:rgba(255,255,255,.92);
  -webkit-backdrop-filter:blur(12px);backdrop-filter:blur(12px);
  border-bottom:1px solid var(--line);transition:box-shadow .2s}
.site-header.scrolled{box-shadow:var(--sh-sm)}
.nav-main{display:flex;align-items:center;justify-content:space-between;height:78px}
.brand{display:flex;align-items:center;gap:11px}
.brand .mark{width:38px;height:38px;flex:none}
.brand .wordmark{display:flex;flex-direction:column;line-height:1}
.brand .wordmark b{font-size:23px;font-weight:800;letter-spacing:.04em;color:var(--primary-deep);
  font-family:"Pretendard Variable",var(--font)}
.brand .wordmark span{font-size:10.5px;font-weight:600;color:var(--ink-4);letter-spacing:-.01em;margin-top:3px}

.gnb{display:flex;height:100%}
.gnb>li{display:flex}
.gnb>li>a{display:flex;align-items:center;height:100%;padding:0 22px;font-weight:600;
  font-size:var(--fs-h-xs);color:var(--ink);position:relative;transition:color .15s}
.gnb>li>a::after{content:"";position:absolute;left:22px;right:22px;bottom:-1px;height:3px;
  background:var(--primary);border-radius:3px;transform:scaleX(0);transform-origin:center;transition:transform .22s var(--ease)}
.gnb>li:hover>a,.gnb>li.active>a{color:var(--primary)}
.gnb>li:hover>a::after,.gnb>li.active>a::after{transform:scaleX(1)}

.nav-tools{display:flex;align-items:center;gap:10px}
.nav-tools .icon-btn{width:44px;height:44px;border-radius:var(--r-sm);border:1px solid var(--line);
  background:#fff;display:flex;align-items:center;justify-content:center;color:var(--ink-2);transition:.16s}
.nav-tools .icon-btn:hover{border-color:var(--primary);color:var(--primary)}
.nav-tools .icon-btn svg{width:20px;height:20px}
.hamburger{display:none}

/* MEGA MENU */
.mega{position:absolute;left:0;right:0;top:100%;background:#fff;border-top:1px solid var(--line);
  box-shadow:var(--sh-lg);opacity:0;visibility:hidden;transform:translateY(-8px);
  transition:.22s var(--ease);z-index:190}
.site-header.mega-open .mega{opacity:1;visibility:visible;transform:none}
.mega-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:8px 28px;padding:34px 0 40px}
.mega-col h4{font-size:var(--fs-h-xs);font-weight:700;color:var(--primary-deep);padding-bottom:12px;
  margin-bottom:10px;border-bottom:2px solid var(--primary-soft)}
.mega-col li a{display:block;padding:7px 0;font-size:var(--fs-body-s);color:var(--ink-3);
  border-radius:4px;transition:.13s}
.mega-col li a:hover{color:var(--primary);padding-left:6px}
.mega-backdrop{position:fixed;inset:0;background:rgba(10,25,60,.18);opacity:0;visibility:hidden;
  transition:.22s;z-index:180}
.site-header.mega-open~.mega-backdrop,body.mega-on .mega-backdrop{opacity:1;visibility:visible}

/* ============================================================
   FOOTER
   ============================================================ */
.site-footer{background:#0c1830;color:#9aa7bd;font-size:var(--fs-body-s)}
.footer-links{border-bottom:1px solid rgba(255,255,255,.09)}
.footer-links .wrap{display:flex;flex-wrap:wrap;gap:8px 26px;padding-block:20px}
.footer-links a{color:#c5cfdf;font-weight:500;padding:4px 0;transition:.14s}
.footer-links a:hover{color:#fff}
.footer-links a.em{color:#fff;font-weight:700}
.footer-main{padding-block:46px}
.footer-main .wrap{display:grid;grid-template-columns:1.4fr 1fr auto;gap:40px;align-items:start}
.footer-brand .mark{width:34px;height:34px;margin-bottom:16px}
.footer-brand b{display:block;color:#fff;font-size:20px;font-weight:800;letter-spacing:.04em}
.footer-info p{margin-bottom:5px;line-height:1.7;color:#9aa7bd}
.footer-info b{color:#cdd7e6;font-weight:600}
.footer-marks{display:flex;flex-direction:column;gap:18px;align-items:flex-end}
.footer-marks img{height:34px;width:auto;opacity:.95}
.footer-marks .wa{height:46px}
.footer-sns{display:flex;gap:10px}
.footer-sns a{width:38px;height:38px;border-radius:50%;background:rgba(255,255,255,.08);
  display:flex;align-items:center;justify-content:center;color:#c5cfdf;transition:.15s}
.footer-sns a:hover{background:var(--primary);color:#fff}
.footer-sns svg{width:18px;height:18px}
.copyright{border-top:1px solid rgba(255,255,255,.09);padding-block:18px}
.copyright .wrap{display:flex;justify-content:space-between;flex-wrap:wrap;gap:10px;
  font-size:var(--fs-body-xs);color:#74819a}

/* ============================================================
   CAROUSEL primitives
   ============================================================ */
.carousel{position:relative}
.carousel-track{display:flex;gap:24px;overflow-x:auto;scroll-snap-type:x mandatory;
  scroll-behavior:smooth;padding-bottom:4px;scrollbar-width:none}
.carousel-track::-webkit-scrollbar{display:none}
.carousel-track>*{scroll-snap-align:start;flex:none}
.carousel-ctrl{display:flex;align-items:center;gap:6px}
.car-btn{width:42px;height:42px;border-radius:50%;border:1px solid var(--line-2);background:#fff;
  display:flex;align-items:center;justify-content:center;color:var(--ink-2);transition:.15s}
.car-btn:hover{border-color:var(--primary);color:var(--primary);background:var(--primary-tint)}
.car-btn svg{width:18px;height:18px}
.car-count{font-size:var(--fs-body-s);font-weight:600;color:var(--ink-3);font-variant-numeric:tabular-nums;
  padding:0 10px}
.car-count b{color:var(--ink)}

/* reveal */
.reveal{opacity:0;transform:translateY(22px);transition:.7s var(--ease)}
.reveal.in{opacity:1;transform:none}

/* skip link */
.skip{position:absolute;left:-999px;top:0;z-index:999;background:var(--primary);color:#fff;
  padding:10px 18px;border-radius:0 0 8px 0;font-weight:600}
.skip:focus{left:0}

/* ---------- TWEAKS PANEL (vanilla) ---------- */
#twk{position:fixed;right:18px;bottom:18px;z-index:2147483646;width:286px;
  background:rgba(255,255,255,.86);-webkit-backdrop-filter:blur(20px) saturate(150%);
  backdrop-filter:blur(20px) saturate(150%);border:1px solid rgba(255,255,255,.7);
  border-radius:16px;box-shadow:0 20px 60px rgba(10,25,60,.26);color:var(--ink);
  font-size:12px;display:none;overflow:hidden}
#twk.open{display:block}
#twk .twk-hd{display:flex;align-items:center;justify-content:space-between;padding:13px 12px 13px 16px;
  border-bottom:1px solid rgba(0,0,0,.06)}
#twk .twk-hd b{font-size:13px;font-weight:700;display:flex;align-items:center;gap:7px}
#twk .twk-hd b svg{width:16px;height:16px;color:var(--primary)}
#twk .twk-x{border:0;background:transparent;color:var(--ink-3);width:26px;height:26px;border-radius:7px;font-size:15px}
#twk .twk-x:hover{background:rgba(0,0,0,.06);color:var(--ink)}
#twk .twk-body{padding:14px 16px 18px;display:flex;flex-direction:column;gap:15px;
  max-height:70vh;overflow:auto}
#twk .twk-sec{font-size:10px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-4)}
#twk .twk-grp{display:flex;flex-direction:column;gap:8px}
#twk .twk-lbl{font-weight:600;color:var(--ink-2);display:flex;justify-content:space-between}
#twk .twk-lbl .v{color:var(--ink-4);font-weight:500}
#twk .sw-row{display:flex;gap:8px;flex-wrap:wrap}
#twk .sw{width:34px;height:34px;border-radius:9px;border:2px solid #fff;
  box-shadow:0 0 0 1px var(--line-2);cursor:pointer;transition:.13s;position:relative}
#twk .sw.on{box-shadow:0 0 0 2px var(--primary);transform:scale(1.06)}
#twk .seg{display:flex;background:rgba(0,0,0,.05);border-radius:9px;padding:3px;gap:2px}
#twk .seg button{flex:1;border:0;background:transparent;color:var(--ink-3);font-weight:600;font-size:11px;
  padding:7px 4px;border-radius:6px;transition:.13s}
#twk .seg button.on{background:#fff;color:var(--primary);box-shadow:var(--sh-sm)}
#twk input[type=range]{-webkit-appearance:none;width:100%;height:4px;border-radius:99px;background:rgba(0,0,0,.12)}
#twk input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;border-radius:50%;
  background:var(--primary);box-shadow:0 1px 4px rgba(0,0,0,.25);cursor:pointer}

/* ============================================================
   RESPONSIVE
   ============================================================ */
@media (max-width:1080px){
  .mega-grid{grid-template-columns:repeat(4,1fr)}
  .footer-main .wrap{grid-template-columns:1fr 1fr}
  .footer-marks{grid-column:1/-1;flex-direction:row;align-items:center;justify-content:space-between}
}
@media (max-width:860px){
  :root{--tscale:.92}
  .gnb,.topbar{display:none}
  .hamburger{display:flex}
  .nav-main{height:64px}
  .section{padding-block:calc(56px*var(--dscale))}
  .section-head{margin-bottom:28px}
  .section-head--row{flex-direction:column;align-items:flex-start}
  .footer-main .wrap{grid-template-columns:1fr}
  .footer-marks{flex-direction:column;align-items:flex-start;gap:14px}
}
@media (max-width:560px){
  :root{--tscale:.86}
  .wrap{width:calc(100% - 32px)}
  .btn--lg{padding:14px 22px}
}

/* mobile drawer */
.drawer{position:fixed;inset:0;z-index:300;visibility:hidden}
.drawer.open{visibility:visible}
.drawer-bd{position:absolute;inset:0;background:rgba(10,25,60,.4);opacity:0;transition:.25s}
.drawer.open .drawer-bd{opacity:1}
.drawer-panel{position:absolute;top:0;right:0;width:min(86vw,360px);height:100%;background:#fff;
  transform:translateX(100%);transition:.28s var(--ease);overflow-y:auto;padding:20px}
.drawer.open .drawer-panel{transform:none}
.drawer-panel .d-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:18px}
.drawer-panel details{border-bottom:1px solid var(--line)}
.drawer-panel summary{padding:15px 4px;font-weight:700;font-size:var(--fs-h-xs);color:var(--ink);
  list-style:none;display:flex;justify-content:space-between;align-items:center;cursor:pointer}
.drawer-panel summary::-webkit-details-marker{display:none}
.drawer-panel summary::after{content:"+";color:var(--ink-4);font-weight:400;font-size:20px}
.drawer-panel details[open] summary::after{content:"  "}
.drawer-panel .d-sub a{display:block;padding:9px 12px;color:var(--ink-3);font-size:var(--fs-body-s)}
.drawer-panel .d-sub a:hover{color:var(--primary)}