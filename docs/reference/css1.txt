/* ============================================================
   EIASS    home.css  (main page sections)
   ============================================================ */

/* ---------- HERO ---------- */
.hero{position:relative;color:#fff;overflow:hidden;isolation:isolate}
.hero-bg{position:absolute;inset:0;z-index:-2}
.hero-bg::after{content:"";position:absolute;inset:0}
/* default = photo */
body[data-hero="photo"] .hero-bg{
  background:#0c1e59 url("https://images.unsplash.com/photo-1466611653911-95081537e5b7?auto=format&fit=crop&w=1920&q=72") center/cover no-repeat;}
body[data-hero="photo"] .hero-bg::after{
  background:linear-gradient(105deg,rgba(20,45,95,.55) 0%,rgba(30,70,130,.4) 42%,rgba(90,140,190,.22) 100%);}
body[data-hero="gradient"] .hero-bg{
  background:linear-gradient(120deg,var(--primary-deep) 0%,var(--p80) 40%,var(--p60) 100%);}
body[data-hero="gradient"] .hero-bg::after{
  background:radial-gradient(120% 90% at 85% 0%,color-mix(in srgb,var(--p40) 55%,transparent) 0%,transparent 55%);}
body[data-hero="solid"] .hero-bg{background:var(--primary-deep);}
body[data-hero="solid"] .hero-bg::after{
  background:linear-gradient(120deg,transparent 60%,rgba(255,255,255,.05) 100%);}
.hero-grid{position:absolute;inset:0;z-index:-1;opacity:.5;
  background-image:linear-gradient(rgba(255,255,255,.05) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.05) 1px,transparent 1px);
  background-size:64px 64px;mask:linear-gradient(105deg,transparent 30%,#000 100%)}

.hero-inner{padding-top:84px;padding-bottom:150px;position:relative}
.hero h1{font-size:var(--fs-display-l);font-weight:800;line-height:1.16;letter-spacing:-.03em;
  margin:18px 0 0;max-width:18ch;text-wrap:balance}
.hero .en{font-size:var(--fs-body-s);font-weight:700;letter-spacing:.18em;color:#9fc0ff;text-transform:uppercase}
.hero .sub{margin-top:18px;font-size:var(--fs-body-l);color:#c4d3ee;max-width:46ch;line-height:1.6}

.searchbar{margin-top:34px;max-width:680px;background:rgba(255,255,255,.97);border-radius:var(--r-pill);
  padding:8px 8px 8px 26px;display:flex;align-items:center;gap:14px;
  box-shadow:0 24px 60px rgba(6,16,44,.35);border:1px solid rgba(255,255,255,.5)}
.searchbar svg.lead-ico{width:22px;height:22px;color:var(--primary);flex:none}
.searchbar input{flex:1;border:0;outline:0;background:transparent;font-size:var(--fs-body-l);
  color:var(--ink);font-family:inherit;min-width:0}
.searchbar input::placeholder{color:var(--ink-4)}
.searchbar .btn{flex:none}
.kw-row{display:flex;align-items:center;gap:10px;margin-top:18px;flex-wrap:wrap}
.kw-row .lbl{display:inline-flex;align-items:center;gap:6px;font-size:var(--fs-body-s);color:#bcd0fb;font-weight:600}
.kw-row .lbl svg{width:16px;height:16px}
.kw-chip{padding:7px 14px;border-radius:var(--r-pill);font-size:var(--fs-body-s);color:#dbe6fb;font-weight:500;
  background:rgba(255,255,255,.1);border:1px solid rgba(255,255,255,.18);transition:.15s var(--ease)}
.kw-chip:hover{background:rgba(255,255,255,.22);color:#fff;transform:translateY(-1px)}

/* floating decorative map */
.hero-map{position:absolute;right:-40px;top:80px;width:520px;max-width:42vw;opacity:.5;z-index:-1;
  filter:drop-shadow(0 10px 30px rgba(0,0,0,.3))}
@media (max-width:980px){.hero-map{display:none}}

/* ---------- STAT DASHBOARD (overlap) ---------- */
.dash-wrap{margin-top:-104px;position:relative;z-index:5}
.dash{background:#fff;border-radius:var(--r-lg);box-shadow:var(--sh-xl);border:1px solid var(--line);
  overflow:hidden}
.dash-top{display:grid;grid-template-columns:1.15fr repeat(4,1fr) 1.5fr;align-items:stretch}
.stat{padding:30px 24px;border-right:1px solid var(--line);display:flex;flex-direction:column;gap:7px}
.stat .lab{font-size:var(--fs-body-s);color:var(--ink-3);font-weight:600}
.stat .num{font-size:var(--fs-h-l);font-weight:800;color:var(--primary-deep);letter-spacing:-.02em;line-height:1;
  display:flex;align-items:baseline;gap:3px}
.stat .num small{font-size:var(--fs-body-m);font-weight:600;color:var(--ink-3)}
.stat.feat{background:var(--primary-tint)}
.stat.feat .num{font-size:var(--fs-display-s);color:var(--primary)}
.delta{display:inline-flex;align-items:center;gap:3px;font-size:var(--fs-body-xs);font-weight:700;color:var(--danger)}
.delta svg{width:12px;height:12px}
.delta.up{color:var(--eco)}
.dash-cta{background:var(--primary-deep);color:#fff;display:flex;flex-direction:column;justify-content:center;
  gap:6px;padding:26px 26px;position:relative;overflow:hidden}
.dash-cta .pin{position:absolute;right:18px;top:18px;width:40px;height:40px;opacity:.25}
.dash-cta b{font-size:var(--fs-h-s);font-weight:700;letter-spacing:-.01em;display:flex;align-items:center;gap:8px}
.dash-cta span{font-size:var(--fs-body-s);color:#b9c8e6}
.dash-cta a{margin-top:10px;display:inline-flex;align-items:center;gap:7px;font-weight:700;color:#9fc0ff;font-size:var(--fs-body-s)}
.dash-cta a svg{width:16px;height:16px;transition:.18s}
.dash-cta:hover a svg{transform:translateX(4px)}

.dash-tick{display:flex;border-top:1px solid var(--line);background:var(--bg-2)}
.tick{flex:1;display:flex;align-items:center;gap:12px;padding:15px 24px;min-width:0}
.tick+.tick{border-left:1px solid var(--line)}
.tick .tag{display:inline-flex;align-items:center;gap:5px;font-size:var(--fs-body-xs);font-weight:700;flex:none}
.tick .tag svg{width:15px;height:15px}
.tick .msg{font-size:var(--fs-body-s);color:var(--ink-2);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1}
.tick .date{font-size:var(--fs-body-xs);color:var(--ink-4);flex:none}

/* ---------- SERVICE GRID ---------- */
.svc-tabs{display:inline-flex;gap:4px;background:var(--bg-1);border:1px solid var(--line);
  border-radius:var(--r-pill);padding:5px;margin:0 auto}
.svc-tabs [data-tab]{border:0;background:transparent;padding:10px 20px;border-radius:var(--r-pill);
  font-weight:600;font-size:var(--fs-body-s);color:var(--ink-3);transition:.15s}
.svc-tabs [data-tab].on{background:var(--primary);color:#fff;box-shadow:var(--sh-sm)}
.svc-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;margin-top:40px}
.svc-card{display:flex;flex-direction:column;align-items:center;text-align:center;gap:14px;padding:34px 18px;
  background:#fff;border:1px solid var(--line);border-radius:var(--r-md);transition:.2s var(--ease);cursor:pointer}
.svc-card:hover{transform:translateY(-5px);box-shadow:var(--sh-lg);border-color:transparent}
.svc-card:hover .ico-tile{background:var(--primary);color:#fff;transform:scale(1.06)}
.svc-card .t{font-size:var(--fs-h-xs);font-weight:700;color:var(--ink)}
.svc-card .d{font-size:var(--fs-body-s);color:var(--ink-3);margin-top:-7px}

/* ---------- 援  李몄뿬 (dark band) ---------- */
.civic{background:linear-gradient(135deg,var(--primary-deep),var(--p80));color:#fff;position:relative;overflow:hidden}
.civic::before{content:"";position:absolute;right:-10%;top:-30%;width:55%;height:160%;
  background:radial-gradient(circle,rgba(120,170,255,.18),transparent 65%)}
.civic .section-head{margin-bottom:26px}
.civic h2{color:#fff}
.civic-tabs{display:flex;gap:24px;border-bottom:1px solid rgba(255,255,255,.16);margin-bottom:28px}
.civic-tabs [data-tab]{border:0;background:transparent;color:#9fb6e0;font-weight:700;font-size:var(--fs-h-xs);
  padding:0 0 14px;position:relative;transition:.15s}
.civic-tabs [data-tab].on{color:#fff}
.civic-tabs [data-tab].on::after{content:"";position:absolute;left:0;right:0;bottom:-1px;height:3px;background:#fff;border-radius:3px}
.civic-card{width:330px;background:#fff;border-radius:var(--r-md);padding:26px;color:var(--ink-2);
  display:flex;flex-direction:column;gap:14px;transition:.2s var(--ease)}
.civic-card.hot{background:var(--primary);color:#fff}
.civic-card .badges{display:flex;gap:7px}
.civic-card .ttl{font-size:var(--fs-h-xs);font-weight:700;color:var(--ink);line-height:1.4;
  display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;min-height:2.8em}
.civic-card.hot .ttl{color:#fff}
.civic-card .meta{display:flex;justify-content:space-between;align-items:center;font-size:var(--fs-body-s);
  color:var(--ink-3);border-top:1px solid var(--line);padding-top:14px;margin-top:auto}
.civic-card.hot .meta{color:#cfe0ff;border-color:rgba(255,255,255,.25)}
.civic-card .meta b{color:var(--primary)}
.civic-card.hot .meta b{color:#fff}

/* ---------- SCHEDULE ---------- */
.sch-card{width:268px;background:#fff;border:1px solid var(--line);border-radius:var(--r-md);
  padding:24px;display:flex;flex-direction:column;gap:14px;transition:.2s var(--ease)}
.sch-card:hover{transform:translateY(-4px);box-shadow:var(--sh-lg);border-color:transparent}
.sch-card .badges{display:flex;gap:7px}
.sch-card .ttl{font-size:var(--fs-h-xs);font-weight:700;color:var(--ink);line-height:1.4;
  display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;min-height:2.8em}
.sch-card .loc{display:flex;align-items:center;gap:6px;font-size:var(--fs-body-s);color:var(--ink-3)}
.sch-card .loc svg{width:16px;height:16px;color:var(--primary);flex:none}
.sch-card .when{display:flex;align-items:flex-end;justify-content:space-between;border-top:1px solid var(--line);padding-top:14px}
.sch-card .day{font-size:48px;font-weight:800;color:var(--primary-deep);line-height:.9;letter-spacing:-.03em}
.sch-card .ym{font-size:var(--fs-body-s);color:var(--ink-3);text-align:right;line-height:1.5}

/* ---------- EDU +  묒쓽 꾪솴 ---------- */
.edu-split{display:grid;grid-template-columns:1.7fr 1fr;gap:30px;align-items:start}
.edu-list{display:flex;flex-direction:column}
.edu-item{display:flex;gap:18px;align-items:flex-start;padding:22px 4px;border-top:1px solid var(--line);transition:.15s}
.edu-item:first-child{border-top:0}
.edu-item:hover{background:#fff;border-radius:var(--r-sm);padding-inline:18px;box-shadow:var(--sh-sm)}
.edu-item .tags{display:flex;gap:7px;flex:none;flex-direction:column;align-items:flex-start;width:96px}
.edu-item .body .t{font-size:var(--fs-h-xs);font-weight:700;color:var(--ink);line-height:1.45}
.edu-item .body .d{font-size:var(--fs-body-s);color:var(--ink-3);margin-top:6px;
  display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
.edu-item .body .when{font-size:var(--fs-body-s);color:var(--ink-4);margin-top:8px}
.side-stack{display:flex;flex-direction:column;gap:20px}
.promo{border-radius:var(--r-md);padding:26px;position:relative;overflow:hidden;color:#fff;min-height:150px;
  display:flex;flex-direction:column;justify-content:space-between}
.promo.energy{background:#1a2e22 url("https://images.unsplash.com/photo-1509391366360-2e959784a276?auto=format&fit=crop&w=900&q=70") center/cover}
.promo.energy::after{content:"";position:absolute;inset:0;background:linear-gradient(120deg,rgba(10,40,26,.86),rgba(20,60,40,.5))}
.promo>*{position:relative;z-index:1}
.promo h3{font-size:var(--fs-h-s);font-weight:700;display:flex;align-items:center;gap:8px}
.promo h3 svg{width:20px;height:20px}
.promo p{font-size:var(--fs-body-s);color:rgba(255,255,255,.85);margin-top:6px}
.promo .go{align-self:flex-end;width:44px;height:44px;border-radius:50%;background:rgba(255,255,255,.18);
  display:flex;align-items:center;justify-content:center;transition:.18s;border:1px solid rgba(255,255,255,.3)}
.promo .go:hover{background:#fff;color:var(--primary-deep)}
.promo .go svg{width:20px;height:20px}
.consult{background:var(--primary);color:#fff}
.consult::after{display:none}
.consult .conrows{display:grid;grid-template-columns:1fr 1fr;gap:16px 20px;margin-top:8px}
.conrow .ctop{display:flex;justify-content:space-between;align-items:baseline;font-size:var(--fs-body-s);color:#d6e2fb}
.conrow .cnum{font-size:var(--fs-h-s);font-weight:800;color:#fff;margin-top:2px}
.conrow .cnum small{font-size:var(--fs-body-xs);font-weight:600;color:#bcd0fb}
.conrow .bar{height:6px;border-radius:99px;background:rgba(255,255,255,.2);margin-top:8px;overflow:hidden}
.conrow .bar i{display:block;height:100%;border-radius:99px;background:#9fc0ff}

/* ---------- 愿  ⑥궗 댄듃 (green band) ---------- */
.family{background:#10271a url("https://images.unsplash.com/photo-1511497584788-876760111969?auto=format&fit=crop&w=1920&q=70") center/cover;position:relative;color:#fff}
.family::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(8,28,16,.9),rgba(10,34,20,.84))}
.family .wrap{position:relative;z-index:1}
.fam-grid{display:flex;flex-wrap:wrap;gap:12px;margin-top:24px}
.fam-link{display:flex;align-items:center;gap:10px;padding:16px 22px;border-radius:var(--r-sm);
  background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.16);color:#eaf3ec;font-weight:600;
  font-size:var(--fs-body-m);transition:.16s var(--ease)}
.fam-link:hover{background:rgba(255,255,255,.16);transform:translateY(-2px)}
.fam-link svg{width:20px;height:20px;color:#7fd3a3}

/* ---------- to top ---------- */
.totop{position:fixed;right:22px;bottom:22px;width:48px;height:48px;border-radius:50%;background:#fff;
  border:1px solid var(--line-2);box-shadow:var(--sh-md);display:flex;align-items:center;justify-content:center;
  color:var(--ink-2);z-index:120;opacity:0;visibility:hidden;transform:translateY(10px);transition:.25s}
.totop.show{opacity:1;visibility:visible;transform:none}
.totop:hover{color:var(--primary);border-color:var(--primary)}
.totop svg{width:22px;height:22px}

/* ---------- RESPONSIVE ---------- */
@media (max-width:1080px){
  .dash-top{grid-template-columns:repeat(3,1fr)}
  .stat,.dash-cta{border-bottom:1px solid var(--line)}
  .dash-cta{grid-column:1/-1}
  .dash-tick{flex-direction:column}
  .tick+.tick{border-left:0;border-top:1px solid var(--line)}
  .svc-grid{grid-template-columns:repeat(2,1fr)}
  .edu-split{grid-template-columns:1fr}
}
@media (max-width:860px){
  .hero-inner{padding-top:48px;padding-bottom:120px}
  .hero h1{font-size:var(--fs-display-s)}
  .dash-top{grid-template-columns:repeat(2,1fr)}
  .dash-top .feat{grid-column:1/-1}
}
@media (max-width:560px){
  .svc-grid{grid-template-columns:repeat(2,1fr);gap:12px}
  .dash-top{grid-template-columns:1fr}
  .searchbar{flex-wrap:wrap;border-radius:var(--r-lg);padding:14px}
  .searchbar .btn{width:100%}
  .edu-item .tags{flex-direction:row;width:auto}
}