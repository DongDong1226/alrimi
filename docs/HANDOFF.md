# 인수인계 — 우리동네 개발사업 알리미

> **"이어서 작업하자"로 시작한 새 세션은 이 문서를 먼저 읽고 §2 로 간다.**
> 규칙·구조·왜 그렇게 만들었는지는 `CLAUDE.md`에.
> 처음 배포 설정을 하는 사람은 `docs/SETUP.md`에.

- 마지막 갱신: **2026-08-06 (저녁 — 화면·기능 개선 세션)**
- 저장소: `DongDong1226/alrimi` (**공개**)
- 배포: **https://dongdong1226.github.io/alrimi/** ← 지금 살아 있다
- 목표: 9월 데모에서 실제로 돌아가는 화면

> ### 🔴 새 세션이 가장 먼저 알아야 할 세 줄
> 0. **8/6 저녁에 화면을 크게 손봤다** (부분 선택 · 전국 · 내 위치 · 동네 경계 · 범위 3단).
>    코드 시험은 통과했지만 **사람 눈으로는 아직 안 봤다.** §2 "사람 눈으로 확인 필요"부터.
> 1. **화면·데이터·배포는 전부 정상.** 사이트는 48건으로 잘 돌아간다. 급한 불은 없다.
> 2. **수집만 아직 수동이다.** 매일 아침 `tools\run_daily.bat` 더블클릭. 이건 실패한 적이 없다.
> 3. **정해야 할 것 하나: 수집을 어디서 돌릴지** (§2 1순위).
>    GitHub 무료 러너의 직접 접속은 불가로 결론 났다(6번 중 1번 성공).
>    선택지 = ① 담당자 PC에 self-hosted 러너 ② AWS 서울 서버 ③ 지금처럼 수동 ④ WARP 우회(아래).
> 4. **세션 마지막에 WARP 우회 장치를 넣어 뒀다 — 아직 시험 전이다.**
>    Actions 에서 `EIASS 연결 확인 (시험용)` 을 한 번 돌리면 직접/터널 두 길의 성패가
>    주석(Annotations)에 한국어로 나온다. **터널이 성공하면 아무것도 더 안 해도**
>    07시·13시 자동 수집이 그 길로 살아난다(collect.yml 에 이미 내장). §2 참고.
>
> 커밋·푸시는 사람이 GitHub Desktop에서 직접 한다. **지금 고친 것들이 아직 안 올라갔을 수 있으니
> `git status`부터 확인할 것.**

---

## 0. 한 문단 요약 — 지금 어디까지 왔나

**만들려던 기능은 다 만들었고, 배포도 잘 돌아간다.** 전국 공람 관련 사업 48건을 수집해
GitHub Pages에 올렸고, 지도·지오코딩·AI 해석·설명회 안내·의견 제출 안내가 전부 실제
데이터로 동작한다. 8/2~8/3에 화면을 실사용 기준으로 크게 다듬었다 — 환경영향분석
가독성, 휴대폰 대응, 페이지 나누기, 지도 마커 색, 반경 판정 버그, 그리고 가장 중요하게
**"공람 종료일"이 아니라 "의견제출 마감일" 기준으로 사업을 보여주도록 규칙 자체를 바꿨다**
(단, AI 해석만은 법적으로 공람 기간에만 보여준다 — 절대 헷갈리면 안 되는 부분).

**2026-08-06에 자동 수집을 시도했으나 실패로 끝났다. 지금도 수집은 한국 PC에서 손으로 돌린다.**
그 전까지 적혀 있던 "EIASS가 해외 IP를 막는다"는 **사실이 아니었다**(전 세계 17곳에서 정상 접속).
그런데 **GitHub 러너에서만 유독 EIASS 접속이 안 된다** — 6번 시도해 1번 성공(약 17%).
결정적으로, 같은 러너·같은 순간에 **VWorld는 0.43초에 붙는데 EIASS만 시간 초과**했다.
그래서 원인은 "해외 차단"도 "Azure↔한국 경로"도 아닌 **EIASS 쪽 문제**로 좁혀졌다(§4).

**다음 세션에서 정할 가장 큰 결정: 수집을 어디서 돌릴 것인가**(§2 맨 위).
선택지는 ① 담당자 PC에 self-hosted 러너 설치 ② AWS 서울 서버 ③ 지금처럼 수동 유지 세 가지다.
그때까지는 매일 아침 `tools\run_daily.bat`을 더블클릭한다. 이건 **한 번도 실패한 적이 없다.**

화면·데이터·배포는 전부 정상이다. 사이트는 48건으로 잘 돌아가고 있다.

---

## 1. 새 세션에서 처음 할 일 (환경 띄우기)

```bash
cd C:\dev\EIASS
python -m http.server 8000
```

브라우저에서 `http://localhost:8000` → 동네 선택 → 홈 화면.
(`file://` 로 열면 json을 못 읽어 표본 데이터로 대체된다. 반드시 서버로.)

- 관리자 진입: 첫 화면 맨 아래 "기후에너지환경부" 클릭 → 비밀번호 `admin1234`
- **내 PC에서는 지도가 안 보이는 게 정상이다.** `assets/config.js`가 비어 있어서다.
  보려면 관리자 화면에 `.env`의 localhost용 VWorld 키를 넣는다.
  (배포 사이트는 Actions가 키를 심어 주므로 그냥 보인다)
- `.env`에 `VWORLD_KEY`, `ANTHROPIC_API_KEY` 있음 — **확인함**
- 코드를 고친 뒤 로컬에서 확인할 때는 `index.html?v=아무숫자`로 열 것.
  안 그러면 브라우저가 옛 CSS/JS를 계속 쓴다(§5 함정 참고).

---

## 2. 다음에 할 일 ★ 여기부터 보면 된다

### ★★ 1순위 — 결정: 수집을 어디서 돌릴 것인가 (사람이 정해야 함)

**이것만 정하면 나머지는 금방 끝난다.** 2026-08-06에 GitHub 자동 수집을 켜 봤지만
EIASS 접속이 안 돼서 3번 연속 실패했다(§4에 측정치 전부 있음). 선택지 세 가지:

| 방법 | 비용 | 안정성 | 사람 손 | 걸림돌 |
|---|---|---|---|---|
| **A. 담당자 PC에 self-hosted 러너 설치** | 0원 | **높음** (한국 IP, 로컬은 매번 0.08초·무실패) | 없음 | PC가 켜져 있어야 함. 설치 1회 필요 |
| **B. AWS 서울 리전 서버** | 월 7천원 | 높음 | 없음 | 2~3명 비개발자가 서버(보안 패치·접속키)를 관리해야 함 |
| **C. 지금처럼 수동 유지** | 0원 | 높음 | **매일 아침 더블클릭** | 사람이 기억해야 함. 담당자 부재 시 멈춤 |
| **D. GitHub 러너 + WARP 우회** ← 8/6 저녁 추가 | 0원 | **미지수 — 시험 대기** | 없음 | 아래 "지금 당장 할 일" 한 번이면 판명 |

- **GitHub 무료 러너의 직접 접속은 사실상 불가**로 결론 났다. 6번 중 1번 성공.
- **D의 근거**: EIASS 는 해외를 안 막고(17/20곳 정상), **Cloudflare 서버에서는 정상 응답했다**(확인함).
  문제는 Azure 대역뿐. 그래서 `collect.yml`이 **EIASS 행 트래픽만** Cloudflare WARP 터널
  (무료·계정 불요)로 태우게 해 뒀다. GitHub·VWorld·Anthropic 트래픽은 터널을 안 탄다.
- 9월 데모까지는 C로도 충분하다. 서두를 필요 없다.
- **A를 고르면**: 새 세션에서 "self-hosted 러너 설치하자"고 하면 명령어를 단계별로 안내한다.
  `collect.yml`의 `runs-on: ubuntu-latest`를 `runs-on: self-hosted`로 한 줄 바꾸면 된다.

**지금 당장 할 일 (2분) — D가 되는지 확인:**
1. Actions 탭 → `EIASS 연결 확인 (시험용)` → `Run workflow`
2. 끝나면 실행 페이지의 주석(Annotations)을 본다. `[판정]` 줄에 결론이 한국어로 나온다.
   - **`WARP 터널이 통한다`** → 끝. 아무것도 더 안 해도 07시·13시 자동 수집이 이 길로 돈다.
     며칠 초록불만 확인하면 자동화 완성이다.
   - **`둘 다 실패`** → GitHub 러너로는 방법이 없는 것. A(self-hosted 러너)로 간다.
   - 한 번으로 단정하지 말고 **두세 번** 돌려 볼 것 (§4의 교훈 2).

### 그때까지의 운영 방법 (지금 상태)

1. **매일 아침 `C:\dev\EIASS\tools\run_daily.bat` 더블클릭.** 수집→커밋→푸시까지 한 번에 한다.
   푸시하면 `deploy.yml`이 사이트를 자동으로 다시 올린다. **이 경로는 한 번도 실패한 적이 없다.**
2. `collect.yml`의 자동 실행(07시·13시)은 **켜 둔 채로 두었다.**
   - 거의 실패하지만 **무해하다** — 커밋 전에 멈추므로 자료가 망가지지 않고, 비용도 0원이다.
   - 어쩌다 성공하면 그날은 자동으로 갱신되니 손해 볼 게 없다.
   - **실패 메일이 하루 두 번 오는 게 싫으면** `collect.yml`의 `schedule:` 아래 `- cron:` 두 줄을
     주석 처리하면 된다. 그것 말고는 끌 이유가 없다.

### 2순위 — 데이터가 많이 늘었다 (확인 필요)

로컬 재현 중에 확인한 것: **전략환경영향평가 목록이 43건으로 늘었다** (8/3에는 23건).
아직 `projects.json`에는 반영되지 않았다(48건 그대로). 다음에 `run_daily.bat`을 돌리면
새 사업이 많이 들어와서 **평소보다 오래 걸리고 AI 비용도 몇 천 원 나올 수 있다.** 정상이다.

### ★ 사람 눈으로 확인 필요 — 캡처해서 알려 주세요

코드로는 확인했습니다(가로 넘침 0, 콘솔 오류 0, 동작 시험 통과).
남은 건 **"보기 좋은가 / 말이 자연스러운가"** 뿐이라 브라우저로 직접 봐야 합니다.
`http://localhost:8000/index.html?v=아무숫자` 로 열고, 이상하면 그 화면을 캡처해 주세요.

> 내 PC 에서는 **지도가 안 나오는 게 정상**입니다(`assets/config.js`가 비어 있어서).
> 지도·경계까지 보려면 관리자 화면(`admin1234`)에 `.env` 의 localhost 용 VWorld 키를 넣으세요.
> 배포 사이트에서는 그냥 보입니다.

**이번에 새로 만든 것 (2026-08-06 저녁)**

1. **첫 화면 — 시·도만 골라도 되는지.** 시·도를 고르면 시·군·구 칸이 `전체`로 열리고
   읍·면·동 칸은 잠깁니다. 잠긴 칸이 "고장난 것"처럼 보이지 않는지
2. **첫 화면 — 맨 위 `전국`.** 고르면 아래 두 칸이 다 잠기고, 들어가면 전국 44건이 보입니다
3. **`지금 내 위치로 보기` 단추** — 어두운 첫 화면 위에서 잘 보이는지.
   **휴대폰에서 꼭 눌러 보세요** (위치 권한 팝업 → 허용 → 동네가 자동으로 잡힘).
   ※ 배포 사이트(https)나 localhost 에서만 동작합니다
4. **보는 범위 단추 3개** (`○○ 안` / `우리 집 반경 3km` / `전국`) —
   홈 화면 "우리 동네 사업" 제목 오른쪽. 세 개가 한 줄에 들어가는지, 글자가 안 잘리는지
5. **지도 화면 위쪽 띠의 범위 단추 3개** — 휴대폰에서는 **두 줄**로 내려갑니다.
   그게 답답해 보이지 않는지 (지도가 그만큼 줄어듭니다)
6. **동네 경계 초록 점선** — 지도와 홈 화면 미니 지도 둘 다.
   점선 색·굵기가 반경 원(파란 점선)과 헷갈리지 않는지, 미니 지도 축척이 알맞은지
7. **범위를 좁혔을 때 빈 화면** — 대부분의 동네는 `○○ 안` 이 0건입니다.
   "반경 3km로 넓혀 보기 / 전국 사업 모두 보기" 두 단추가 나오는데, 그 안내 문구가
   **막다른 길처럼 느껴지지 않는지**가 가장 중요합니다
8. **지도 화면 주소칸의 `내 위치` 작은 단추** — `이 주소로 보기` 옆에 붙습니다

**8/2~8/3에 만들어 아직 눈으로 안 본 것 (그대로 남음)**

- 환경영향분석 탭 — 문장을 점으로 나눈 목록, 사업개요 카드 강조가 읽기 좋은지
- 주민 설명회 안내 목록 — 회차별 일시·장소 짝짓기, "지남"/"생략"/"미정" 뱃지
- 페이지 나누기 단추 모양(EIASS 원본 캐러셀 스타일로 맞춤)
- 지도 마커 색(전략=짙은 남색 원 / 환경=앰버 마름모) — 실제 지도 위에서 잘 보이는지
- 휴대폰 지도 화면의 "지도 ↔ 사업 내용" 전환, `지도 보기` 버튼
- 이용안내 모달의 법 조문 인용 — **국가법령정보센터에서 원문 조문 재대조 권장**
  (특히 전략환경영향평가 공람기간 20~40일, 시행령 제13조 부분)

### 확인이 필요한 데이터 이상 — 다음 수집 때 로그 확인

- **11건의 신규 수집 사업에 AI 해석이 비어 있다** (2026-08-03 수집분).
  전부 공람 종료 후 유예기간에 새로 잡힌 사업들 — 요약문 PDF를 못 찾았거나
  AI 호출이 실패했을 가능성. `data/build_log.txt`에서 이 사업들 로그를 확인할 것.
  (참고: 이 사업들은 이미 공람이 끝나 AI 해석을 화면에 보여주지도 않으므로 급하지 않음.
  다만 왜 실패했는지는 알아 두는 게 좋음 — 다른 사업에도 같은 문제가 있을 수 있음)

### 기능 추가 후보 (우선순위 순, 안 바뀜)

| 무엇 | 왜 / 메모 |
|---|---|
| **공람 마감 알림 (`.ics` 다운로드)** | 서버 없이 가능. "의견 낼 수 있는 기간"이 이 서비스의 존재 이유라 효과가 가장 크다 |
| ~~읍면동 경계 표시~~ | **2026-08-06 완료** (§7) |
| 위치 계속 따라가기(`watchPosition`) | 지금은 누를 때 한 번만 잡는다. 걸어 다니며 보는 화면이 아니라 급하지 않다 |
| 사후환경영향조사 추가 | 지금은 범위 밖. 수집 대상을 넓히는 일 |
| 접근성 점검 (스크린리더) | 키보드·aria는 손봤지만 실제 리더로는 안 돌려봤다 |

### 손댈 수 없다고 결론 난 것 (다시 시도하지 말 것)

> ⚠️ 여기 있던 **"GitHub Actions에서 EIASS 수집 — 해외 IP 차단"은 틀린 항목이었다.**
> 2026-08-06에 실측으로 뒤집혀 지웠다(§4). **이 목록에 무언가를 넣을 때는
> 한 번 실패한 것으로 단정하지 말고, 재현되는지 두세 번은 확인하고 넣을 것.**

- **사업 구간만 잘라낸 하천 노선** — 지금은 하천 전체가 나온다. 화면에 그 사실을 표시해 둠
- **도로·철도 선형 사업의 노선 자동 추출** — 관리자가 직접 그리는 것 외에 방법 없음
- **`<select>` 펼친 목록의 글꼴·색** — 윈도우가 그리는 것이라 CSS로 못 바꾼다

---

## 3. 지금 상태

### 동작 확인된 것 (배포 사이트 기준)

| 기능 | 상태 |
|---|---|
| EIASS 수집 (전략 + 환경영향평가) | 동작 — 48건 (공람 중 + 의견제출 유예기간 포함) |
| 요약문 PDF → AI 환경영향분석 (8개 고정 항목) | 동작 — 48건 중 37건 (11건 원인 미확인, §2) |
| VWorld 지오코딩 (주소 → 좌표) | 동작 — 46건 좌표 확보 |
| 지도 (Leaflet + VWorld 타일), 마커·반경 원 | 동작. 마커 색·모양 8/3 변경(원본 확인 필요) |
| 하천 사업 노선 자동 표시 | 동작 — 5건 |
| 선형 사업 반경 판정 (노선까지 최단거리) | 동작 — 8/3 추가. 대표주소만으론 반경 밖으로 잘못 판정되던 문제 수정 |
| 주민 설명회 안내 (일시·장소·의견받는곳) | 동작 — 8/3 신규. 회차별 짝짓기, 지남/생략/미정 구분 |
| 전국 협의 진행 중 건수 (전략/환경/소규모) | 동작 — 8/3 신규. EIASS에서 매일 자동 수집, 비용 0원 |
| 보는 범위 3단 전환 (동네 안 / 반경 / 전국) | 동작 — 8/6 개편. 고른 동네 모양에 맞는 기본값이 자동으로 잡힌다 |
| 시·도만 골라도 보기 · 시·도 칸의 `전국` | 동작 — 8/6 신규 |
| 지금 내 위치로 보기 (GPS → 동네 자동 설정) | 동작 — 8/6 신규. https·localhost 에서만 |
| 읍면동·시군구 경계 표시 + 경계 기준 사업 가리기 | 동작 — 8/6 신규. 노선이 경계를 지나는 사업도 잡는다 |
| 동네 변경 모달 | 동작 — 첫 화면으로 안 튄다 |
| 브라우저 뒤로가기 | 동작 — 8/3 추가. 화면 전환 기록을 남겨 이전 화면으로 복귀 |
| 의견 제출 → EIASS 안내 후 이동 | 동작 |
| 의견제출 마감 기준 필터링 + AI 해석은 공람기간만 차단 | 동작 — 8/3 규칙 변경 (§4 CLAUDE.md 참고) |
| 관리자 노선 직접 그리기 | 동작 — 8/3부터 지도 화면에서 완전히 분리(관리자 전용) |
| 휴대폰 반응형 (글자·버튼 크기, 지도/상세 전환) | 동작 — 8/3 개선 |
| 페이지 나누기 (사업 카드/설명회 목록) | 동작 — 8/3 신규 |
| **푸시 → 자동 배포** | 동작. 배포마다 파일 버전 자동 갱신(캐시 문제 해결) |
| **손으로 수집** (`run_daily.bat`, 한국 PC) | **동작 — 한 번도 실패한 적 없음.** 지금은 이걸로 운영한다 |
| ~~GitHub Actions 자동 수집~~ | **거의 안 됨** — 켜 뒀지만 6번 중 1번 성공. EIASS 접속 문제 (§2·§4). 실패해도 무해 |
| 수집 실패 시 안전장치 | 동작 — 8/6 신규. 재시도 4회+1회 / 목록 실패 시 저장 안 함 / 건수 급감 시 커밋 중단 |
| 실패 원인을 밖에서 읽기 | 동작 — 8/6 신규. `collect.yml`이 로그 끝부분을 GitHub 주석으로 내보낸다 |

### 데이터 (`data/projects.json` — git에 포함된다)

- 생성일 2026-08-03, **48건** (전략 33 + 환경 15 — 의견제출 유예기간까지 담아서 늘어남)
- AI 해석 37건 / 좌표 46건 / 노선 도형 5건
- `stats.underReview`: 전략 115 · 환경 121 · 소규모 391 = **전국 627건** (매일 자동 갱신)

> 반경 안에 사업이 0건으로 나오는 건 **정상일 수 있다.** 우리 동네 근처에 마침
> 없을 뿐이다. 상단 "전국" 스위치를 누르면 다 보인다.

### 자동화 구조 (2026-08-06 현재 — 수집만 아직 수동)

```
  ■ 지금 실제로 쓰는 길 (한 번도 실패한 적 없음)

  [담당자 PC — 한국]                       [GitHub]
  run_daily.bat 더블클릭
    수집 → 커밋 → 푸시  ─────────────▶  deploy.yml → 사이트 배포 ✅


  ■ 켜 두었지만 거의 실패하는 길

  매일 07시·13시(한국)
        │
        ▼
   collect.yml ──▶ EIASS 수집 ❌ 접속 안 됨 (6번 중 1번 성공)
                      │
                      ▼
                  아무것도 저장하지 않고 멈춤 → 자료 안전, 비용 0원
```

- **수집은 사람이 한다.** 매일 아침 `tools\run_daily.bat` 더블클릭.
- **배포는 자동이다.** 푸시만 되면 `deploy.yml`이 알아서 올린다. 이쪽은 문제없다.
- **자동 수집이 실패해도 사이트는 안 죽는다.** 어제 자료로 정상 동작한다
  (화면이 날짜를 다시 판단하므로 기한 지난 사업이 새어 나가지 않는다).
- 실패하면 GitHub이 저장소 주인에게 메일을 보낸다. 하루 두 번 온다.
  귀찮으면 `collect.yml`의 `- cron:` 두 줄을 주석 처리한다.

### 비용 (`claude-haiku-4-5`, 실측 기반)

| | 비용 |
|---|---|
| 사업 1건당 | 약 $0.02 |
| 매일 증분 (신규 1~3건 + 협의건수 조회 0원) | 월 $1~2 (3,000원 이하) |

증분 수집이 비용의 90% 이상을 줄인다. 서버 비용은 0원(사람 PC에서 돌리므로).

> **2026-08-06에 소넷(`claude-sonnet-5`)을 시험했다가 되돌렸다.**
> 품질은 좋았다 — 시험에서 8개 항목 중 원문에 없는 4개를 정확히 `null` 처리했다.
> 되돌린 이유는 값이 3배($0.02 → $0.05~0.08/건)인데, 같은 날 수집이 계속 실패하면서
> 재시도로 비용이 더 나갈 위험이 있었기 때문이다. 수집이 안정된 뒤에 다시 검토할 만하다.
> **다시 바꿀 때는 `max_tokens`를 6000 → 16000으로 반드시 같이 올려야 한다**(§5 함정 참고).

증분 수집이 비용의 90% 이상을 줄인다. 돈 문제는 사실상 없다.
**서버 비용은 0원이다** — GitHub Actions 무료 사용량 안에서 돈다(공개 저장소는 무제한).
하루 두 번 돌아도 마찬가지다. 검토하던 AWS Lightsail 월 7천원은 안 쓰게 됐다.

---

## 4. 중요한 결정과 이유 (되돌리지 말 것)

세부 규칙은 전부 `CLAUDE.md`에 있다. 여기는 **왜 그렇게 결정했는지**만 요약.

### 기준은 "공람 종료일"이 아니라 "의견제출 마감일" (2026-08-03 변경)
환경영향평가법 시행령 제38조: 공람이 끝난 뒤 7일 이내까지 의견을 낼 수 있다.
공람 종료일로 끊으면 아직 의견을 낼 수 있는 사업이 화면에서 사라진다.

### 단, 환경영향분석(AI 해석)만은 "공람 기간"에만 보여준다 ★ 헷갈리기 쉬움
평가서 초안 자체가 법적으로 공람 기간에만 열람 가능하다. 의견은 그 뒤에도 낼 수 있지만,
**해석은 공람 기간이 끝나면 바로 닫는다.** `viewOpen`(공람 중)과 `open`(의견 가능)을
코드에서 분리해 두었다 — 절대 하나로 합치지 말 것. `CLAUDE.md` "기간 규칙" §4 참고.

### AI는 번역만 한다
항목을 8개로 고정, 없으면 `null` + "관련 내용이 없습니다". 판단·홍보 문구 금지.

### 도면(그림)을 읽어 노선을 뽑지 않는다
좌표 격자가 없어 픽셀→위경도 변환 기준점이 없다. 대신 하천 이름으로 VWorld 하천망도에서
실제 도형을 받는다. 하천 사업에만 적용(도로 요약문의 "지나가는 하천" 이름과 구분).

### 선형 사업은 주소 한 점이 아니라 노선까지의 최단거리로 반경을 판단한다 (8/3 추가)
대표 주소가 노선에서 멀리 떨어진 경우가 흔하다(파주 주소, 계양구 코앞 노선 등).
노선 도형이 있는 사업만 해당(도로·철도는 여전히 주소 한 점).

### 휴대폰용 사이트를 따로 만들지 않는다
같은 화면이 폭에 따라 바뀐다. 주소도 코드도 하나로 유지해야 2~3명이서 따라간다.

### EIASS 접속 — "해외 IP 차단"은 틀렸고, 그렇다고 GitHub에서 되지도 않는다 (2026-08-06) ★★

**이 항목은 원래 "수집은 한국에서만 돌린다 / EIASS가 해외 IP를 막는다"였다. 사실이 아니었다.**
같은 실수를 반복하지 않도록 어떻게 틀렸는지까지 남긴다.

**무엇이 틀렸나**
- 근거가 **2026-08-01에 딱 한 번 돌려 실패한 것** 하나뿐이었다. 재시도가 없었다.
- 원인을 밝히려고 `collect.yml`에 넣어 둔 '연결 확인' 단계는 **그 실패 뒤에 추가됐고,
  오늘까지 한 번도 실행된 적이 없었다.** 진단 도구를 만들어 놓고 써 보지 않았다.
- 그런데 결론은 `CLAUDE.md`·`HANDOFF.md`·`collect.yml` 세 곳에 "확인함"으로 박혔고,
  "다시 시도하지 말 것" 목록에까지 올라갔다. 그 뒤로 아무도 의심하지 않았다.
- 그 틀린 전제 위에서 **AWS Lightsail 이전(월 7천원 + 서버 관리)** 을 검토했다.

**실제로 확인한 것 (2026-08-05~06)**
- 전 세계 20개 노드 중 17곳 정상 — 미국 LA **0.49초**, 댈러스 1.09초, 독일·프랑스·네덜란드·러시아·인도·베트남
- GitHub 러너(미국 Azure)에서도 EIASS **0.23초**, VWorld 0.22초 정상
- `www.eiass.go.kr`은 AAAA(IPv6) 레코드가 없다 → IPv6 문제도 아니었다
- 다만 **간헐적으로 접속이 안 되는 것은 사실**이다. GitHub에서 3번 시험해 1번 성공:

  | 회차 | 러너 IP | 결과 |
  |---|---|---|
  | 1차 | `52.159.244.171` (San Jose) | DNS 조회 실패 (10초) |
  | 2차 | `9.234.149.180` (Boydton) | DNS는 됐는데 TCP 443 무응답 (20초) |
  | 3차 | `20.161.78.74` (Boydton) | 전부 정상 (0.23초) |

  **2·3차는 같은 데이터센터인데 결과가 달랐다.** 차단이라면 세 번 다 같은 지점에서 막혔어야 하고,
  실패 지점이 DNS·TCP로 제각각일 이유도 없다. 이건 차단이 아니라 불안정이다.

**그래서 어떻게 했나 — 그리고 그래도 안 됐다 (같은 날 오후 추가 기록)**

재시도 통로(`SESSION`, 4·8·16초 쉬며 4번)를 넣고, `collect.yml`에 1분 쉬고 통째로 한 번 더
시도하는 장치까지 넣은 뒤 실제로 3번 돌렸다. **3번 다 실패했다.**

| 실행 | 소요 | 실패 지점 |
|---|---|---|
| 00:38 | 100초 | 목록 받기 — 재시도 4번 소진 후 죽음(예외 처리가 없어 통째로 중단) |
| 00:50 | 148초 | 같음. 연결 확인 단계도 19초 걸림(둘 다 무응답) |
| 00:59 | 410초 | 2번 시도 모두 `ConnectTimeout`. **연결 확인: EIASS 실패(14.7초) / VWorld 성공(0.43초)** |

**마지막 줄이 결정적이다.** 같은 러너·같은 순간에 **VWorld는 0.43초에 붙는데 EIASS만 시간 초과**했다.
- "해외 IP 차단"이 아니다 → 전 세계 17곳에서 정상 접속된다.
- "Azure↔한국 경로 문제"도 아니다 → 같은 러너에서 VWorld(한국)는 멀쩡하다.
- 남은 설명은 **EIASS 쪽에서 특정 IP·대역을 불안정하게 거부한다**는 것뿐이다.

**최종 집계: GitHub 러너에서 EIASS 접속 6번 시도 → 1번 성공(약 17%).**
반면 **한국 PC에서는 매번 0.08초, 실패 0회.**

→ 결론: **GitHub 무료 러너로 수집하는 것은 사실상 불가.** 수집 위치를 정하는 것이 §2의 1순위다.

**교훈 두 가지**
1. **한 번 실패한 것을 "확인함"이라고 쓰지 말 것.** 특히 그 결론이 돈 쓰는 결정으로 이어질 때는.
2. **반대로, 몇 번 성공한 것을 "된다"고 단정하지도 말 것.** 3번 중 1번 성공을 보고
   "재시도로 덮을 수 있다"고 판단해 자동화를 켰지만, 실제로는 덮이지 않았다.
   접속이 안 될 때는 **몇 분 동안 계속** 안 되는 양상이라 재시도가 무력하다.

**추가 (같은 날 저녁): WARP 우회 장치를 넣었다 — 시험 대기**

원인이 "EIASS 쪽이 Azure 대역을 거르는 것"으로 좁혀졌으므로, 남은 수단은
**나가는 IP 를 바꾸는 것**뿐이다. 근거: Cloudflare 서버(미국)에서는 EIASS 가
정상 응답했다(r.jina.ai 실측). 그래서:

- `collect.yml` — 수집 전에 **EIASS 행 트래픽만**(해당 IP /32) Cloudflare WARP 터널
  (무료·계정 불요, `wgcf` 도구)로 라우팅한다. 순서: WARP(2408) → WARP(500) → 직접 → 실패 시
  비용 0원으로 안전 중단. GitHub·VWorld·Anthropic 트래픽은 터널을 타지 않는다.
- `check.yml` — 직접/WARP 두 길을 모두 시험해 `[판정]` 주석으로 결론을 낸다.
- **아직 러너에서 한 번도 안 돌려봤다.** 성패는 §2의 "지금 당장 할 일"로 확인한다.
- **입장 정리**: EIASS 는 해외 접속을 막지 않으므로(17/20곳 정상) 이것은 의도적 차단을
  뚫는 게 아니라 "다른 회선으로 거는 것"이다. 발주처가 그 사이트의 주인이기도 하다.
  EIASS 측이 원치 않는다는 신호가 보이면 이 방식은 중단하고 A(self-hosted)로 간다.

---

## 5. 알려진 함정 (다 실제로 겪은 것)

| 함정 | 증상 | 대응 |
|---|---|---|
| **EIASS 접속이 간헐적으로 안 됨** | 어떤 날은 DNS 조회 실패, 어떤 날은 TCP 무응답 (**차단 아님**, §4) | `build_data.py`의 `SESSION`이 4·8·16초 쉬며 4번 재시도. 하루 두 번 실행 |
| **한 번 실패를 "확인함"으로 단정** | 틀린 제약이 문서 3곳에 박혀 반년 갈 뻔했다 (§4) | 제약을 문서에 넣기 전에 **두세 번 재현**해 볼 것 |
| **목록 받다 끊기면 통째로 죽음** | 2026-08-06 수집 실행이 100초 만에 실패. 재시도 4번(≈78~100초)을 다 쓰고 예외가 그대로 터졌다 | `fetch_open_items` 를 try 로 감싸 **아무것도 저장하지 않고** 안내 후 중단. 반쪽 저장이 더 위험하다 |
| **반쪽 수집이 안전장치를 통과함** | 전략 43건만 받고 환경 16건을 놓치면 48→43건. 10% 감소라 `collect.yml` 의 "절반 이하면 중단"을 통과해 버린다 | 목록 실패 시 아예 저장하지 않는 것으로 막았다 |
| **소넷은 `max_tokens`를 생각+답변이 나눠 쓴다** | 모델만 소넷으로 바꾸고 `max_tokens=6000`을 그대로 두면 생각하다 자리를 다 써서 JSON이 잘린다 | 소넷으로 바꿀 때는 **16000으로 같이 올린다**. `b.type=="text"` 필터도 필수(생각 덩어리가 섞여 옴) |
| **Actions 로그는 저장소 주인만 본다** | 도와주는 사람이 실패 원인을 못 봐서 진단이 계속 막혔다 (`403 Must have admin rights`) | `::error::` / `::notice::` **주석**으로 내보내면 누구나 API로 읽는다. `collect.yml`·`check.yml`이 그렇게 한다 |
| **한 번 성공을 "된다"로 단정** | 3번 중 1번 성공을 보고 자동화를 켰다가 3번 연속 실패 | 접속이 안 될 땐 몇 분간 계속 안 된다. 재시도로 안 덮인다 (§4) |
| **시험용 `--limit`이 진짜 데이터를 덮어씀** | `--limit 3`으로 돌리면 48건이 6건으로 줄어 그대로 커밋됨 | `collect.yml`이 `limit` 있으면 커밋 안 함 + 건수 급감 시 중단 |
| **VWorld 키 앞뒤 공백** | 지도 타일은 나오는데 지오코더만 `INVALID_KEY` | 코드 3곳에서 `strip()`. 거부되면 길이부터 세어 본다 |
| **`.bat` 줄바꿈이 LF** | 더블클릭해도 조용히 안 열림 | 반드시 CRLF. `.gitattributes`로 고정 |
| **배치 `ERRORLEVEL`** | 멀쩡한 실행이 "실패"로 끝남 | `echo` 뒤 말고 명령 직후 바로 확인 |
| **목록 표가 두 개 / 날짜순 아님** | 중복 수집·누락 | 사업번호로 중복 제거, 빈 페이지 5쪽까지 넘겨봄 |
| **AI 응답 잘림** | JSON 파싱 실패로 해석 통째 유실 | `max_tokens` 6000, 잘리면 `stop_reason` 로그 |
| **증분 수집이 옛 값을 붙잡음** | `미정`으로 한 번 저장되면 영영 `미정` | `needs_detail_refresh()` — 안 정해진 값 있으면 상세 재조회(PDF·AI는 건너뜀) |
| **브라우저가 옛 app.js를 붙잡음** | 배포는 됐는데 화면은 옛 코드 | 자산 주소에 `?v=커밋SHA`를 배포 때마다 자동으로 심음(`deploy.yml`) |
| **격자 `1fr`이 상자보다 넓어짐** | 휴대폰에서 오른쪽이 잘려 보임(스크롤도 안 생김) | `minmax(0,1fr)` + `min-width:0` |
| **`hidden`이 안 먹힘** | 클래스 `display`가 브라우저 기본을 이김 | `[hidden]{display:none!important}` 리셋에 추가 |
| **일시·장소 순서로 짝지으면 틀림** | 기장군 사업: 일시·장소 순서가 다름 | 지역 이름으로 매칭, 안 되면 원문 통째로 표시 |
| **`file://`로 열기** | 표본 데이터로 대체됨 | 반드시 `python -m http.server 8000` |
| **EIASS 상세는 POST 전용** | 링크로 못 엶 | 숨은 폼 제출 (`openEiassSource`) |
| **윈도우 cp949 콘솔** | 한글 로그에서 죽음 | 파이썬은 `log()` 사용 |
| **시도가 16개** | 17개가 아님 | 광주+전남이 VWorld 최신 자료 기준 통합됨 |

---

## 6. 파일 지도

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 4개 + 모달 5개 |
| `assets/app.js` | 모든 화면 동작 |
| `assets/app.css` / `tokens.css` | 스타일 / 디자인 토큰 |
| `assets/config.js` | **빈 채로 커밋.** 배포 때 VWorld 키가 채워진다 |
| `tools/build_data.py` | EIASS 수집. `--full` / `--retry-analysis` / `--skip-stats` / `--limit` |
| `tools/build_regions.py` | 행정구역 목록 (한 번만 돌리면 됨) |
| `tools/run_daily.bat` | 수집 → 커밋 → 푸시. **반드시 CRLF**. 급할 때 손으로 돌리는 용도 |
| `.github/workflows/deploy.yml` | 푸시하면 배포. 키 심기 + 캐시 버스팅(버전 번호 삽입) |
| `.github/workflows/collect.yml` | **매일 07시·13시(한국) 자동 수집** → 커밋 → 배포 |
| `.github/workflows/check.yml` | 직접/WARP 두 경로로 EIASS에 닿는지 시험. `[판정]` 주석에 결론. 데이터 안 건드림 |
| `data/projects.json` | 수집 결과 + `stats.underReview`. **git 포함** |
| `data/regions.json` | 전국 행정구역 |
| `data/routes.json` | 관리자가 그린 노선 (없어도 정상) |
| `docs/SETUP.md` | 배포 설정 설명서 |
| `.env` | API 키 (**git 제외**) |

---

## 7. 지금까지 한 일 (요약, 자세한 건 git log)

**2026-08-01** — 실제 EIASS 데이터 연동, 지도·노선 기능, 전국 수집 자동화 구축,
GitHub Pages 배포 구성. (자세한 내용은 Notion "1차 작업 기록" 페이지 참고)

**2026-08-02 ~ 08-03** — 실사용 관점 전면 개선:
- 환경영향분석 가독성 개편(문장 목록화, 항목 없음 표시 개선)
- 휴대폰 반응형 전면 대응(글자·버튼 크기, 지도 화면 전환)
- 페이지 나누기, 관리자 전용 노선 그리기 분리, 지도 마커 색·모양 개선
- **"의견을 낼 수 있는 초안 공람" → "주민 설명회 안내"로 개편**(회차별 일시·장소, 전화번호 제거)
- EIASS 협의 진행 중 건수 자동 수집 → 대시보드 가짜 숫자를 실제 값으로 교체
- 선형 사업 반경 판정 버그 수정(노선까지 최단거리 반영)
- **기준을 공람 종료일 → 의견제출 마감일로 전환**, 단 AI 해석은 공람 기간에만 유지
- 이용안내를 환경영향평가법 시행령 조문 근거로 재작성
- 첫 화면 이름 입력·인사말 삭제, 브라우저 뒤로가기 수정, 배포 캐시 문제 해결

**2026-08-06** — **수집 자동화 시도 → 실패. 다만 잘못된 기록을 여러 개 바로잡았다.**

*밝혀낸 것*
- **"EIASS 해외 IP 차단"은 사실이 아니었다** — 전 세계 20개 노드 중 17곳 정상 접속 (§4)
- 그런데 **GitHub 러너에서만 EIASS가 안 된다** — 6번 중 1번 성공(17%)
- 결정적 증거: 같은 러너·같은 순간에 **VWorld 0.43초 성공 / EIASS 14.7초 시간초과**
  → Azure↔한국 경로 문제가 아니라 EIASS 쪽 문제

*고친 것*
- `build_data.py`에 재시도 통로(`SESSION`) — 연결 실패 시 4·8·16초 쉬며 최대 4번
- `collect.yml` 자동 실행 켬(07시·13시) + 1분 쉬고 통째로 1회 더 재시도
- **목록 받기 실패 시 아무것도 저장하지 않고 멈춤** — 반쪽 저장이 안전장치를 통과하던 구멍 차단
- 시험용 `--limit`이 진짜 데이터를 덮어쓰던 위험 제거
- 실패 원인을 **GitHub 주석으로 내보내게** 함 (로그는 저장소 주인만 볼 수 있어서)
- `check.yml` 신규 — EIASS에 닿는지만 확인하는 시험용(데이터 안 건드림)
- 실행 시각 09시 → **07시**로 변경

*해봤다가 되돌린 것*
- AI 모델 하이쿠 → **소넷** → **하이쿠로 복귀**. 품질은 좋았으나 값이 3배라 비용 부담 (§3 비용)
- AWS Lightsail 이전 검토 → **보류**. 틀린 전제로 시작했지만, 지금은 다시 선택지에 올라 있다 (§2)

*같은 날 저녁 추가*
- **WARP 우회 장치** — `collect.yml`이 EIASS 행 트래픽만 Cloudflare WARP 터널로 태우게 함.
  `check.yml`은 직접/WARP 두 길을 시험해 `[판정]` 주석으로 결론을 냄. **아직 시험 전** (§2·§4)

*남은 것* → **§2의 "지금 당장 할 일"(check.yml 실행)로 WARP 성패 확인** → 되면 자동화 완성,
안 되면 **수집 위치 결정**(self-hosted 러너 / AWS / 수동 유지)

**2026-08-06 (저녁)** — **화면·기능 개선.** GitHub Actions 문제는 손대지 않았다(§2 그대로).

1. **동네를 세 칸 다 고르지 않아도 되게 했다.** 시·도 칸 맨 위에 `전국`,
   아래 두 칸 맨 위에 `전체`. 위 칸을 안 고르면 아래 칸은 잠긴다.
   `ALL_SIDO`/`ANY` 표시값과 `isNation()`/`hoodSgg()`/`hoodDong()` 도우미로 다룬다
2. **`지금 내 위치로 보기`** — 첫 화면·동네 변경 모달·지도 화면 세 곳.
   GPS 좌표 → VWorld 역지오코딩 → 동네 자동 설정. 거리 기준점은 동 중심이 아니라
   **실제 서 있는 자리**. 주소를 못 찾아도 좌표는 남아 반경으로는 쓸 수 있다
3. **보는 범위를 2단 → 3단으로.** `동네 안` / `반경 N km` / `전국`.
   동네를 새로 고르면 그 모양에 맞는 기본값이 자동으로 잡힌다(`applyAutoScope`) —
   **시·도 한가운데 기준 반경 3km는 뜻이 없기 때문**
4. **읍면동·시군구 경계 표시**(오래 미뤄 둔 항목). VWorld 2D데이터 API를 JSONP로 부른다.
   지도·미니 지도에 초록 점선. **경계 안에 실제로 있는 사업만 보기**도 여기에 얹었다
5. 주소 글자만으로도 구역 판정이 되게 했다 — **경계를 못 받아도 동작한다.**
   경계는 거기에 더해 **노선이 경계를 지나는 사업**을 잡는 데 쓴다
   (실측: 고양시 덕양구는 주소로 맞는 사업 0건인데 한강 고양권역 하천 노선이 지난다)

*확인한 것* — 지역 필터 실측(경기도 6건 / 창원 의창구 1건 / 고양 덕양구는 노선으로 1건),
GPS 흐름(좌표 흉내 → 동네 자동 설정 → 경계까지), 휴대폰 375px 가로 넘침 0, 콘솔 오류 0.
**단, 사람 눈으로는 아직 아무도 안 봤다 — §2 "사람 눈으로 확인 필요"가 다음 할 일이다.**

*고친 곳* — `index.html`, `assets/app.js`, `assets/app.css` 세 파일. 수집기(파이썬)는 안 건드렸다.

*배포본을 휴대폰으로 보고 고친 것 (같은 날, 2차)*
- **가로로 든 휴대폰에서 지도 화면 사업 목록 높이가 0px 이었다** (812×375 실측).
  미디어쿼리를 폭 기준으로만 만들어서, 폭 넓고 높이 낮은 화면이 "데스크톱"으로 판정됐다.
  → `@media (max-height:560px) and (max-width:1024px)` 추가. 목록 0 → **212px**
- **세로 휴대폰에서도 목록이 37px** 이었다 (375×812). 위쪽 띠 2줄 + 주소칸의 `내 위치` 때문.
  → 띠를 한 줄로 되돌리고(돌아가기 글자 일부 감춤) 지도 62dvh → 52dvh. 37 → **210px**
- **`100vh` → `100dvh`.** 휴대폰 브라우저는 주소창 높이까지 100vh 에 넣어 화면 아래가 잘린다
- **첫 화면 기본을 `전국`으로.** 처음 온 사람은 동네를 아직 안 정했다
- **전국을 보는데 미니 지도가 하남시를 찍던 문제.** 기준 좌표 기본값을 우리 집인 양 쓴 탓.
  → 전국일 때는 거리·우리 집 마커·반경 원을 **전부 뺀다** (`recomputeDistances` 한 곳에서 차단)
- **반경을 주민이 직접 고르게** 했다 (내 동네 카드의 `반경` 칸, 1~20km). **기본 5km**
- **읍면동 중심점이 한쪽에 치우쳐 있었다.** 지오코더는 도형 중심이 아니라 *대표 지번*을 준다.
  → 경계에서 무게중심을 직접 구해 옮긴다. 실측 이동 거리 **경서동 2.5km / 미사동 2.4km / 사림동 0.7km**

*배포본을 눈으로 보고 바로 고친 것 (같은 날)*
- **지도가 우리 동네를 구석으로 밀어냈다.** 인천 경서동을 보는데 노선으로 걸린 사업의
  대표 주소(파주)까지 담느라 축척이 zoom 10 으로 벌어져 경계가 52×25px 짜리로 찌그러졌다.
  → `fitGisView()` — `동네 안`을 보는 중에 경계가 있으면 **경계에 맞춘다**(zoom 12, 206×101px).
- **꼬리표와 제목이 어긋났다** — "우리 동네 사업" 위에 "전국에서 계획 중인 사업입니다".
  → 꼬리표(`#projKicker`)도 범위에 따라 바뀐다.
- 휴대폰 범례가 항목 6개로 늘어 지도를 많이 가려서 글자·여백을 줄였다.
  더 줄이려면 **접었다 펴는 방식**으로 바꿔야 하는데, 새 조작이 생기는 일이라 먼저 물어볼 것.

---

## 8. 진행 기록은 Notion에도 있다

팀·멘토 공유용 진행 기록은 Notion "1차 작업 기록 · 첫 화면 디자인 컨셉 선정" 페이지
맨 아래 "진행 기록" 섹션에 날짜별로 쌓고 있다. 이 문서(HANDOFF.md)는 **다음 세션이 이어받기
위한 기술 인수인계**이고, Notion은 **왜 그렇게 결정했는지의 팀 공유 기록**이라 목적이 다르다.
큰 작업을 끝내면 두 곳 다 갱신할 것.
