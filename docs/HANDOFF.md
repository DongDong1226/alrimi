# 인수인계 — 우리동네 개발사업 알리미

> **"이어서 작업하자"로 시작한 새 세션은 이 문서를 먼저 읽고 §2 로 간다.**
>
> | 무엇을 알고 싶나 | 어디로 |
> |---|---|
> | **지금 어디까지 왔고 다음에 뭘 하나** | **이 문서** — §2 로 간다 |
> | 바뀌지 않는 규칙·구조 | `CLAUDE.md` (세션마다 자동으로 들어온다) |
> | **왜 그렇게 결정했나 / 예전에 뭘 해봤나** | **`docs/HISTORY.md`** — 필요할 때만 열어 본다 |
> | 처음 배포 설정을 하는 사람 | `docs/SETUP.md` |
>
> **`HISTORY.md` 를 매번 읽지 않아도 된다.** 이미 내린 결정을 뒤집으려 하거나
> "이거 예전에 해봤나?" 싶을 때만 열면 된다.

- 마지막 갱신: **2026-08-15 (수집 자동화 완료 — EIASS·VWorld 둘 다 서울 중계)**
- 저장소: `DongDong1226/alrimi` (**공개**)
- 배포: **https://dongdong1226.github.io/alrimi/** ← 지금 살아 있다 (41건, 자동 갱신 중)
- 목표: 9월 데모에서 실제로 돌아가는 화면

> ### 🔴 새 세션이 가장 먼저 알아야 할 것
>
> **1. 급한 불은 없다. 코드로 막힌 곳은 다 뚫렸다.**
> 사이트는 41건(좌표 39건)으로 잘 돌아가고, **수집·배포가 매일 자동으로 돈다.**
> 사람이 매일 눌러야 하던 `run_daily.bat` 은 이제 안 눌러도 된다.
>
> **2. ✅ 수집 자동화가 완성됐다 (2026-08-15 확정). 이게 이 프로젝트의 가장 큰 숙제였다.**
> 오래 막혀 있던 원인은 **미국(GitHub) 에서 한국 서버가 응답하지 않는 것**이었고,
> **EIASS 와 VWorld 둘 다** 같은 문제였다. **서울에 중계(Supabase Edge Function)를 두어** 풀었다.
> - **자동 수집 3회 연속 성공** (08-14 14:41 · 08-15 07:11 · 08-15 13:38) — 미리 정해 둔 합격 기준
> - 08-15 23:33 에 손으로 한 번 더 돌려(`Run workflow`) VWorld 고침까지 확인했다 — 역시 성공
> - 좌표 **30/41 → 39/41**. 못 찾은 2건은 VWorld 에 그 주소가 없다(한국에서 불러도 같다)
> - 실행 주석에 `[통로] 서울 중계로 EIASS 연결 성공` 이 찍힌다
> - 자세한 것은 §2 "최근에 끝낸 것", 경위는 `docs/HISTORY.md` §4-0
>
> **3. ★ Supabase 커넥터가 붙어 있다 — AI 가 중계 함수를 직접 다룰 수 있다 (2026-08-15).**
> 예전에는 대시보드에 손으로 붙여넣어야 했다. 이제 새 세션에서 바로 된다:
> - 함수 배포 (`tools/relay/index.ts` 를 그대로 올림)
> - **함수 로그 확인** — 중계가 실패하면 왜인지 볼 수 있다
> - 배포본과 저장소가 어긋났는지 대조
>
> **4. 지금 할 일은 '눈으로 보는 것'이다 (§2 1순위).**
> 잴 수 있는 것은 2026-08-15 에 다 쟀다(아래 표). 남은 건 **"보기 좋은가 / 헷갈리지 않는가"** 뿐이고,
> 이건 도구를 바꿔도 기계가 못 한다.
> **★ 캡처를 하려면 Claude Code 의 브라우저 창(Browser pane)을 띄워야 한다.**
> 안 띄우면 화면이 그려지지 않아 캡처가 실패한다(`the Browser pane is not displayed`).
>
> **5. 화면 파일이 두 벌이다 — 갈라지면 이 구조가 실패한다.**
> `index.html`(데스크톱) / `m.html`(휴대폰). 짧은 변 ≤600px + 손가락 기기면 자동으로 `m.html`.
> **두뇌(`app.js`)와 안내문(`texts.js`)은 하나로 공유한다.** 규칙은 `CLAUDE.md` "화면 파일이 두 개다".
>
> **6. '내가 담은 사업'(화면 5)은 데스크톱에만 있다.**
> 카드의 별표로 담는다. `m.html` 에는 그 화면이 없어 별표를 `mobile.css` 에서 감춰 뒀다.
> 기한이 지난 사업은 지우지 않고 흐리게 남기되, **환경영향분석은 예외 없이 공람 기간 규칙을 따른다.**
>
> ---
> **작업 시작 전 습관 두 가지**
> - `git status` 먼저. 커밋·푸시는 **사람이 GitHub Desktop에서** 한다 (AI가 하지 않는다).
> - **`assets/config.js` 에 VWorld 키를 적지 말 것.** 8/7에 그렇게 하다 공개 저장소에
>   키가 올라가 교체해야 했다. 로컬에서 지도를 봐야 하면 **관리자 화면**에 넣는다.

---

## 0. 한 문단 요약 — 지금 어디까지 왔나

**만들려던 기능은 다 만들었고, 수집·배포가 자동으로 돈다.** 전국 공람 관련 사업 **41건**을 수집해
GitHub Pages에 올렸고, 지도·지오코딩·AI 해석·설명회 안내·의견 제출 안내가 전부 실제
데이터로 동작한다. 가장 중요한 규칙은 **"공람 종료일"이 아니라 "의견제출 마감일" 기준으로
사업을 보여주는 것**이다 (단, AI 해석만은 법적으로 공람 기간에만 보여준다 — 절대 헷갈리면
안 되는 부분).

**8/6~8/8 사흘간 화면을 크게 손봤다.** 굵직한 것만:

- **휴대폰 화면을 따로 분리했다** — `m.html` + `mobile.css` + `mobile.js`.
  원래 "따로 만들지 않는다"가 결정이었는데, 폭 기준으로 데스크톱을 줄여 쓰는 방식이
  계속 깨져서(가로 화면 목록 0px, 세로 37px) **뒤집었다.**
  단 판단 로직(`app.js`)과 법 조문 안내문(`texts.js`)은 **하나로 공유**한다.
- **동네를 세 칸 다 안 골라도 되게** 했고(`전국`·`전체` 추가), **`지금 내 위치로 보기`**(GPS)를 넣었다.
- **읍면동·시군구·시도 경계**를 지도에 그린다. 시도만 원본이 3MB라
  `tools/build_boundaries.py`로 미리 단순화해 `data/sido.json`(367KB)에 넣어 뒀다.
- **보는 범위 3단**(동네 안 / 반경 N km / 전국). 반경은 주민이 직접 고른다(기본 5km).
- 그 밖에 다크 모드 색 반전 차단, 지도 축척 기준 정리, 배경 사진 교체 등. 자세한 건 §7.

**★ 수집 자동화가 풀렸다 (2026-08-15 확정).** 오래 막혀 있던 문제였다.
"EIASS가 해외 IP를 막는다"는 **사실이 아니었고**(전 세계 17곳 정상), 그렇다고
GitHub 러너에서 되지도 않았다(6번 중 1번). WARP 우회도 3번 연속 실패해 탈락했다.

**답은 EIASS로 가는 요청만 서울을 거치게 하는 것이었다** — Supabase Edge Function(서울 리전).
그 과정에서 **원인이 두 가지**였음이 드러났다: ① 나가는 IP ② **EIASS가 중간 인증서를
안 보내는 것**(브라우저·파이썬은 알아서 보충해 줘서 여태 아무도 몰랐다).
자동 수집이 **3회 연속 성공**해 미리 정해 둔 합격 기준을 채웠다.

**그 과정에서 지오코딩도 같은 문제였음이 드러나 같이 풀었다.** VWorld 역시 GitHub 에서
아예 답을 하지 않아 새 사업 좌표가 전부 비어 있었는데(그런데도 수집은 '성공'으로 끝났다),
VWorld 도 서울 중계를 태워 **좌표 30/41 → 39/41** 로 회복했다.

화면·배포는 전부 정상이다.

---

## 1. 새 세션에서 처음 할 일 (환경 띄우기)

```bash
cd C:\dev\EIASS
python -m http.server 8000
```

브라우저에서 `http://localhost:8000` → 동네 선택 → 홈 화면.
(`file://` 로 열면 json을 못 읽어 표본 데이터로 대체된다. 반드시 서버로.)

- 관리자 진입: 첫 화면 맨 아래 "기후에너지환경부" 클릭 → 비밀번호 `admin1234`
- **★ 내 PC 에서 지도가 안 나오면 대부분 이것이다 — `wdn.settings` 가 지워진 것.**
  `assets/config.js` 는 로컬에서 **비어 있는 게 정상**이라, 지도는 오직
  브라우저에 저장해 둔 키로만 나온다. 그 칸을 지우면(개발자도구로 만지거나, 브라우저 청소)
  **지도가 통째로 사라진다.** 배포 사이트와는 아무 상관이 없다.
  → **`route_editor.html` 맨 위 칸이나 관리자 화면에 `.env` 의 로컬용 키를 다시 넣으면 된다.**
  2026-08-16 부터 두 화면이 **같은 칸(`wdn.settings.vworldKey`)** 을 쓴다 — 한 번만 넣으면 된다.
- **내 PC에서는 지도가 안 보이는 게 정상이다.** `assets/config.js`가 비어 있어서다.
  보려면 **관리자 화면에** `.env`의 localhost용 VWorld 키를 넣는다.
  → **`config.js` 에 직접 적지 말 것** (8/7에 그렇게 하다 공개 저장소에 키가 올라갔다).
  배포 사이트는 Actions가 키를 심어 주므로 그냥 보인다.
- `.env`에 `VWORLD_KEY`, `ANTHROPIC_API_KEY` 있음 — **확인함** (VWorld 로컬 키는 8/7에 교체됨)
- 코드를 고친 뒤 로컬에서 확인할 때는 `index.html?v=아무숫자`로 열 것.
  안 그러면 브라우저가 옛 CSS/JS를 계속 쓴다(§5 함정 참고).

**휴대폰 화면(`m.html`)을 확인하는 방법**

| 무엇 | 어떻게 |
|---|---|
| PC 브라우저에서 보기 | `http://localhost:8000/m.html` 을 직접 열고, 개발자도구에서 화면 크기를 375×812 로 |
| 데스크톱 화면을 강제로 | `index.html?pc=1` (휴대폰이어도 안 보낸다. 선택을 탭 세션 동안 기억) |
| 휴대폰 화면을 아예 끄기 | `index.html` 맨 위 `var MOBILE_ON = true;` → `false` (§7의 🔌 참고) |
| 실기기(위치 기능) | **https 에서만 된다.** 배포 사이트에서 확인할 것 |

> **임시 파일 이름은 밑줄로 시작할 것** (`_m_test.html` 등). `.gitignore` 가 `_*.html` 을
> 무시한다. 8/7에 시험용 사본이 실수로 커밋됐다.

---

## 2. 다음에 할 일 ★ 여기부터 보면 된다

### ★★ 1순위 — 눈으로 확인해 줄 것 (사람만 할 수 있는 일)

**코드로 막힌 곳은 다 뚫렸다. 남은 건 "보기 좋은가 / 헷갈리지 않는가" 뿐이다.**

#### 잴 수 있는 것은 다 쟀다 (2026-08-15, 배포 사이트 실측)

| 무엇 | 결과 |
|---|---|
| 배경 사진 위 흰 글씨 대비 | **h1 자리 5.48** · 히어로 전체 최악 4.93 (기준: 큰 글씨 3 / 본문 4.5) → **통과** |
| 반경 원 규칙 | `near` 만 `true`, `region`·`all` 은 `false` → **규칙대로** |
| 시·도 경계 파일 | `data/sido.json` 367KB, HTTP 200 정상 |
| 페이지 나누기 | 카드 6개 / 쪽 단추 6개 → 동작 |
| 지도 마커 | 좌표 있는 39건이 대상, 없는 2건은 제외(정상) |

> 대비는 **사진을 캔버스에 실제 배치(`cover`)대로 그리고 덮개까지 합성한 뒤,
> 진짜 글자가 놓인 자리**(x270 y137 w726 h130)에서 잰 값이다.
> 배경 사진을 바꾸면 이 측정을 다시 해야 한다 (`CLAUDE.md` "배경 사진을 바꾸면…").

#### 그래서 사람이 봐 줘야 할 것만 남았다

**기계가 못 재는 것들이다. 도구를 바꿔도 안 된다.**

1. **휴대폰 지도 화면의 시트 흐름** — 들어가면 지도만 보이고, 아래 `목록 보기` 배지를 눌러야
   목록이 올라온다. 손잡이로 닫힘/작게/반/크게 4단계를 돈다. **이 흐름이 헷갈리지 않는지.**
2. **휴대폰 홈의 숫자 카드 3개** — 옆으로 밀어 보는 방식. 아래 점 3개가 위치를 알린다.
   **"3개가 있다"는 것이 보이는지.**
3. **배경 사진의 느낌** — 읽히는 것은 위에서 확인됐다(5.48). 남은 건 **보기 좋은가**.
4. **환경영향분석 탭** — 문장을 점으로 나눈 목록이 읽기 좋은지.
5. **주민 설명회 안내** — 회차별 일시·장소 짝짓기가 자연스러운지.
6. **지도 마커** — **핀(물방울)**. 전략=짙은 남색 / 환경=빨강. 실제 지도 위에서 구분되는지.
7. **시·도 경계 해안선** — 작은 섬을 걸러냈으니 어색하지 않은지.

#### ★ 이렇게 하면 빠르다

**Claude Code 의 브라우저 창(Browser pane)을 띄운 뒤 "화면 캡처해 줘"라고 한다.**
AI 가 데스크톱·휴대폰 크기로 각 화면을 찍어서 보내 준다.
그러면 사람은 **직접 돌아다니지 않고 사진만 보고 이상한 것을 짚으면 된다.**

> **창을 안 띄우면 캡처가 실패한다** — `the Browser pane is not displayed, so the page
> is not compositing frames`. 도구 문제가 아니라 창이 안 보여서 화면이 안 그려지는 것이다.
>
> **Playwright 를 새로 넣을 필요는 없다.** 지금 도구가 같은 일을 한다(주소 이동·크기 변경·
> JS 실행·캡처). Playwright 는 창 없이도 캡처되는 장점이 있지만, npm + 브라우저 바이너리
> 300MB 를 팀 PC 마다 설치해야 해서 이 팀 구성에는 맞지 않는다.

#### AI 가 대신 할 수 있는데 아직 안 한 것

- **이용안내 법 조문 대조** — `assets/texts.js` 의 인용을 국가법령정보센터 원문과 맞춰 본다.
  (특히 전략환경영향평가 공람기간 20~40일, 시행령 제13조)
  **창을 안 띄워도 지금 바로 할 수 있다.** 웹에서 원문을 받아 대조하면 된다.
- 휴대폰 크기(375×812)에서 넘침·잘림 재기 — 크기와 동작은 잴 수 있다(느낌은 사람 몫).

**이미 사람이 보고 확인된 것** (다시 물어볼 필요 없음): 시·도만 골라 보기 · 맨 위 `전국` ·
`지금 내 위치로 보기`(실기기 GPS 동작 확인) · 보는 범위 단추 3개 · 동네 경계 초록 점선 ·
휴대폰 화면 자동 전환 · 다크 모드 색 반전 차단 · 설명회 카드 압축 · 모달 위치 ·
담은 사업 화면(별표·마감 카드·빈 화면).

### ~~확인이 필요한 데이터 이상~~ → **2026-08-08 원인 파악 완료. 고칠 것 없음**

**"AI 해석이 비어 있는 6건"의 원인을 찾았다. 실제로는 7건이고, 원인이 두 가지로 갈린다.
둘 다 버그가 아니라 정상 동작이다.**

**원인 A — 공람이 끝나 EIASS가 첨부파일을 내렸다 (6건)**

EIASS 상세를 다시 조회해 보니 이 6건은 **첨부파일이 0개**다. 못 찾은 게 아니라 없다.
그리고 **6건 전부 공람 종료일이 2026-07-31로 같다.** 우연이 아니다 —
평가서 초안은 법적으로 공람 기간에만 열람할 수 있으므로 끝나면 파일이 내려간다.

| 사업 | 공람 종료 | 첨부 |
|---|---|---|
| 도림천 등 6개 하천기본계획 / 만수주공아파트 재건축 / 포항 호미곶 골프&리조트 / 한탄강권역(신천) 하천기본계획 / 선산공원묘원 증설 / 동부권 광역자원회수시설 증설 | 모두 2026-07-31 | 모두 0개 |

이 사업들은 7/31에 공람이 끝났지만 의견 제출 기한(+7일)이 남아 목록에 있었고,
**그때 처음 수집돼서** 이미 파일이 없었던 것이다.
→ **고칠 것이 없다.** 어차피 `viewOpen`이 false라 화면에 해석을 보여주지도 않는다.
   지금 동작이 법 취지에 정확히 맞다. **앞으로도 이런 건은 계속 생긴다 (정상).**

> 반대 증거도 확인했다 — 해석이 있는 43건 중 공람이 끝난 5건(8/3~8/6 종료)은
> **공람 중일 때 미리 받아 둔 것**이라 해석이 남아 있다. 증분 수집이 잘 돌고 있다는 뜻이다.

**원인 B — 스캔 PDF라 뽑을 글자가 없다 (1건: `25-A-00부대`)**

요약문 PDF는 있고 잘 받아진다. 그런데 **7.7MB 문서에서 글자가 1,753자밖에 안 나온다.**
뽑힌 것도 `1.1 계획의 배경 및 목적`, `<표 1-1>`, `ㆍ ㆍ ㆍ` 뿐 — **목차와 표 캡션만 있고
본문이 하나도 없다.** 본문이 전부 이미지인 스캔본이다(군부대 사업이라 그럴 수 있다).

→ **AI가 8개 항목을 전부 `null`로 둔 것은 규칙을 정확히 지킨 것이다**
   ("원문에 없으면 억지로 채우지 마라"). 목차만 보고 지어냈다면 그게 훨씬 큰 사고다.
→ **OCR은 권하지 않는다**: 외부 도구가 필요해 "라이브러리 안 늘린다" 규칙과 부딪히고,
   한글 표 인식이 부정확해 **틀린 글자를 AI에 먹이는 위험**이 생긴다. 49건 중 1건이다.
→ 대신 화면에서 **"요약문이 이미지로 되어 있어 해석하지 못했습니다"** 로 구분해 주면
   정직하다. 지금은 "요약문에 관련 내용이 없습니다"로 나오는데,
   **내용이 없는 게 아니라 읽지 못한 것**이라 살짝 오해의 소지가 있다. (아직 안 함)

### ✅ 최근에 끝낸 것 ⓪-4 — 도로·철도 노선 그리기 도구 (2026-08-16)

**`route_editor.html`** (저장소 맨 위) — 담당자용 별도 도구.
주민 화면의 판단 코드(`app.js`)는 한 줄도 안 건드렸다.

| 어디서 | 어떻게 |
|---|---|
| **배포 사이트** | 관리자 화면(비밀번호) → **노선 그리기 도구 열기**. VWorld 키가 이미 심어져 있어 그냥 열린다 |
| 내 PC | `python -m http.server 8000` → `http://localhost:8000/route_editor.html` (키는 맨 위 칸에) |

- **★ 파일은 반드시 맨 위 폴더에 둔다.** `deploy.yml` 이 맨 위 `*.html` 만 올린다.
  `tools/` 로 옮기면 배포에서 빠져 관리자 화면 단추가 404 가 된다
- **★ 사업마다 파일을 내려받지 않아도 된다 (2026-08-16 고침).**
  처음엔 그린 것이 메모리에만 있어서 사업 하나마다 내려받게 됐고 `routes (1).json`,
  `routes (2).json` 이 쌓였다. 두 가지로 고쳤다:
  - 그린 노선을 **localStorage 에 즉시 쌓는다** — 새로 고쳐도 안 없어진다.
    칸 이름은 `wdn.routes` 로 **app.js 와 같다** → 같은 브라우저의 알리미 화면에서 바로 미리보기가 된다
  - **`data/routes.json` 을 직접 덮어쓴다** (File System Access API). 처음 한 번 파일을 고르면
    그 뒤로는 누를 때마다 조용히 덮어쓴다. 크롬·엣지 전용이라 [내려받기] 를 예비로 남겼다
  - 화면에 **`노선 3건 · 그중 2건이 아직 파일에 안 들어갔습니다`** 로 상태를 보여주고,
    목록에도 `미저장` 딱지를 붙인다
- **★ 커밋은 사람이 한다.** GitHub Pages 는 정적 파일을 올려두는 곳이라 브라우저에서
  **저장소** 파일을 못 쓴다(로컬 파일은 위 방식으로 쓸 수 있다).
  GitHub 토큰을 브라우저에 두면 저장소 쓰기 권한을 공개하는 셈이라 안 된다.
- 도구 코드를 `app.js` 에 넣지 않은 이유: `app.js` 는 두 화면 공용 두뇌인데,
  도면 처리(행렬 풀이·픽셀 훑기) 400줄은 주민에게 전혀 필요 없다. 모든 방문자가 내려받게 된다
- **지금 대상 4건**: 면목선 도시철도 · 대구도시철도1호선 영천 연장 · 광양(Ⅳ) 공업용수도 ·
  기장군 농어촌도로(이건 "기장군 전역"이라 선으로 그릴 대상이 아니다)
- 화면 왼쪽에 **EIASS 사업개요(시점·종점·연장)** 를 그대로 띄워 두고 보면서 그린다
- **길이 검산이 핵심이다** — 그린 길이 vs 고시 연장. ±15% 밖이면 경고.
  실측: 면목선을 직선으로 대충 5점 찍으니 `5.70km / 고시 9.36km 대비 −39% — 많이 다릅니다`
- **가져오기도 된다** — geojson.io·Google 내 지도(KML)·QGIS 에서 그린 선을 붙여넣으면 읽는다
- 결과는 `routes.json` 으로 내려받아 `data/routes.json` 에 덮어쓰고 커밋하면 끝

**★ 도면에서 반자동으로 따는 기능을 넣었다 (같은 날 추가)**

"도면 사진을 넣으면 자동으로 노선을 따 오게 하자"는 이야기에서 나왔다.
**AI 시각 인식으로 좌표를 만들지는 않는다** — 비전 모델은 정확한 픽셀 좌표를 못 내면서
모른다고 하지 않고 그럴듯한 숫자를 낸다. 지도에서는 그 오차가 수백 m~km 라
**틀린 노선이 그려지는 것은 노선이 없는 것보다 나쁘다.**

대신 **막히는 한 단계만 사람이 메우는 방식**으로 만들었다:

| 단계 | 누가 | 비고 |
|---|---|---|
| 기준점 3~4쌍 짝짓기 (도면 ↔ 지도) | **사람** | 도면에 위도·경도가 안 적혀 있어 여기만 사람이 필요하다 |
| 선 색 픽셀 골라내기 → 가장 큰 덩어리 → 가장 긴 경로 | 자동 | AI 아님. 색 걸러내기라 결과가 항상 같다 |
| 픽셀 → 위경도 → 3.그리기 로 넘김 | 자동 | 웹 메르카토르 평면에서 계산 |

- 캡처 그림을 **Ctrl+V** 로 바로 붙여넣을 수 있다 (EIASS 첨부를 캡처해 쓰는 흐름)
- 기준점 2개=닮음 / 3개=어파인 / **4개 이상=투영**(캡처가 기울어져도 보정)
- **남는 오차(RMS)를 미터로 표시** — 기준점을 잘못 짚으면 바로 드러난다
- 범례·글자가 같은 색이면 **범위 지정(끌기)** 으로 좁힌다

**실측 (정답을 아는 시험 도면을 만들어 넣고 잼)**

| | 결과 |
|---|---|
| 따온 길이 | **8.14 km** (정답 8.117 km — 0.3% 차이) |
| 위치 오차 | **중앙값 18m · 최대 22m** |
| 범례의 빨간 조각 · 회색 도로선 · 검은 글씨 | 모두 걸러짐 |
| 파란 선 도면 | 색 안 고르면 *"거의 못 찾았습니다"* 안내, 색 고르면 **8.116km** |
| 기준점 없이 추출 | *"기준점을 먼저 2개 이상 짝지어 주세요"* 로 막힘 |

**★ 실제 도면에서 바로 문제가 나와 고쳤다 (같은 날)**

면목선 도면(네이버 지도 위에 빨간 노선)에서 **절반만 따왔다.**
원인은 **역 표시(노랑·파랑 원)가 노선 위를 덮어 선이 동강 난 것**이었다.
'가장 큰 덩어리 하나'만 쓰고 있어서 나머지 절반이 통째로 빠졌는데,
**화면에는 그럴듯하게 보여서** 눈치채기 어려웠다.

| 고친 것 | 내용 |
|---|---|
| **끊긴 틈 잇기** | 덩어리를 다 찾아 **끝점이 가까운 것끼리 이어 붙인다**. 기본 80px |
| **뭉툭한 덩어리 건너뛰기** | `106` 노선번호 딱지가 선과 같은 빨강이다. `몸통길이/칸수 >= 0.12` 인 것만 선으로 본다 |
| **이어붙이기** | 틈 잇기로도 안 되면 범위를 나눠 두 번 추출해 붙인다 |
| **다 알리기** | `끊긴 곳 1군데를 이었습니다(틈 59px) · 딱지처럼 뭉툭한 덩어리 1개는 건너뛰었습니다` |

실측 (역 표시가 선을 덮고 `106` 딱지가 있는 도면, 정답 8.093km):

| 틈 잇기 | 결과 |
|---|---|
| 0px(끔) / 40px | **4.02km — 정답의 50%** ⚠ 반쪽. 다만 *"멀리 떨어진 조각 1개는 못 붙였습니다"* 로 경고는 떴다 |
| **60px 이상 (기본 80px)** | **8.15km — 정답의 101%**, 오차 최대 44m. `106` 딱지는 건너뜀 |

틈 잇기를 끄고 **범위를 나눠 두 번 추출 → 이어붙이기**로도 4.02 → 8.15km 로 완성됐다.

> **★ 40px 기본값이 함정이었다.** 역 표시 두 개가 겹치니 틈이 **59px** 였다.
> 이 도구에서 가장 위험한 것은 실패가 아니라 **조용히 반쪽만 따오는 것**이다.
> 그래서 못 붙인 조각이 있으면 반드시 ⚠ 로 알린다.

> 남은 일: 도면에 적힌 지명을 읽어 **기준점 후보를 추천**하는 것(글자 읽기라 안전하다).
> 아직 안 넣었다.

**적용 통로는 새로 만들 것이 없었다.** `data/routes.json` → `loadRoutes()` → `manualRoutes`
→ `routeOf()` 가 이미 있고, 우선순위가 **직접 그린 것 > 수집한 하천 도형** 이다.

확인(시험용 routes.json 을 잠깐 넣고 실측, 확인 뒤 지웠다):

| | 결과 |
|---|---|
| `routeOf()` 출처 | **manual** |
| 지도에 그려진 선 | **1개 / 점 5개**, 툴팁에 사업명 |
| 반경 판정 | 주소 기준 13.3km인데 **노선 기준 10.13km** → 반경에 10.13km를 씀 |

지도·반경·상세가 **한꺼번에** 반영된다. 따로 붙일 코드가 없다.

### ✅ 최근에 끝낸 것 ⓪-3 — 새 하천 사업 2건에 노선이 안 그려졌다 (2026-08-16)

**'한강 서울권역', '상두천'** 두 건이 지도에 선이 없었다. **자료가 없는 게 아니라 못 받은 것이었다.**

| 사업 | 공람 시작 | 노선 |
|---|---|---|
| 한강 서울권역 하천기본계획 | **2026-08-14** | ❌ 0개 |
| 상두천 하천기본계획 재수립 및 실시설계 | **2026-08-13** | ❌ 0개 |
| 철마천 등 3개소 | 2026-08-05 | ✅ 3개 |
| 밀양강권역 / 한강 고양권역 / 낙동강남해권역 / 갑천 | 07-22 ~ 08-03 | ✅ 6~12개 |

**안 되는 두 건이 정확히 가장 최근 두 건이다.** 둘 다 **VWorld 가 죽어 있던 8/13~8/15에
처음 수집된 사업**이다. 한국 PC 에서 지금 물어보니 **7개·1개 도형이 그대로 나온다.**

#### 진짜 원인 — 좌표는 되받는데 노선은 안 되받았다

`refresh_cached()` 에 `if cached.get("lat") is None:` 으로 **좌표 되받기**는 있었는데
**노선에는 그런 코드가 없었다.** 증분 수집은 이미 받아 둔 사업을 통째로 건너뛰므로
**한 번 비면 영영 비어 있었다.** 8/15에 VWorld 를 고쳐 좌표는 30→39로 회복됐지만
(그 되받기 코드 덕분에) **노선은 아무도 다시 받지 않았다.**

#### 고친 것

- `refresh_cached()` — `isRiver` 이고 `riverNames` 는 있는데 `routeGeom` 이 비었으면 다시 받는다.
  **VWorld 만 부르므로 돈이 들지 않는다** (PDF·AI 는 안 건드린다). `--skip-route` 도 따른다
- `extract_river_names()` — `계획대상하천` 같은 설명하는 말을 걸러낸다.
  하천 이름 8개까지만 조회하는데 그 자리를 낭비하고 있었다.
  **접미사 거르기는 `대상하천` 만 좁게 잡았다** — `주평소하천`·`국서소하천`처럼
  `소하천`으로 끝나는 **진짜 하천 이름이 실제로 있다.** 넓게 거르면 멀쩡한 하천이 사라진다
- 지금 저장된 `data/projects.json` 에도 채워 넣었다 — **노선 있는 사업 5건 → 7건**

#### 확인 (브라우저에서 실제 그리기 함수 `drawRoutes()` 를 돌려 잼)

| | 선 | 점 | 첫 좌표 |
|---|---|---|---|
| 한강 서울권역 | **7** | 476 | 37.5685, 127.1531 (서울 동부) |
| 상두천 | **1** | 22 | 35.6357, 127.0602 (정읍 산외면) |

전체 43개 선이 모두 지도에 붙었다.

> **★ 교훈 — 되받기(backfill)가 없는 값은 조용히 영영 빈다.**
> 8/15에 배운 "실패를 조용히 삼키지 않는다"의 다음 단계다. 실패를 **로그에 남기는 것만으로는
> 부족하고**, 나중에 **다시 시도하는 자리**가 있어야 한다.
> 남은 구멍: **요약문 PDF 를 못 받아 `riverNames` 자체가 빈 사업**은 되받을 수 없다
> (PDF 를 다시 받아야 해서 `--full` 이 필요하다). 지금은 그런 사업이 없다.

### ✅ 최근에 끝낸 것 ⓪-5 — 위성지도 추가 (2026-08-16)

**기본은 일반지도, 단추 하나로 위성 전환.** 지도 화면(두 화면 모두) + 노선 그리기 도구.

- 위성은 지명이 없어 **`Satellite` + `Hybrid`(지명·도로) 두 장 세트**로 올린다.
  실측 무게: 일반 4~38KB / 위성 13~41KB + Hybrid 42~83KB → **3배 넘게 무겁다.** 그래서 기본이 아니다
- `Satellite` 만 **확장자가 `jpeg`** 다. `gray` 는 이 키로 안 되고, `midnight` 은 이 서비스엔 안 맞는다
- 고른 값은 `wdn.maplayer` 에 남고 **노선 도구와 공유**한다
- **미니 지도에는 안 넣었다** — 작아서 마커·경계가 묻힌다

**★ 색 잡는 데서 한 번 크게 헛짚었다. 이 실패를 꼭 남겨 둔다.**

타일만 바꿨더니 "마커·노선이 안 보인다"는 지적을 받았다. 그래서 **위성 배경 밝기를 실측**하고
(산·논밭·도시·물 5곳 — 중앙 0.069 · 하위5% 0.021 · 상위5% 0.196)
대비비가 높은 색으로 바꿨다. 수치는 1.10 → 9.23 으로 좋아졌는데
**화면은 더 나빠졌다.** 파스텔이라 **위성 사진의 안개·구름처럼 보였다.**

> **무엇이 잘못이었나 — 글자용 잣대를 마커에 갖다 썼다.**
> WCAG 명도 대비는 '배경과 밝기가 얼마나 다른가'만 본다. 그래서 계속 옅은 쪽으로 밀린다.
> 그런데 **마커는 글자가 아니라 지도 위에 올려놓은 물건**이다.
> 잣대가 달라야 했다 — **채도 + 테두리 + 그림자**.

**지도 서비스들(구글·네이버·카카오·애플)이 실제로 하는 방식**으로 다시 했다:
① 진한 색(파스텔 금지) ② 흰 테두리 2~3px ③ 그림자로 띄우기
④ 위성이라고 마커 색을 바꾸지 않기 ⑤ 노선은 짙은 테두리 + 채도 높은 선

| 요소 | 일반 | 위성 |
|---|---|---|
| 전략 마커 | `--p80` | **`--accent`**(선명한 파랑) — 짙은 남색만은 어두운 산 위에서 원리적으로 안 된다 |
| 환경 마커 | `--warn` | **그대로** (원래 잘 보였다) |
| 우리 집 | `--eco` | **그대로**, 흰 테두리 4px |
| 노선(고름) | `--eco` | `--live` + `--p90` 테두리 |
| 노선(안 고름) | `#3f9aae` | `--accent` + `--p90` 테두리 |
| 동네 경계 | 초록 점선 | 흰 점선 |
| 반경 원 | 파랑 | `--accent` + `--p90` 원 한 겹 더 |

- 고른 노선을 **흰색으로 하지 않는다** — 동네 경계가 이미 흰 점선이라 겹친다
- **새 색은 하나도 안 만들었다** — 전부 `tokens.css` 값이고 JS 는 `cssVar()` 로 읽어 쓴다

**★ 그래서 마커를 핀으로 바꿨다 (사람이 결정, 2026-08-16)**

원·마름모는 지도에 **칠해진 것**처럼 보였다. 핀은 뾰족한 끝이 자리를 가리키고
흰 테두리·그림자로 떠 있어 **꽂힌 물건**으로 읽힌다(지도 앱들이 다 그렇게 한다).
**일반지도·위성 둘 다 핀이다.**

| | 전 | 후 |
|---|---|---|
| 전략환경영향평가 | 짙은 남색 **원** | 짙은 남색 **핀** (`--p80` / 위성에선 `--accent`) |
| 환경영향평가 | 앰버 **마름모** | **빨강 핀** (`--live`) |
| 우리 집 | 초록 원 | **초록 원 그대로** — 지도 앱들이 '현재 위치'를 점으로 그린다. 사업(핀) ↔ 내 자리(점) |
| 미니 지도 | 작은 점 | **작은 점 그대로** (핀을 넣으면 카드만 한 지도를 덮는다) |

**★ 환경영향평가를 빨강으로 옮긴 근거 — 지도가 이미 쓰는 색을 피한다.**
VWorld 일반지도 픽셀 실측: **주황·앰버 51.8% · 노랑 27.7% · 초록 7.5% · 파랑 4.5% · 빨강 0.11%.**
앰버 마커는 고속도로·IC 표시와 같은 색이라 묻힐 수밖에 없었다.

확인:

| | 결과 |
|---|---|
| 핀 끝이 좌표를 가리키나 | **어긋남 0px** (가운데였으면 17px 위로 밀렸다) |
| 색 | 전략 `rgb(15,39,117)` / 환경 `rgb(222,52,18)` / 위성 전략 `rgb(13,149,250)` |
| 고른 핀 | 25×34 → **30×41** 로 커짐 |
| 미니 지도 | 작은 점 39개 · 핀 0개 |
| 범례 | 마커 색과 같이 맞춤 |

실측 (데스크톱·휴대폰·노선 도구 모두):

| | 결과 |
|---|---|
| 일반 → 위성 전환 | 위성 12장 + 라벨 12장, **깨진 타일 0** |
| 위성일 때 노선 | 고른 것 흰색 4.5px / 안 고른 것 `--p10` 3px / 테두리 `--p90` 8·6.5px |
| 일반으로 되돌리기 | 원래 색으로 정확히 복귀 (`#3f9aae` · `--p80` · `--eco`) |
| 휴대폰 위쪽 띠 | **52px 한 줄 유지** · 가로 스크롤 없음 |
| 기억 | `wdn.maplayer` 에 저장, 노선 도구와 공유 |

### ✅ 최근에 끝낸 것 ⓪-6 — 휴대폰에도 '내가 담은 사업' (2026-08-16)

**입구는 머리말 오른쪽 별표 단추**다. 아래 탭이 이미 5개로 꽉 찼고(애플 권장도 5개),
즐겨찾기를 머리말에 두는 것이 앱들의 흔한 방식이다.

- **`app.js` 는 한 줄도 안 고쳤다.** `m.html` 에 `#scr-saved` 를 만들면서
  칸 이름(`#savedGrid`·`#savedNote`·`#savedClosedWrap`…)을 `index.html` 과 똑같이 맞췄더니
  `renderSaved()` 가 그대로 두 화면을 함께 쓴다. **여기에 따로 코드를 만들면 두 화면이 갈라진다**
- `mobile.css` 의 `.star,[data-save]{display:none!important}` 를 지웠다
  (볼 화면이 없어서 감춰 뒀던 것이다)
- `mobile.js` 의 `syncTabs()` 에 담은 사업 화면을 더했다 —
  안 그러면 담은 사업을 보는 중에 **'홈' 탭에 불이 들어와** 지금 홈에 있는 것처럼 보인다

실측 (375×812):

| | 결과 |
|---|---|
| 카드 별표 | 3개 모두 보임 (예전에는 감춰져 있었다) |
| 담기 → 배지 | 2건 담으니 머리말 배지 **2** |
| 화면 전환 | `#scr-saved` 로 넘어감 · 카드 2개 · 카드 폭 343px · **넘침 없음** |
| 탭 막대 | 보이고, **불 켜진 탭 없음** (홈에 잘못 불 들어오지 않음) |
| 별표 해제 | 카드 2→1 · 배지 2→1 |
| 돌아가기 | 홈으로 · 홈 탭에 불 |

> 기한이 지난 사업을 아래에 따로 모으는 부분은 **지금 자료에 마감된 사업이 0건이라 못 봤다.**
> 코드는 데스크톱과 같은 것을 쓰므로(8/8 확인됨) 동작은 같다.

### ✅ 최근에 끝낸 것 ⓪-7 — '새로 올라온 사업' 알림 (2026-08-16)

**서버·계정·알림 권한이 하나도 안 든다.** 마지막으로 본 날짜(`wdn.lastSeen`)만
그 브라우저에 적어 두고, 그보다 늦게 **공람이 시작된** 사업을 새것으로 본다.

- 알림 줄(`#newBanner`) + 카드의 `NEW` 딱지. **두 화면이 같은 칸을 쓴다**
- 건수는 **지금 보고 있는 범위**를 따른다
- **첫 방문에는 아무것도 표시하지 않는다** — 41건이 전부 NEW 면 뜻이 없다
- `✕`를 눌러야 확인 처리된다. `보기`만으로는 안 지운다 (보다 말 수도 있으니까)

실측:

| | 결과 |
|---|---|
| 첫 방문 | 오늘 날짜만 저장 · 배너 없음 · 새 사업 **0건** |
| 8/10에 봤다고 치면 | **7건** (8/11~8/14 시작) · 배너 `전국에 새 사업 7건이 올라왔습니다` |
| 범위를 바꾸면 | 전국 7건 → **군포 당정동 안 1건** |
| NEW 딱지 | 1쪽(6개)이 전부 새 사업이라 6개 — 7번째는 2쪽 (정상) |
| `✕` 닫기 | 배너 사라짐 · 딱지 0 · 날짜 오늘로 갱신 |
| 휴대폰(375) | 배너 347×66px · **넘침 없음** · 딱지 3개 |

> **다음 단계는 `.ics`다** (아래 "기능 추가 후보" 참고). 그게 서버 없이
> 진짜 '알림'에 가장 가까운 길이다 — 폰 캘린더가 대신 알려 준다.

### ✅ 최근에 끝낸 것 ⓪-8 — 마감일을 캘린더에 넣기 (2026-08-16)

**서버 없이 '알림'에 가장 가까운 길.** 넣어 두면 **폰 캘린더가 대신 알려 준다.**

**★ 갤럭시까지 되게 두 가지 길을 함께 뒀다:**

| | 어디에 좋나 |
|---|---|
| `.ics` 파일 받기 | 아이폰·PC — 파일을 열면 바로 들어가고 **미리 알림도 들어간다** |
| **구글 캘린더 링크** | **갤럭시** — 파일을 받아 앱을 고를 필요 없이 일정 추가 화면이 바로 뜬다 |

- 자리: **사업 상세**(한 건) · **담은 사업 화면**(담은 것 한 번에)
- 미리 알림 **사흘 전 · 당일 아침 9시** 두 번
- 온종일 일정이라 `DTEND` 는 마감일 **+1일** (규칙)

> **★ 함정 — `.ics` 한 줄은 75바이트를 넘으면 안 된다.**
> 한글은 한 글자가 3바이트라 사업명이 대부분 넘는다. 안 접으면 까다로운 캘린더 앱이
> **파일을 통째로 못 읽는다.** `icsFold()` 가 글자 단위로 센다(한글이 잘리면 안 되므로).
> 실측: 접기 전에는 긴 줄이 여럿 → 접은 뒤 **75바이트 넘는 줄 0개**, 펼치면 제목이 온전.

실측:

| | 결과 |
|---|---|
| 41건 한 파일 | 1136줄 · 44.5KB · 이벤트 41개 · 넘치는 줄 **0** |
| 미리 알림 | 사업당 **2개**(`VALARM`) |
| 상세 화면 | 캘린더 칸 + 파일 받기 + 구글 링크(새 탭) |
| 담은 사업 | `캘린더 파일 받기 (3건)` |
| 휴대폰(375) | 343×191px · 단추 높이 40px · **넘침 없음** |

### ✅ 최근에 끝낸 것 ⓪-9 — 지역 캘린더 구독 (2026-08-16)

**서버 없이 '알림'에 가장 가까운 답.** 한 번 등록해 두면
그 지역에 **새 사업이 뜰 때마다 폰 캘린더에 저절로 들어오고 알림도 온다.**

- `build_data.py` 가 **272개** 파일을 만든다 (전국1 + 시도16 + 시군구255) → `data/cal/`
- 자리는 **관심목록 화면**. 구독이 위, 담은 사업 파일 받기가 아래
- 아이폰 `webcal://` · 갤럭시·PC 구글 캘린더 · 주소 복사, 세 가지

**★ 알림 받을 지역은 보고 있는 동네와 따로 고른다** (2026-08-16 추가)

읍·면·동까지 좁혀 들어온 사람도 **알림은 시·군·구 전체로 넓혀 받고 싶을 수 있다** —
옆 동네 사업도 우리 생활권이기 때문이다. 그래서 구독 칸에 시·도/시·군·구 고르는 칸을 뒀다.
처음 값만 지금 동네를 따라가고(당정동 → 군포시), 그 뒤로는 따로 기억한다(`wdn.calRegion`).

실측 (당정동으로 들어온 사람 기준):

| 고른 것 | 지금 받는 지역 | 파일 |
|---|---|---|
| (처음) | 경기도 군포시 | `경기도_군포시.ics` |
| 시·군·구 → 전체 | **경기도** | `경기도.ics` |
| 성남시 분당구 | 경기도 성남시 분당구 | `경기도_성남시-분당구.ics` |
| 전국 | 전국 | `all.ics` |
| 시·도만 충청북도로 | 충청북도 | 시·군·구가 **'전체'로 되돌아감** ✓ |

주소는 실제로 `200 text/calendar` 로 응답. 휴대폰 343×447px · 넘침 없음.

**홈 목록 아래에 한 줄 링크(`.cal-link`)를 뒀다.** 설정 칸을 관심목록으로 옮기면서
거기까지 안 들어가는 사람이 구독이 있는 줄 모르게 됐기 때문이다.
설정을 통째로 홈에 두면 화면이 길어지므로 **데려가기만** 한다.

| | 결과 |
|---|---|
| PC | **1217×49 한 줄** · '알림 설정 →' 이 오른쪽 끝에 붙음 |
| 휴대폰(375) | 347×97 (두 줄로 눕힘) · 넘침 없음 · 높이 44px 이상 |
| 누르면 | 관심목록 화면 · **탭 불도 관심목록으로** · 구독 설정 바로 보임 |

**세 가지 함정을 미리 막았다:**

| 함정 | 안 막으면 |
|---|---|
| 사업 0건 지역 파일을 안 만듦 | 구독한 주소가 404 → 캘린더 앱이 구독을 버린다. 그래서 **전부 만든다**(빈 파일 335B) |
| `DTSTAMP` 를 오늘로 | 내용이 같아도 **272개가 날마다 커밋**된다 → 공람 시작일로 고정 + 같으면 안 씀 |
| `collect.yml` 이 `projects.json` 만 add | **캘린더가 영영 안 올라간다** → `git add -A data/cal` 추가 (실제로 빠져 있었다) |

실측 (주소를 실제로 받아서):

| 고른 동네 | 주소 응답 | 일정 |
|---|---|---|
| 전국 | 200 `text/calendar` | 41건 |
| 경기도 군포시 | 200 | 1건 |
| 충청북도 **청주시 청원구**(띄어쓰기) | 200 | 1건 |
| 경상남도 (시·도만) | 200 | 3건 |
| 강원 고성군 (사업 0건) | **200** | 0건 — 구독이 안 깨진다 |

- 전체 272개 · **210KB** · 75바이트 넘는 줄 0개 · CRLF
- 휴대폰(375): 347×239px · 단추 3개 모두 40px · 넘침 없음

### ✅ 최근에 끝낸 것 ⓪-10 — 휴대폰 쓰임새 손보기 (2026-08-16, 실사용 지적)

폰으로 직접 써 보고 나온 네 가지를 고쳤다. **셋은 "있는 줄도 몰랐다"는 종류였다.**

**① 아래 탭 막대가 안 보였다**
흰 배경 + 옅은 회색 글자(`--ink-4`) + 1px 선이라 본문과 안 갈렸다.
**하나만 고쳐서는 안 되고 셋을 함께** 고쳐야 했다:
위쪽 **그림자**(선 1px 로는 부족) · 글자 `--ink-4`→`--ink-2`(**대비 2.6 → 9.3**) ·
고른 탭 아이콘 뒤 **알약 배경**(색 말고 모양으로도 알 수 있게).

**② 탭 구성: '안내'를 빼고 '관심목록'을 넣었다**
안내는 자주 볼 것이 아니라 머리말·푸터로 충분하다. 담아 둔 사업은 마감을 지켜보려고
담은 것이라 **자주 들어와야 하는 자리**다. 탭은 5개를 유지했다(애플 권장).

**③ 지도에서 목록·상세를 열면 돌아가기가 어려웠다**
손잡이는 '한 단계씩 도는' 물건이라 **'작게'에서 지도를 보려면 세 번**을 눌러야 했고,
상세를 열면 나갈 길이 그것뿐이었다. 손잡이 줄 오른쪽에 **`지도 보기` 단추**를 뒀다.

| 단계 | 결과 |
|---|---|
| 들어오면 | `sheet-closed` · 단추 숨김(뜻이 없으므로) |
| 목록 보기 | `sheet-peek` · 단추 보임 |
| 상세 열기 | `sheet-full + on-detail` |
| **`지도 보기` 한 번** | `sheet-closed` · **상세도 같이 닫힘** · 지도 760px 복귀 |

**④ 지도는 언제나 일반지도로 시작한다**
고른 값을 기억하지 않는다. 예전에 위성으로 저장해 둔 브라우저도 이제 일반지도로 열린다(실측).
위성은 지명 글자가 없어 첫 화면으로 맞지 않다. `route_editor.html` 은 예외로 기억한다.

**⑤ 휴대폰의 캘린더 구독 칸을 관심목록 화면으로 옮겼다**
사업 목록 아래에 두었더니 스크롤 끝까지 내려야 나와서 아무도 못 봤다.
캘린더 이야기(마감일 넣기 + 지역 구독)를 한자리에 모았다. **데스크톱은 그대로**다.

### ✅ 최근에 끝낸 것 ⓪-11 — 휴대폰 화면 군더더기 덜기 (2026-08-16, 실사용 지적)

**① 첫 화면 동네 고르기 세 칸이 화면 절반을 먹었다**
칸 하나가 66px × 3 = 210px. 시·도와 시·군·구를 **나란히** 두고 높이를 줄여 **125px** 로.
읍·면·동만 한 줄을 다 쓴다(이름이 길다 — 예: 오창읍 장대리).
동네 변경 모달은 그대로 **세 칸 한 줄**을 유지한다(모달을 짧게 두려던 원래 뜻).

**② 숫자 카드를 옆으로 밀어 보게 한 것을 없앴다**
세 칸을 밀어 보게 하고 점으로 위치를 알렸는데, **밀 수 있다는 걸 모르면 세 번째는 영영 안 보이고
첫 칸도 잘려 보였다.** 이제 **두 칸이 한 화면에 딱**(169×119 두 개) 들어간다.
`전국 협의 진행 중`은 우리 동네 이야기가 아니라 전국 통계라 뺐다 —
그 숫자는 첫 화면 아래 `nation` 줄에 그대로 있다. 스크롤·점 코드도 지웠다.

**③ 푸터를 줄였다**
운영기관·전화를 빼고(문의 모달에 있다) 저작권 한 줄 + 링크만 남겼다.
`이용안내 · 문의 · 넓은 화면(PC)` / `자료 출처 EIASS · 공공누리 제4유형`

> **★ 출처 표기를 아주 없애지는 않았다.** 공공누리 제4유형은 **출처표시가 이용 조건**이고,
> 온라인은 **출처 사이트로 가는 링크**가 함께 있어야 한다. 그래서 한 줄로 줄이되
> EIASS·공공누리 두 링크는 남겼다. 전문(변경금지 포함)은 이용안내 모달에 그대로 있다(확인함).

### ✅ 최근에 끝낸 것 ⓪-12 — 휴대폰 간격 조이기 (2026-08-16)

**① '반경 밖에 N건 더 있음'을 휴대폰에서 뺐다**
지금 보는 것 말고 다른 것까지 알려 주면 좁은 칸이 글자로 빽빽해진다.
`가장 빠른 의견 마감 D-38` 은 남긴다(쓸모 있다). **데스크톱은 그대로** —
자리가 넉넉하고, 범위를 넓힐지 판단하는 데 도움이 된다.
`app.js` 가 그 부분만 `<span class="stat-outside">` 로 감싸고 `mobile.css` 에서 숨긴다
(판단 코드를 두 벌로 만들지 않으려고 이렇게 했다).

**② 세로 간격을 전반적으로 좁혔다**

| | 전 | 후 |
|---|---|---|
| 앞 구역 끝 → 푸터 링크줄 | **36px** | **14px** |
| 푸터 높이 | 186px | **150px** |
| 구역(`.m-sec`) 위/아래 | 22/8 | 16/6 |
| 구역 사이(`--alt`) | 18px | 12px |
| 구역 제목 아래 | 14px | 10px |
| 숫자 카드 칸 | 14px | 12/6 |
| 내 동네 카드 | 6/14/10 | 4/12/8 |

문서 전체 높이 3378 → **3298px**.

### ✅ 최근에 끝낸 것 ⓪-13 — 화면을 그림으로 찍는 도구 (`tools/shot.py`, 2026-08-16)

**치수만으로는 "답답한지 넉넉한지"를 알 수 없다.** 브라우저 창이 안 떠 있으면
캡처가 막히므로(프레임을 안 만든다), 창 없이 찍는 길을 만들었다.

```bash
python tools/shot.py --click "#setForm button[type=submit]" --scroll bottom
```

- **playwright 가 있으면 그것을 쓴다** — 진짜 휴대폰 흉내(viewport meta·배율 3배·터치)
- 없으면 크롬/엣지 headless (설치 불필요). 단 **데스크톱 취급**이라 viewport meta 를 무시한다
- 가로로 삐져나가면 `⚠` 로 알려 준다

> **★ 이 차이 때문에 하마터면 없는 버그를 잡을 뻔했다.**
> 크롬 headless 로 찍었더니 첫 화면 오른쪽이 잘려 보여 "레이아웃이 넘쳤다"고 판단할 뻔했는데,
> playwright 로 다시 찍으니 멀쩡했다. **크롬 headless 가 뷰포트 meta 를 무시한 것**이었다.

**찍어 보고 바로 하나 고쳤다** — 남색 푸터가 끝난 뒤 탭 막대까지 **흰 띠**가 남아 있었다.
아래 여백을 `.m-main` 이 아니라 **푸터가** 갖게 해서 남색이 탭 막대까지 이어지게 했다.
치수만 봤으면 못 찾았을 종류다.

### 🔴 사람이 결정해야 할 것 — 공공누리 '변경금지' (2026-08-16 확인)

**EIASS 자료는 공공누리 제4유형이다.** 푸터에서 직접 확인했다
(`img_opentype04.png` = 4유형). 제4유형은 **출처표시 + 상업적 이용금지 + 변경금지**다.

| 조건 | 우리 상태 |
|---|---|
| 출처표시 | ✅ **넣었다** (푸터 2곳 · 이용안내 · `projects.json` 의 `source`) |
| 상업적 이용금지 | ✅ 지킴 (광고·유료 없음) |
| **변경금지** | ⚠️ **걸린다.** AI가 요약문을 옮기는 것은 형식상 '개작'이다 |

EIASS 저작권정책 원문에도 "무단 변경, 복제·배포, 개작 등의 이용은 금지"가 있다.

**코드로 풀 수 있는 문제가 아니다.** 다만 이 프로젝트는 **발주처가 그 자료의 운영기관
(기후에너지환경부 국립환경과학원)** 이라 물어보면 끝나는 문제이기도 하다.

- **할 일: 발주처에 "AI 요약 표시가 공공누리 조건에 맞는지" 서면으로 확인받는다.**
  9월 데모 전에 메일 한 통이면 된다. 답을 받으면 여기와 `CLAUDE.md` 에 적는다.
- 그때까지의 완화책은 이미 넣어 두었다 — 해석은 공람 기간에만 · "원문이 아니다" 명시 ·
  원문 링크 상시 제공 · 비영리 · 판단이나 평가를 덧붙이지 않음(번역만).
- 참고: 평가서 초안 자체는 **사업자가 쓴 문서**다. EIASS 도
  "기후에너지환경부가 저작권 전부를 갖고 있지 아니한 자료도 제공된다"고 밝히고 있다.

### ✅ 최근에 끝낸 것 ⓪-2 — 첫 화면 기본 동네가 '하남시 미사동'으로 남아 있었다 (2026-08-16)

기본값은 2026-08-06에 **전국**으로 바꿨는데 두 군데가 옛 값을 붙잡고 있었다.

| 어디 | 무엇 |
|---|---|
| `index.html` | 머리말 알약·큰 제목·내 동네 카드에 `경기 하남시 미사동`이 **글자로 박혀 있었다**. `m.html` 은 이미 `전국`이라 두 화면이 서로 달랐다 |
| 브라우저에 저장된 설정 | 8/6 이전에 한 번이라도 들어온 브라우저는 `wdn.settings` 에 옛 기본 동네가 남아 **새 기본값이 영영 안 먹었다** (저장된 값이 `DEFAULTS` 를 덮어쓴다) |

- `app.js` 에 **한 번만 도는 정리 코드**를 넣었다 — 저장된 기본 동네가 옛 값과 똑같으면 지우고 전국으로 되돌린다
- 관리자 화면의 기본 동네 칸을 비우면 이제 `전국/전체/전체`가 된다 (안내문도 같이 적었다)
- 실측: 옛 값을 일부러 심어 두고 새로 고치니 `전국`으로 바뀌었다

### ✅ 최근에 끝낸 것 ⓪ — 공람 끝난 사업의 AI 해석이 공개 파일에 남아 있었다 (2026-08-16)

**화면은 잠겨 있었는데 자료는 안 잠겨 있었다.** 고쳤다.

- `app.js` 의 `eiaSection()` 이 공람 기간(`viewOpen`)으로 해석을 가리는 것은 맞다.
  그런데 그건 **"화면에 안 그린다"** 는 뜻일 뿐이고,
  `https://dongdong1226.github.io/alrimi/data/projects.json` 은 **주소만 알면 누구나 내려받는다.**
- **실측: 41건 중 10건**이 공람이 끝났는데도 해석 글자를 파일에 그대로 담고 있었다.
  (의견 제출 기한이 남아 화면에는 남아 있는 사업들 — 그래서 눈에 안 띄었다)
- 고친 것
  - `build_data.py` — 저장 직전에 `view_closed()` 로 걸러 `analysis` 를 `null` 로 지운다.
    **기준은 `periodEnd`(공람 종료)다.** `opinionEnd` 가 아니다.
  - `--retry-analysis` 가 공람 끝난 사업을 다시 받지 않게 막았다 (지운 것을 되살리게 된다)
  - `collect.yml` — **한국 0시 10분** 수집을 하나 더 걸었다. 공람은 자정에 끝나는데
    다음 수집이 7시면 그 7시간 동안 파일에 남는다. 새 사업이 없으면 비용은 사실상 0원
  - 지금 저장된 `data/projects.json` 에도 같은 규칙을 한 번 적용해 두었다 (10건 → 0건)
- **남는 한계 — 지난 커밋에는 그대로 있다.** 공개 저장소라 `git log -p data/projects.json`
  으로 옛 판을 꺼내면 지운 해석이 나온다. 이건 **되돌릴 방법이 사실상 없다**
  (히스토리를 다시 써도 이미 복제된 것은 못 지운다). 앞으로 안 쌓이게 막은 것이 이번 조치다.

### ✅ 최근에 끝낸 것 ① — 지오코딩도 서울 중계로 (2026-08-15 확정)

**GitHub 에서 좌표가 하나도 안 채워지던 문제를 풀었다.** 실측으로 확인했다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 좌표 | 30 / 41 | **39 / 41** |
| 새로 채워진 사업 | — | **9건** (빠졌던 그 9건 전부) |
| 좌표를 잃은 사업 | — | **0건** |
| 지도에 찍히는 사업 | 30 | **39** |

**주민 화면에서 확인 (군포 당정동 기준)** — 배포 사이트 실측:

| 보는 범위 | 고치기 전 | 고친 뒤 |
|---|---|---|
| **반경 (읍·면·동을 고르면 기본값)** | **0건** | **2건** |
| 당정3지구 사업 | 안 보임 | **보임 · 우리 집에서 0.74km** |

자기 동네 코앞 740m 사업이 안 보이던 것이 고쳐졌다.

#### 무엇이 문제였나

GitHub 러너에서 VWorld 가 **아예 답을 하지 않았다** — 진짜 키는 `502 Bad Gateway`,
`domain` 을 붙이면 연결이 그냥 끊겼고, **틀린 키(대조군)도 똑같이 끊겼다.**
키를 거부하는 것이라면 `HTTP 200` 에 `status=ERROR / INCORRECT_KEY` 가 와야 한다 —
그런 답이 아예 없었으니 **키가 검사되는 단계까지 가지도 못한 것이다.**
한국에서는 같은 배포용 키로 Referer 없이도 잘 된다. **EIASS 와 같은 모양이었다.**

#### 고친 것

**VWorld 도 서울 중계를 태운다.** EIASS 와 완전히 같은 방식이라 새 구조는 없다.

- `tools/relay/index.ts` — `ALLOW_PREFIXES` 에 `api.vworld.kr` 추가 (Supabase 버전 7로 배포됨)
- `tools/build_data.py` — `setup_relay()` 가 VWorld 주소에도 같은 통로를 붙인다
- `geocode_address()` — **조용한 실패 제거.** `NOT_FOUND` 가 아닌 응답은 `[지오코딩 거부]` 로 남긴다
- `check.yml` — VWorld 를 **직접 / 중계 두 경로로** 시험. 직접이 실패하는 것은 이제 정상이다

**Anthropic 은 중계를 안 탄다** — 미국에서 잘 붙고, API 키가 중계를 지나가지 않는 편이 낫다.

#### 남은 2건은 고칠 수 없다

| 사업 | 주소 |
|---|---|
| 포천 5군단 군관사 조성사업 | 경기도 포천시 이동면 장암리 (성장로1289번길 71) |
| 신호남복합(500MW) 건설사업 | 전라남도 여수시 월내동 (여수산단로 1203) |

**한국 PC 에서 직접 불러도 똑같이 안 나온다** — VWorld 에 그 주소가 없다.
8/7 판도 47/49 였다. **39/41 이 자연스러운 한계다.**

> **교훈 두 가지**
> - **"연결이 되나"와 "실제로 되나"는 다른 질문이다.** 예전 점검은 `curl https://api.vworld.kr/`
>   가 응답만 하면 "OK"라고 했다. 정작 지오코딩은 하나도 안 되고 있었다.
> - **실패를 조용히 삼키면 안 된다.** 좌표가 하나도 안 채워졌는데 로그가 깨끗했고,
>   수집도 '성공'으로 끝났다. '못 찾음'과 '거부당함'은 반드시 갈라서 남긴다.

---

### ✅ 최근에 끝낸 것 ② — 수집 자동화 (2026-08-15 확정)

**자동 수집이 3회 연속 성공했다.** 미리 정해 둔 합격 기준을 채웠다.

| 한국시각 | 결과 |
|---|---|
| 08-14 10:50 | 연결 확인 (Secret 넣은 직후) ✅ |
| **08-14 14:41** | 자동 수집 ✅ → `데이터 갱신 2026-08-14` 커밋 |
| **08-15 07:11** | 자동 수집 ✅ → 커밋 |
| **08-15 13:38** | 자동 수집 ✅ → `데이터 갱신 2026-08-15` 커밋 |

- 그 전(08-14 07:30 이전) 실행이 전부 실패한 것은 **Secret 이 없어서 정상적으로 멈춘 것**이다.
- 실행 주석에 `[통로] 서울 중계로 EIASS 연결 성공 (10698 bytes) / VWorld OK` 가 찍힌다.
  이 단계는 **지역이 `ap-northeast-2` 가 아니면 실패로 치므로**, 서울에서 돈 것이 확인된 것이다.
- **`run_daily.bat` 을 매일 누르지 않아도 된다.** 급할 때 손으로 돌리는 길로만 남겨 둔다.


### 기능 추가 후보 (우선순위 순, 안 바뀜)

| 무엇 | 왜 / 메모 |
|---|---|
| **★ 공람 마감 알림 (`.ics` 다운로드)** | **다음에 할 일.** 서버 없이 가능하고, 담은 사업 화면(8/8 신규)과 바로 이어진다. 아래 참고 |
| **휴대폰(`m.html`)의 담은 사업 화면** | 8/8에 데스크톱만 만들었다. 탭 막대 자리를 정해야 한다 (지금 5개로 꽉 참). 아래 §7 참고 |
| ~~읍면동 경계 표시~~ | **2026-08-06 완료** (§7). 시·군·구 경계도 같이 된다 |
| ~~시·도 경계 표시~~ | **2026-08-08 완료** (§7). `tools/build_boundaries.py` → `data/sido.json` |
| 위치 계속 따라가기(`watchPosition`) | 지금은 누를 때 한 번만 잡는다. 걸어 다니며 보는 화면이 아니라 급하지 않다 |
| 사후환경영향조사 추가 | 지금은 범위 밖. 수집 대상을 넓히는 일 |
| 접근성 점검 (스크린리더) | 키보드·aria는 손봤지만 실제 리더로는 안 돌려봤다 |
| **워크플로 액션 판 올리기** | GitHub 이 `Node.js 20 is deprecated` 경고를 낸다. **지금은 알아서 24로 돌려주므로 문제없다.** 언젠가 지원이 끊기면 멈추니, `actions/checkout@v4→v5`·`setup-python@v5→v6` 처럼 번호만 올리면 된다 (10분) |

**`.ics` 마감 알림 — 이야기해서 정해 둔 것 (2026-08-08)**

`.ics`는 일정 하나가 적힌 1KB짜리 텍스트 파일이다. 내려받아 열면 폰·PC의 캘린더 앱이
"이 일정을 추가할까요?" 하고 묻고, 확인하면 **주민 기기에 저장된다.**
그때부터 우리 서비스와 무관해진다 — **서버도 계정도 개인정보 수집도 필요 없다.**
정부 서비스로서 이게 큰 장점이다.

| | 방식 1 · 파일 내려받기 | 방식 2 · 구독(`webcal://`) |
|---|---|---|
| 새 사업이 뜨면 | 모름 (그때 받은 것만) | 자동으로 들어옴 |
| 기간이 연장되면 | 옛 날짜 그대로 | 자동 반영 |
| 만들기 | 쉬움 (JS로 즉석 생성) | 중간 (수집 때 파일 생성) |
| 주민이 하는 일 | 버튼 → 확인 | 주소 등록 (어려움) |

- **방식 1을 먼저 한다.** 사업 상세와 담은 사업 화면에 버튼 하나.
- 방식 2도 우리 구조에 맞긴 한다 — GitHub Pages는 정적 파일을 올리는 곳이고 `.ics`도
  정적 파일이라, `run_daily.bat`이 수집할 때 같이 만들어 올리면 **서버 없이 구독형**이 된다.
- **넣을 날짜는 `의견제출 마감일`이다.** 공람 종료일이 아니다 (공람이 끝나도 7일 더 낼 수 있다).
- **미리 알림은 3일 전 + 당일 아침.** 마감 당일에만 울리면 늦다 — 의견을 쓰는 데 시간이 걸린다.
- 설명회 일정도 넣을지는 미정. `briefInfo()`가 회차별 날짜를 읽지만 **자유 형식이라 못 읽는 것이 섞여 있다.**
  넣는다면 **읽어낸 것만 넣고 못 읽은 것은 조용히 뺀다** (원문은 절대 고치지 않는다).

### 손댈 수 없다고 결론 난 것 (다시 시도하지 말 것)

> ⚠️ 여기 있던 **"GitHub Actions에서 EIASS 수집 — 해외 IP 차단"은 틀린 항목이었다.**
> 2026-08-06에 실측으로 뒤집혀 지웠다(§4). **이 목록에 무언가를 넣을 때는
> 한 번 실패한 것으로 단정하지 말고, 재현되는지 두세 번은 확인하고 넣을 것.**

- **사업 구간만 잘라낸 하천 노선** — 지금은 하천 전체가 나온다. 화면에 그 사실을 표시해 둠
- **도로·철도 선형 사업의 노선 자동 추출** — 관리자가 직접 그리는 것 외에 방법 없음
- **`<select>` 펼친 목록의 글꼴·색** — 윈도우가 그리는 것이라 CSS로 못 바꾼다

---

## 3. 지금 상태

### 동작 확인된 것 (배포 사이트 기준)

| 기능 | 상태 |
|---|---|
| EIASS 수집 (전략 + 환경영향평가) | 동작 — **41건** (공람 중 + 의견제출 유예기간 포함). 건수는 매일 바뀐다 |
| 요약문 PDF → AI 환경영향분석 (8개 고정 항목) | 동작 — 41건 중 39건. 못 채운 것은 공람이 끝나 첨부가 내려갔거나 스캔 PDF |
| VWorld 지오코딩 (주소 → 좌표) | 동작 — **41건 중 39건**. GitHub 에서는 서울 중계를 거친다. 남은 2건은 VWorld 에 주소가 없다 |
| 지도 (Leaflet + VWorld 타일), 마커·반경 원 | 동작. 마커 색·모양 8/3 변경(원본 확인 필요) |
| 하천 사업 노선 자동 표시 | 동작 — 5건 |
| 선형 사업 반경 판정 (노선까지 최단거리) | 동작 — 8/3 추가. 대표주소만으론 반경 밖으로 잘못 판정되던 문제 수정 |
| 주민 설명회 안내 (일시·장소·의견받는곳) | 동작 — 8/3 신규. 회차별 짝짓기, 지남/생략/미정 구분 |
| 전국 협의 진행 중 건수 (전략/환경/소규모) | 동작 — 8/3 신규. EIASS에서 매일 자동 수집, 비용 0원 |
| 보는 범위 3단 전환 (동네 안 / 반경 / 전국) | 동작 — 8/6 개편. 고른 동네 모양에 맞는 기본값이 자동으로 잡힌다 |
| 시·도만 골라도 보기 · 시·도 칸의 `전국` | 동작 — 8/6 신규 |
| 지금 내 위치로 보기 (GPS → 동네 자동 설정) | 동작 — 8/6 신규. https·localhost 에서만 |
| 읍면동·시군구 경계 표시 + 경계 기준 사업 가리기 | 동작 — 8/6 신규. 노선이 경계를 지나는 사업도 잡는다 |
| **시·도 경계 표시** | 동작 — 8/8 신규. `data/sido.json`(미리 단순화, 367KB). VWorld 키 없이도 나온다 |
| **휴대폰 전용 화면 (`m.html`)** | 동작 — 8/6 신규. 짧은 변 ≤600px + 손가락 기기면 자동 이동. `?pc=1` 로 탈출 |
| **내가 담은 사업 (화면 5)** | 동작 — 8/8 신규. 카드 별표로 담고 헤더 메뉴에서 본다. **데스크톱만** (휴대폰은 감춰 둠) |
| 반경을 주민이 직접 고르기 (1~20km) | 동작 — 8/6 신규. 기본 5km. 내 동네 카드의 `반경` 칸 |
| 다크 모드에서 색 뒤집힘 차단 | 동작 — 8/7 신규. `color-scheme:light` 두 줄. **지우면 안 된다** |
| 동네 변경 모달 | 동작 — 첫 화면으로 안 튄다 |
| 브라우저 뒤로가기 | 동작 — 8/3 추가. 화면 전환 기록을 남겨 이전 화면으로 복귀 |
| 의견 제출 → EIASS 안내 후 이동 | 동작 |
| 의견제출 마감 기준 필터링 + AI 해석은 공람기간만 차단 | 동작 — 8/3 규칙 변경 (§4 CLAUDE.md 참고) |
| 관리자 노선 직접 그리기 | 동작 — 8/3부터 지도 화면에서 완전히 분리(관리자 전용) |
| 휴대폰 반응형 (글자·버튼 크기, 지도/상세 전환) | 동작 — 8/3 개선 |
| 페이지 나누기 (사업 카드/설명회 목록) | 동작 — 8/3 신규 |
| **푸시 → 자동 배포** | 동작. 배포마다 파일 버전 자동 갱신(캐시 문제 해결) |
| **GitHub Actions 자동 수집** | **동작 — 8/15 확정.** 매일 07·13시. EIASS 는 서울 중계를 거친다 |
| 손으로 수집 (`run_daily.bat`, 한국 PC) | 동작 — 한 번도 실패한 적 없음. **급할 때 쓰는 길로만 남겨 둔다** |

| 수집 실패 시 안전장치 | 동작 — 8/6 신규. 재시도 4회+1회 / 목록 실패 시 저장 안 함 / 건수 급감 시 커밋 중단 |
| 실패 원인을 밖에서 읽기 | 동작 — 8/6 신규. `collect.yml`이 로그 끝부분을 GitHub 주석으로 내보낸다 |

### 데이터 (`data/projects.json` — git에 포함된다)

- 생성일 **2026-08-15**, **41건** (공람 중 + 의견제출 유예기간)
- AI 해석 39건 / 좌표 **39건** / 노선 도형 5건
- `stats.underReview`: 전략 117 · 환경 113 · 소규모 367 = **전국 597건** (수집할 때마다 갱신)
- **건수는 매일 바뀐다.** 위 숫자는 2026-08-15 수집분 기준이다
- 좌표 39/41 — 못 찾은 2건은 VWorld 에 그 주소가 없다(한국에서 불러도 같다). 정상이다

> 반경 안에 사업이 0건으로 나오는 건 **정상일 수 있다.** 우리 동네 근처에 마침
> 없을 뿐이다. 상단 "전국" 스위치를 누르면 다 보인다.

### 자동화 구조 (2026-08-15 현재 — 수집까지 자동)

```
  ■ 지금 실제로 도는 길

  매일 07시·13시(한국)
        │
        ▼
   collect.yml
        │
        ├─▶ [가는 길 확인] ── 서울 중계로 EIASS 접속 ✅
        │        (지역이 ap-northeast-2 가 아니면 여기서 멈춘다)
        │
        ├─▶ [수집] EIASS ── 서울 중계 ──▶ 목록·상세·PDF ✅
        │          VWorld ── 서울 중계 ──▶ 좌표 ✅
        │          Anthropic ─ 직접 ────▶ 해석 ✅
        │
        ├─▶ [안전장치] 건수 0 이거나 절반 아래면 커밋 안 함
        │
        └─▶ 커밋 ──▶ deploy.yml ──▶ 사이트 배포 ✅


  ■ 손으로 돌리는 길 (그대로 남아 있다. 급할 때만)

  [담당자 PC — 한국]  run_daily.bat 더블클릭
    수집(EIASS 직접) → 커밋 → 푸시 ──▶ deploy.yml → 배포 ✅
```

- **수집은 사람이 한다.** 매일 아침 `tools\run_daily.bat` 더블클릭.
- **배포는 자동이다.** 푸시만 되면 `deploy.yml`이 알아서 올린다. 이쪽은 문제없다.
- **자동 수집이 실패해도 사이트는 안 죽는다.** 어제 자료로 정상 동작한다
  (화면이 날짜를 다시 판단하므로 기한 지난 사업이 새어 나가지 않는다).
- 실패하면 GitHub이 저장소 주인에게 메일을 보낸다. 하루 두 번 온다.
  귀찮으면 `collect.yml`의 `- cron:` 두 줄을 주석 처리한다.

### 비용 (`claude-haiku-4-5`, 실측 기반)

| | 비용 |
|---|---|
| 사업 1건당 | 약 $0.02 |
| 매일 증분 (신규 1~3건 + 협의건수 조회 0원) | 월 $1~2 (3,000원 이하) |

증분 수집이 비용의 90% 이상을 줄인다. 서버 비용은 0원(사람 PC에서 돌리므로).

> **2026-08-06에 소넷(`claude-sonnet-5`)을 시험했다가 되돌렸다.**
> 품질은 좋았다 — 시험에서 8개 항목 중 원문에 없는 4개를 정확히 `null` 처리했다.
> 되돌린 이유는 값이 3배($0.02 → $0.05~0.08/건)인데, 같은 날 수집이 계속 실패하면서
> 재시도로 비용이 더 나갈 위험이 있었기 때문이다. 수집이 안정된 뒤에 다시 검토할 만하다.
> **다시 바꿀 때는 `max_tokens`를 6000 → 16000으로 반드시 같이 올려야 한다**(§5 함정 참고).

증분 수집이 비용의 90% 이상을 줄인다. 돈 문제는 사실상 없다.
**서버 비용은 0원이다** — GitHub Actions 무료 사용량 안에서 돈다(공개 저장소는 무제한).
하루 두 번 돌아도 마찬가지다. 검토하던 AWS Lightsail 월 7천원은 안 쓰게 됐다.

---

## 4. 중요한 결정과 이유 (되돌리지 말 것)

**아래 결정들은 다 이유가 있어서 그렇게 된 것이다. 뒤집기 전에 `docs/HISTORY.md` 를 읽을 것.**
근거·측정치·실패 경위가 전부 거기 있다. 여기는 **무엇을 정했는지 한 줄씩**만 적는다.

| 결정 | 한 줄 요약 |
|---|---|
| **기준은 공람 종료일이 아니라 의견제출 마감일** (8/3) | 시행령 제38조 — 공람 후 7일 이내까지 의견 가능. 공람 종료로 끊으면 아직 낼 수 있는 사업이 사라진다 |
| **★ 단, AI 해석만은 공람 기간에만** | 평가서 초안은 법적으로 공람 기간에만 열람 가능. `viewOpen`(공람 중)과 `open`(의견 가능)을 **절대 합치지 말 것** |
| **AI 는 번역만 한다** | 항목 8개 고정, 없으면 `null`. 판단·홍보 문구 금지 |
| **도면(그림)을 읽어 노선을 뽑지 않는다** | 좌표 격자가 없어 픽셀→위경도 기준점이 없다. 대신 하천 이름으로 VWorld 하천망도에서 받는다 |
| **선형 사업은 노선까지의 최단거리로 반경 판단** (8/3) | 대표 주소가 노선에서 수십 km 떨어진 경우가 흔하다 |
| ~~휴대폰 사이트를 따로 만들지 않는다~~ → **뒤집었다** (8/6) | 폭 기준으로 데스크톱을 줄여 쓰다 계속 깨졌다. 화면만 나누고 **`app.js`·`texts.js` 는 하나로 공유** |
| **★★ EIASS 접속 — "해외 IP 차단"은 사실이 아니다** (8/6) | 전 세계 17/20곳 정상. 그런데 **GitHub 러너에서만** 안 된다(6번 중 1번 성공). WARP 우회도 3번 연속 실패해 탈락 |

> ### ★ 이 문서를 쓰는 사람이 꼭 기억할 것 (EIASS 건에서 얻은 교훈)
> 1. **한 번 실패한 것을 "확인함"이라고 쓰지 말 것.** 그 결론이 돈 쓰는 결정으로 이어질 때는 특히.
>    실제로 "해외 IP 차단"이라는 **틀린 제약**이 문서 3곳에 박혀 AWS 이전 검토까지 갔다.
> 2. **몇 번 성공한 것을 "된다"고 단정하지도 말 것.** 3번 중 1번 성공을 보고 자동화를 켰다가 3번 연속 실패했다.
>
> 전말은 `docs/HISTORY.md` §4 "EIASS 접속" 에 측정치까지 그대로 남겨 두었다.

---

## 5. 알려진 함정 (다 실제로 겪은 것)

| 함정 | 증상 | 대응 |
|---|---|---|
| **EIASS 접속이 간헐적으로 안 됨** | 어떤 날은 DNS 조회 실패, 어떤 날은 TCP 무응답 (**차단 아님**, §4) | `build_data.py`의 `SESSION`이 4·8·16초 쉬며 4번 재시도. 하루 두 번 실행 |
| **"연결이 되나"와 "실제로 되나"를 헷갈림** | 점검이 `curl https://api.vworld.kr/` 에 응답만 오면 "OK"라고 답했다. **정작 지오코딩은 하나도 안 되고 있었다.** VWorld 는 키를 거부할 때도 **HTTP 200** 에 `status=ERROR` 를 담아 준다 | 점검은 **실제로 쓰는 방식 그대로** 부른다. 상태 코드가 아니라 **본문의 `status` 를 읽는다** |
| **실패를 조용히 삼킴** | 새 사업 좌표가 하나도 안 채워졌는데 **로그가 깨끗했고 수집도 '성공'으로 끝났다.** `geocode_address()` 가 '주소를 못 찾음'과 '거부당함'을 구분 없이 `(None,None)` 으로 넘겼다 | `NOT_FOUND` 가 아닌 응답은 `[지오코딩 거부] status=...` 로 남긴다. **'실패'가 아니라 '반쪽 성공'은 어떤 안전장치에도 안 걸린다** |
| **HTTP 헤더에 한글** | 중계가 오류 까닭을 한글로 헤더에 담았다가, **오류를 알리려다 그 자리에서 또 죽었다**(헤더 값은 라틴 문자만 된다) | 까닭은 **본문**에, 헤더에는 **짧은 아스키 코드**만 |
| **셸 heredoc 이 백슬래시를 먹음** | `collect.yml` 에 `tr -d '\r'` 을 넣었더니 **실제 CR 바이트**가 파일에 박혀 YAML 이 깨졌다. 줄 이음(`\`)도 사라졌다 | 셸 heredoc 으로 코드 파일을 쓰지 않는다. **편집 도구나 파이썬 파일**로 쓴다 |
| **셸에 한글을 박은 판정 스크립트** | 인코딩이 깨져 **8번 성공한 것이 8번 실패로 찍혔다.** 판정을 뒤집을 뻔했다 | 측정·판정 코드는 **파일로 빼서** 돌린다. 셸 한 줄로 판정하지 않는다 |
| **저장소와 배포본이 조용히 어긋남** | Supabase 대시보드는 **버전 관리가 없다.** `tools/relay/index.ts` 만 고치고 배포를 안 하면 옛 판이 계속 돈다(허용 목록을 늘렸는데 `403 not-allowed` 로 막힘) | 고쳤으면 **반드시 다시 배포**하고 `check.yml` 로 확인. 이제 **Supabase 커넥터로 AI 가 직접 배포·대조할 수 있다** |
| **한 번 실패를 "확인함"으로 단정** | 틀린 제약이 문서 3곳에 박혀 반년 갈 뻔했다 (§4) | 제약을 문서에 넣기 전에 **두세 번 재현**해 볼 것 |
| **목록 받다 끊기면 통째로 죽음** | 2026-08-06 수집 실행이 100초 만에 실패. 재시도 4번(≈78~100초)을 다 쓰고 예외가 그대로 터졌다 | `fetch_open_items` 를 try 로 감싸 **아무것도 저장하지 않고** 안내 후 중단. 반쪽 저장이 더 위험하다 |
| **반쪽 수집이 안전장치를 통과함** | 전략 43건만 받고 환경 16건을 놓치면 48→43건. 10% 감소라 `collect.yml` 의 "절반 이하면 중단"을 통과해 버린다 | 목록 실패 시 아예 저장하지 않는 것으로 막았다 |
| **소넷은 `max_tokens`를 생각+답변이 나눠 쓴다** | 모델만 소넷으로 바꾸고 `max_tokens=6000`을 그대로 두면 생각하다 자리를 다 써서 JSON이 잘린다 | 소넷으로 바꿀 때는 **16000으로 같이 올린다**. `b.type=="text"` 필터도 필수(생각 덩어리가 섞여 옴) |
| **Actions 로그는 저장소 주인만 본다** | 도와주는 사람이 실패 원인을 못 봐서 진단이 계속 막혔다 (`403 Must have admin rights`) | `::error::` / `::notice::` **주석**으로 내보내면 누구나 API로 읽는다. `collect.yml`·`check.yml`이 그렇게 한다 |
| **한 번 성공을 "된다"로 단정** | 3번 중 1번 성공을 보고 자동화를 켰다가 3번 연속 실패 | 접속이 안 될 땐 몇 분간 계속 안 된다. 재시도로 안 덮인다 (§4) |
| **시험용 `--limit`이 진짜 데이터를 덮어씀** | `--limit 3`으로 돌리면 48건이 6건으로 줄어 그대로 커밋됨 | `collect.yml`이 `limit` 있으면 커밋 안 함 + 건수 급감 시 중단 |
| **VWorld 키 앞뒤 공백** | 지도 타일은 나오는데 지오코더만 `INVALID_KEY` | 코드 3곳에서 `strip()`. 거부되면 길이부터 세어 본다 |
| **★ `assets/config.js` 에 키를 적어 두고 커밋** | 2026-08-07 실제 발생. 화면 확인하려고 키를 잠깐 넣었는데 그대로 커밋·푸시되어 **공개 저장소에 키가 남았다**(`514496a`). git 은 지운 뒤에도 옛 커밋에 값을 계속 가진다 | **키를 새로 발급받는 것 외에 되돌릴 방법이 없다.** 예방: 내 PC 에서 지도를 볼 땐 `config.js` 말고 **관리자 화면**에 넣는다(브라우저에만 저장됨). 커밋 전 `config.js` 가 `vworldKey: ""` 인지 항상 확인 |
| **시험용 임시 html 이 커밋에 섞임** | `_m_test.html`(화면 확인용 사본)이 `514496a` 에 같이 들어갔다 | `.gitignore` 에 `_*.html` 추가. 임시 파일은 **밑줄로 시작**하는 이름만 쓴다 |
| **`.bat` 줄바꿈이 LF** | 더블클릭해도 조용히 안 열림 | 반드시 CRLF. `.gitattributes`로 고정 |
| **배치 `ERRORLEVEL`** | 멀쩡한 실행이 "실패"로 끝남 | `echo` 뒤 말고 명령 직후 바로 확인 |
| **목록 표가 두 개 / 날짜순 아님** | 중복 수집·누락 | 사업번호로 중복 제거, 빈 페이지 5쪽까지 넘겨봄 |
| **AI 응답 잘림** | JSON 파싱 실패로 해석 통째 유실 | `max_tokens` 6000, 잘리면 `stop_reason` 로그 |
| **증분 수집이 옛 값을 붙잡음** | `미정`으로 한 번 저장되면 영영 `미정` | `needs_detail_refresh()` — 안 정해진 값 있으면 상세 재조회(PDF·AI는 건너뜀) |
| **브라우저가 옛 app.js를 붙잡음** | 배포는 됐는데 화면은 옛 코드 | 자산 주소에 `?v=커밋SHA`를 배포 때마다 자동으로 심음(`deploy.yml`) |
| **★ 커밋을 연달아 하면 배포가 죽는다** | 7분 간격으로 두 번 커밋 → 앞 배포가 **올리는 도중 잘림** → 뒤 배포가 10분 기다리다 `Timeout reached, aborting!` 으로 실패. **사이트는 옛 판 그대로**라 "고쳤는데 그대로네?"가 된다 (2026-08-06 겪음) | `deploy.yml` 의 `cancel-in-progress` 를 **false** 로. 올리는 중인 배포는 끝까지 두어야 한다 (GitHub 공식 Pages 워크플로도 false) |
| **배포 실패를 모르고 화면만 의심함** | 폰에서 안 바뀌길래 판정 규칙·브라우저를 의심했는데, 실제로는 배포가 실패해 파일이 안 올라가 있었다 | **먼저 파일이 살아 있는지 본다** — 브라우저에서 `.../alrimi/m.html` 을 직접 연다. 404면 배포 문제지 화면 문제가 아니다 |
| **격자 `1fr`이 상자보다 넓어짐** | 휴대폰에서 오른쪽이 잘려 보임(스크롤도 안 생김) | `minmax(0,1fr)` + `min-width:0` |
| **`hidden`이 안 먹힘** | 클래스 `display`가 브라우저 기본을 이김 | `[hidden]{display:none!important}` 리셋에 추가 |
| **일시·장소 순서로 짝지으면 틀림** | 기장군 사업: 일시·장소 순서가 다름 | 지역 이름으로 매칭, 안 되면 원문 통째로 표시 |
| **`file://`로 열기** | 표본 데이터로 대체됨 | 반드시 `python -m http.server 8000` |
| **EIASS 상세는 POST 전용** | 링크로 못 엶 | 숨은 폼 제출 (`openEiassSource`) |
| **윈도우 cp949 콘솔** | 한글 로그에서 죽음 | 파이썬은 `log()` 사용 |
| **시도가 16개** | 17개가 아님 | 광주+전남이 VWorld 최신 자료 기준 통합됨 |

---

## 6. 파일 지도

| 파일 | 역할 |
|---|---|
| `index.html` | **데스크톱** 화면 4개 + 모달 5개. 맨 위에서 휴대폰이면 `m.html` 로 보낸다 |
| `m.html` | **휴대폰 전용** 화면. 탭 막대 + 전체화면 지도 + 올라오는 시트 |
| `assets/app.js` | ★ 모든 판단·동작. **두 화면이 함께 쓴다. 복제 금지** |
| `assets/mobile.js` | 휴대폰 배치만 (탭 막대·시트·범례). 판단하는 코드 금지 |
| `assets/texts.js` | 이용안내·문의 본문. **두 화면이 함께 읽는다. 복제 금지** |
| `assets/app.css` / `mobile.css` / `tokens.css` | 공용 스타일 / 휴대폰 배치 / 디자인 토큰 |
| `assets/config.js` | **빈 채로 커밋.** 배포 때 VWorld 키가 채워진다 |
| `tools/build_data.py` | EIASS 수집. `--full` / `--retry-analysis` / `--skip-stats` / `--limit`.<br>`_RelayAdapter` 가 **EIASS·VWorld 만** 서울 중계로 보낸다 (Anthropic 은 직접) |
| `tools/relay/index.ts` | **서울 중계 함수의 정본.** Supabase 에 배포해 쓴다. 고치면 **반드시 다시 배포** |
| `tools/build_regions.py` | 행정구역 목록 (한 번만 돌리면 됨) |
| `tools/build_boundaries.py` | **시·도 경계** 를 미리 단순화해 `data/sido.json` 생성 (개편 때만) |
| `tools/run_daily.bat` | 수집 → 커밋 → 푸시. **반드시 CRLF**. 지금 실제로 쓰는 운영 경로 |
| `.github/workflows/deploy.yml` | 푸시하면 배포. 키 심기 + 캐시 버스팅(버전 번호 삽입) |
| `.github/workflows/collect.yml` | **매일 07시·13시(한국) 자동 수집** → 커밋 → 배포 |
| `.github/workflows/check.yml` | 직접/WARP 두 경로로 EIASS에 닿는지 시험. `[판정]` 주석에 결론. 데이터 안 건드림 |
| `data/projects.json` | 수집 결과 + `stats.underReview`. **git 포함** |
| `data/regions.json` | 전국 행정구역 |
| `data/sido.json` | 시·도 경계 도형 (미리 단순화, 367KB). **git 포함** |
| `data/routes.json` | 관리자가 그린 노선 (없어도 정상) |
| `docs/SETUP.md` | 배포 설정 설명서 (처음 한 번) |
| `docs/HISTORY.md` | **왜 그렇게 결정했나 + 날짜별 작업 기록.** 매번 읽을 필요 없다 |
| `.env` | API 키 (**git 제외**) |

---

## 7. 지금까지 한 일

**날짜별 상세 기록은 `docs/HISTORY.md` 로 옮겼다.** (여기 있던 290줄)
아래는 큰 줄기만이다. "이거 예전에 해봤나?" 싶으면 HISTORY 를 찾아볼 것.

| 날짜 | 무엇을 했나 |
|---|---|
| **08-01** | 실제 EIASS 데이터 연동, 지도·노선, 전국 수집 자동화, GitHub Pages 배포 |
| **08-02~03** | 실사용 관점 전면 개선 — 환경영향분석 가독성, 페이지 나누기, 주민 설명회 안내 개편, **기준을 의견제출 마감일로 전환** |
| **08-06 (낮)** | 수집 자동화 시도 → **실패**. 다만 **"EIASS 해외 IP 차단"이 틀렸음을 밝혀냈다** |
| **08-06 (저녁)** | 동네를 세 칸 다 안 골라도 되게, `내 위치로 보기`(GPS), 보는 범위 3단, 읍면동·시군구 경계 |
| **08-06 (밤)** | **★ 휴대폰 전용 화면 분리** (`m.html`) — 구조가 바뀐 큰 변경 |
| **08-07** | 휴대폰 화면 다듬기(5·6차), 다크 모드 색 반전 차단, **VWorld 개발용 키 유출 → 교체 완료** |
| **08-08** | 지도 축척 기준 정리, 반경 원 규칙, **시·도 경계**(`data/sido.json`), 배경 사진 교체 |
| **08-08 (밤)** | **★ 내가 담은 사업 화면(화면 5) 추가**, AI 해석 빈 7건 원인 규명 |
| **08-09** | 이 문서를 `HANDOFF.md` / `HISTORY.md` 로 갈라 냄 (840줄이라 한 번에 안 읽혔다) |
| **08-09~15** | **★ 수집 자동화 완성** — EIASS·VWorld 를 서울 중계(Supabase)로. 자동 수집 4회 연속 성공, 좌표 30/41→39/41 |
| **08-15** | 코드 점검(담은 사업 '반경 밖' 배지 버그·죽은 코드 정리), 화면 측정(대비 5.48 등), Supabase 커넥터 연결 |

---

## 8. 진행 기록은 Notion에도 있다

팀·멘토 공유용 진행 기록은 Notion "1차 작업 기록 · 첫 화면 디자인 컨셉 선정" 페이지
맨 아래 "진행 기록" 섹션에 날짜별로 쌓고 있다. 이 문서(HANDOFF.md)는 **다음 세션이 이어받기
위한 기술 인수인계**이고, Notion은 **왜 그렇게 결정했는지의 팀 공유 기록**이라 목적이 다르다.
큰 작업을 끝내면 두 곳 다 갱신할 것.
