# 인수인계 — 우리동네 개발사업 알리미

> **"이어서 작업하자"로 시작한 새 세션은 이 문서를 먼저 읽고 §2 로 간다.**
>
> | 무엇을 알고 싶나 | 어디로 |
> |---|---|
> | **지금 어디까지 왔고 다음에 뭘 하나** | **이 문서** — §2 로 간다 |
> | 바뀌지 않는 규칙·구조 | `CLAUDE.md` (세션마다 자동으로 들어온다) |
> | **왜 그렇게 결정했나 / 예전에 뭘 해봤나** | **`docs/HISTORY.md`** — 필요할 때만 열어 본다 |
> | 처음 배포 설정을 하는 사람 | `docs/SETUP.md` |
>
> **`HISTORY.md` 를 매번 읽지 않아도 된다.** 이미 내린 결정을 뒤집으려 하거나
> "이거 예전에 해봤나?" 싶을 때만 열면 된다.

- 마지막 갱신: **2026-09-04 (전체 코드 감사 56건 → 높음 13건 전부 정리 · Anthropic 키 만료 사고 발견)**
- 저장소: `DongDong1226/alrimi` (**공개**)
- 배포: **https://dongdong1226.github.io/alrimi/** ← 지금 살아 있다 (41건, 자동 갱신 중)
- 목표: 9월 데모에서 실제로 돌아가는 화면

> ### 🔴 새 세션이 가장 먼저 알아야 할 것
>
> **1. 급한 불은 없다. 코드로 막힌 곳은 다 뚫렸다.**
> 사이트는 41건(좌표 39건)으로 돌아가고 **수집·배포가 하루 세 번 자동으로 돈다.**
> `run_daily.bat` 을 매일 누를 일이 없다.
>
> **2. 만들려던 기능은 다 만들었다.** 남은 것은 **다듬기와 사람 확인**뿐이다.
> §2 의 표 두 개(1순위 / 다음에 만들 것)가 할 일 전부다.
>
> **★ 2026-09-04 에 전체 코드 감사를 했다 — 56건 중 13건을 고쳤고 높음은 다 정리했다.**
> 남은 것은 **보통 25건 · 낮음 16건**이고, **사람 결정이 필요한 것 두 가지**가 §2 의 ⑬ 절
> 맨 아래에 있다(CSP·웹폰트 / `run_daily.bat` 의 한글 주석 문제).
> **거기부터 보면 된다.** (보고서 전문은 공유 페이지로 따로 있다)
>
> **★ 그때 실제 사고 하나를 잡았다 — Anthropic 키가 8/30 에 만료돼 있었다.**
> 30일짜리로 만든 키였고, **엿새 동안 해석이 안 만들어졌는데 수집은 내내 '성공'** 이었다.
> 안전장치가 **건수만** 본다. **키를 다시 만들 때 만료일을 두지 말 것.**
>
> **3. ★ AI 가 화면을 눈으로 볼 수 있다.** `python tools/shot.py` 로 찍어서 판단한다.
> 예전처럼 "브라우저 창을 띄워 주세요"라고 부탁하지 않아도 된다. (§2 참고)
>
> **4. 지금 남은 것은 대부분 '사람이 해야 하는 것'이다** — 발주처 확인, 실기기 점검.
> 만들 것은 §2 "다음에 만들 것" 표에 우선순위대로 있다.
>
> **5. 화면 파일이 두 벌이다 — 갈라지면 이 구조가 실패한다.**
> `index.html`(데스크톱) / `m.html`(휴대폰). 짧은 변 ≤600px + 손가락 기기면 자동으로 `m.html`.
> **두뇌(`app.js`)와 안내문(`texts.js`)은 하나로 공유한다.**
> 휴대폰 화면을 만들 때 **칸 이름(id)을 `index.html` 과 똑같이 맞추면 `app.js` 를 안 고쳐도 된다** —
> 관심목록 화면이 그렇게 만들어졌다. 규칙은 `CLAUDE.md` "화면 파일이 두 개다".
>
> **6. Supabase 커넥터가 붙어 있다.** 중계 함수를 AI 가 직접 배포하고 로그를 볼 수 있다.
>
> ---
> **작업 시작 전 습관 두 가지**
> - `git status` 먼저. 커밋·푸시는 **사람이 GitHub Desktop에서** 한다 (AI가 하지 않는다).
> - **`assets/config.js` 에 VWorld 키를 적지 말 것.** 8/7에 그렇게 하다 공개 저장소에
>   키가 올라가 교체해야 했다. 로컬에서 지도를 봐야 하면 **관리자 화면**에 넣는다.

---

## 0. 한 문단 요약 — 지금 어디까지 왔나

**만들려던 기능은 다 만들었고, 수집·배포가 자동으로 돈다.** 전국 공람 관련 사업 **41건**을 수집해
GitHub Pages에 올렸고, 지도·지오코딩·AI 해석·설명회 안내·의견 제출 안내가 전부 실제
데이터로 동작한다. 가장 중요한 규칙은 **"공람 종료일"이 아니라 "의견제출 마감일" 기준으로
사업을 보여주는 것**이다 (단, AI 해석만은 법적으로 공람 기간에만 보여준다 — 절대 헷갈리면
안 되는 부분).

**8/6~8/8 사흘간 화면을 크게 손봤다.** 굵직한 것만:

- **휴대폰 화면을 따로 분리했다** — `m.html` + `mobile.css` + `mobile.js`.
  원래 "따로 만들지 않는다"가 결정이었는데, 폭 기준으로 데스크톱을 줄여 쓰는 방식이
  계속 깨져서(가로 화면 목록 0px, 세로 37px) **뒤집었다.**
  단 판단 로직(`app.js`)과 법 조문 안내문(`texts.js`)은 **하나로 공유**한다.
- **동네를 세 칸 다 안 골라도 되게** 했고(`전국`·`전체` 추가), **`지금 내 위치로 보기`**(GPS)를 넣었다.
- **읍면동·시군구·시도 경계**를 지도에 그린다. 시도만 원본이 3MB라
  `tools/build_boundaries.py`로 미리 단순화해 `data/sido.json`(367KB)에 넣어 뒀다.
- **보는 범위 3단**(동네 안 / 반경 N km / 전국). 반경은 주민이 직접 고른다(기본 5km).
- 그 밖에 다크 모드 색 반전 차단, 지도 축척 기준 정리, 배경 사진 교체 등. 자세한 건 §7.

**★ 수집 자동화가 풀렸다 (2026-08-15 확정).** 오래 막혀 있던 문제였다.
"EIASS가 해외 IP를 막는다"는 **사실이 아니었고**(전 세계 17곳 정상), 그렇다고
GitHub 러너에서 되지도 않았다(6번 중 1번). WARP 우회도 3번 연속 실패해 탈락했다.

**답은 EIASS로 가는 요청만 서울을 거치게 하는 것이었다** — Supabase Edge Function(서울 리전).
그 과정에서 **원인이 두 가지**였음이 드러났다: ① 나가는 IP ② **EIASS가 중간 인증서를
안 보내는 것**(브라우저·파이썬은 알아서 보충해 줘서 여태 아무도 몰랐다).
자동 수집이 **3회 연속 성공**해 미리 정해 둔 합격 기준을 채웠다.

**그 과정에서 지오코딩도 같은 문제였음이 드러나 같이 풀었다.** VWorld 역시 GitHub 에서
아예 답을 하지 않아 새 사업 좌표가 전부 비어 있었는데(그런데도 수집은 '성공'으로 끝났다),
VWorld 도 서울 중계를 태워 **좌표 30/41 → 39/41** 로 회복했다.

**★ 2026-08-16 에 휴대폰 쪽을 끝냈다.** 만들려던 기능은 사실상 다 만들어졌다.

- **관심목록**(담은 사업)이 휴대폰에도 생겼다. 아래 탭에서 `안내` 를 빼고 넣었다
- **알림을 서버 없이 풀었다** — 새 사업 표시(브라우저 기억) → 마감일 캘린더 넣기 →
  **지역 캘린더 구독**(5,339개 `.ics`). 구독해 두면 새 사업이 폰 캘린더에 저절로 들어온다.
  **웹 푸시는 서버가 필요해서 안 한다** (§2 참고)
- **노선 그리기 도구**(`route_editor.html`) — 손그리기 + **도면에서 반자동 추출**.
  기준점 3~4개만 사람이 짝지으면 선 따기·좌표 변환은 자동이다.
  실측: 길이 정답 대비 100.3%, 위치 오차 중앙값 18m
- **공람 끝난 사업의 AI 해석이 공개 파일에 남아 있던 것**을 막았다 (41건 중 10건이었다).
  화면만 잠그는 것으로는 부족했다 — `data/projects.json` 은 주소만 알면 누구나 읽는다
- **공공누리 제4유형**을 확인하고 출처 표기를 넣었다. **변경금지 조건은 발주처 확인 대기** (§2)

화면·배포는 전부 정상이다.

---

## 1. 새 세션에서 처음 할 일 (환경 띄우기)

```bash
cd C:\dev\EIASS
python -m http.server 8000
```

브라우저에서 `http://localhost:8000` → 동네 선택 → 홈 화면.
(`file://` 로 열면 json을 못 읽어 표본 데이터로 대체된다. 반드시 서버로.)

- **관리자 진입 (비밀번호 `ehddbs123`) — 길이 세 가지다**
  | 어디서 | 어떻게 |
  |---|---|
  | **어느 화면에서든** | 주소 뒤에 **`?admin=1`** (`.../alrimi/?admin=1`). 즐겨찾기에 넣어 두면 편하다 |
  | 첫 화면 | 맨 아래 **기후에너지환경부** 단추 |
  | 홈 화면 | 푸터의 **운영 기관 이름** 클릭 |

  > **★ 비밀번호가 안 맞을 때는 거의 항상 둘 중 하나다** (2026-08-16에 실제로 겪음)
  > 1. **한/영 키가 한글** — 영문 비밀번호를 치면 한글 자모가 들어가는데
  >    점으로만 보여서 눈치챌 수가 없다. → 칸 오른쪽 **`보기`** 를 누르면 친 글자가 보인다
  > 2. **이 브라우저에 바꾼 비밀번호가 저장돼 있다** — 저장된 설정(`wdn.settings`)이
  >    기본값을 덮어쓴다. 예전에 바꿔 두고 잊으면 그 브라우저에서는 영영 안 열린다.
  >
  > **★ 잊었을 때 되살리는 방법** — 개발자도구(F12) 콘솔에 아래를 넣는다.
  > ```
  > localStorage.removeItem("wdn.settings"); location.reload();
  > ```
  > 저장된 설정이 지워지고 기본값 `ehddbs123` 로 돌아간다.
  > **VWorld 키도 같이 지워지므로** 로컬에서 지도를 보려면 다시 넣어야 한다.
  >
  > > 화면에 **`기본값으로 되돌리기` 단추를 두었다가 뺐다** (2026-08-16).
  > > 두 번만 틀리면 누구나 눌러 초기화할 수 있어 **잠금이 아예 뜻을 잃기 때문**이다.
  > > 되살리는 길은 이렇게 문서에만 둔다.
  >
  > **★ 2026-08-16 이전에는 첫 화면 단추 하나뿐이었다.** 동네를 고르는 순간 그 화면이
  > 숨겨져 단추가 **0×0** 이 되고, 새로 고치기 전에는 어디서도 못 들어갔다 (실제로 막혔다).
  > 휴대폰 화면(`m.html`)에는 관리자 화면이 **원래 없다** — 그건 그대로 둔다.
- **★ 내 PC 에서 지도가 안 나오면 대부분 이것이다 — `wdn.settings` 가 지워진 것.**
  `assets/config.js` 는 로컬에서 **비어 있는 게 정상**이라, 지도는 오직
  브라우저에 저장해 둔 키로만 나온다. 그 칸을 지우면(개발자도구로 만지거나, 브라우저 청소)
  **지도가 통째로 사라진다.** 배포 사이트와는 아무 상관이 없다.
  → **`route_editor.html` 맨 위 칸이나 관리자 화면에 `.env` 의 로컬용 키를 다시 넣으면 된다.**
  2026-08-16 부터 두 화면이 **같은 칸(`wdn.settings.vworldKey`)** 을 쓴다 — 한 번만 넣으면 된다.
- **내 PC에서는 지도가 안 보이는 게 정상이다.** `assets/config.js`가 비어 있어서다.
  보려면 **관리자 화면에** `.env`의 localhost용 VWorld 키를 넣는다.
  → **`config.js` 에 직접 적지 말 것** (8/7에 그렇게 하다 공개 저장소에 키가 올라갔다).
  배포 사이트는 Actions가 키를 심어 주므로 그냥 보인다.
- `.env`에 `VWORLD_KEY`, `ANTHROPIC_API_KEY` 있음 — **확인함** (VWorld 로컬 키는 8/7에 교체됨)
- 코드를 고친 뒤 로컬에서 확인할 때는 `index.html?v=아무숫자`로 열 것.
  안 그러면 브라우저가 옛 CSS/JS를 계속 쓴다(§5 함정 참고).

**휴대폰 화면(`m.html`)을 확인하는 방법**

| 무엇 | 어떻게 |
|---|---|
| **AI 가 찍어서 보기** | `python tools/shot.py` — 진짜 폰 크기·배율로 찍는다 (§2 참고) |
| PC 브라우저에서 보기 | `http://localhost:8000/m.html` 을 직접 열고, 개발자도구에서 화면 크기를 375×812 로 |
| 데스크톱 화면을 강제로 | `index.html?pc=1` (휴대폰이어도 안 보낸다. 선택을 탭 세션 동안 기억) |
| 휴대폰 화면을 아예 끄기 | `index.html` 맨 위 `var MOBILE_ON = true;` → `false` (§7의 🔌 참고) |
| 실기기(위치 기능) | **https 에서만 된다.** 배포 사이트에서 확인할 것 |

> **임시 파일 이름은 밑줄로 시작할 것** (`_m_test.html` 등). `.gitignore` 가 `_*.html` 을
> 무시한다. 8/7에 시험용 사본이 실수로 커밋됐다.

---

## 2. 다음에 할 일 ★ 여기부터 보면 된다

> **작업 시작 전에 `git status` 와 `git fetch` 부터.**
> 자동 수집이 GitHub 에서 하루 세 번 커밋하므로 **원격이 앞서 있는 일이 흔하다.**
> GitHub Desktop 에 "Newer commits on remote" 가 뜨면 오류가 아니다 — `Fetch` → `Pull` → 작업.
> (2026-08-17 기준 로컬은 깨끗하고 전부 푸시돼 있다)

### 🔴 1순위 — 사람만 할 수 있는 것 (코드로 못 푼다)

| # | 무엇 | 메모 |
|---|---|---|
| **1** | **발주처에 공공누리 '변경금지' 확인 메일** | AI 요약이 형식상 '개작'이다. 9월 데모 전에 한 통. 아래 🔴 절 참고 |
| **2** | **다음 배포가 초록불인지 한 번 보기** | 2026-08-16에 워크플로 액션 판을 크게 올렸다(checkout v4→v7 등). 아직 **실제 배포로 검증 안 됨** |
| **3** | **실기기에서 캘린더 구독 확인** | ⚠ **갤럭시는 폰만으로 구독이 안 된다**는 것을 알아냈다(아래 표). 확인할 것은 "PC에서 등록 → 폰에 나타나는가" 다 |
| **4** | **휴대폰 실기기로 한 바퀴** | 지도 마커 → 작은 카드 → 자세히 보기 흐름, 탭 막대, 시트 나가기 |
| **5** | `route_editor` 를 **실제 EIASS 도면**으로 써 보기 | 이번에 3가지를 고쳤다(해제·시작종점·갈래). 실제 도면에서 걸리는 것을 알려 주면 이어서 고친다 |

### ★ 다음에 만들 것 (우선순위 순)

| # | 무엇 | 메모 |
|---|---|---|
| ~~1~~ | ~~캘린더 안내 문구 고치기~~ | **✅ 2026-08-25 완료.** 아래 ⚠ 절 참고 |
| **1** | 접근성 점검 (스크린리더) | 키보드·aria 는 손봤지만 실제 리더로는 안 돌려봤다. 키보드만 한 바퀴는 AI 가 바로 할 수 있다 |
| 2 | `tools/plan_gcp.py` 를 실제 도면으로 시험 | 시험 도면에서는 동 이름만 있어 RMS 543m 였다. **역·교차로 이름**이 있는 실제 도면이면 훨씬 낫다 |
| 3 | 사후환경영향조사 추가 | 수집 대상을 넓히는 일. 지금은 범위 밖 |

---

### ✅ 캘린더 구독 안내 문구 — **고쳤다 (2026-08-25)**

예전 휴대폰 화면에는 이렇게 적혀 있었다:

> "아이폰은 `아이폰 · 캘린더 구독`, **갤럭시는 `구글 캘린더로 구독`이 가장 쉽습니다.**"

**갤럭시 부분이 틀렸다.** 찾아보고 확인한 결과:

| 캘린더 | ICS 주소 구독 |
|---|---|
| **아이폰 기본 캘린더** | ✅ 됨 (`webcal://` 누르면 바로) |
| **구글 캘린더 앱**(안드로이드·아이폰) | ❌ **안 됨** — `URL로 추가`가 **PC 웹에만** 있다 |
| **삼성 캘린더** | ❌ **안 됨** — 외부 ICS URL 자체를 지원 안 함 |
| **네이버 캘린더**(개인용) | ⚠ 확인 안 됨 (문서에 그런 기능 설명이 없다). 네이버 웍스(기업용)는 됨 |
| 구글 캘린더 **PC 웹** | ✅ 됨 |

**그래서 안드로이드 폰에서는 어떤 기본 앱으로도 구독 등록이 안 된다.** 우리 잘못이 아니라
구글·삼성이 앱에 그 기능을 안 넣은 것이다.

**다만 길이 막힌 것은 아니다** — 삼성 캘린더는 **구글 계정 캘린더를 그대로 표시**한다.
PC에서 한 번 등록해 두면 갤럭시 캘린더에 자동으로 나타나고 알림도 울린다.

**★ 2026-08-25 에 고친 것 — 전부 `app.js` 의 `renderCalSub()` 한 곳이다**

- **휴대폰 문구** — 아이폰·갤럭시를 갈라 적었다. 갤럭시는
  **"폰에서는 등록이 안 됩니다"** 라고 밝히고, `주소 복사` → **PC에서 한 번만** 등록하면
  폰 캘린더에 저절로 나타난다는 **되는 길**을 알려 준다.
- **PC 문구** — "갤럭시를 쓰신다면 여기 PC에서 등록하세요"를 넣었다.
- **PC 에 `주소 복사` 를 되살렸다.** 2026-08-16 에 뺐던 결정을 되돌린 것이다 —
  그때 전제("구글 단추가 하는 일을 손으로 하는 것")가 이 발견으로 깨졌다.
  갤럭시 주민은 **결국 PC 에서 등록해야 하고**, 네이버·아웃룩을 쓰면 구글 단추로는 길이 없다.
  **아이폰 구독(`webcal:`)만 PC 에서 계속 뺀다** — PC 에는 열 앱이 없다.
- **복사 성공 문구도 고쳤다.** 예전에는 "캘린더 앱의 'URL로 추가'에 붙여넣으세요"였는데,
  **그 메뉴는 폰 앱에 없다.** 어디에 넣는지("PC에서 구글 캘린더 → 다른 캘린더 + → URL로 추가")를 밝힌다.

> **⚠ 아직 남은 것 — 실기기 확인.** 위 표는 문서를 찾아 확인한 것이고 실제 갤럭시로는 아직 안 눌러 봤다.
> §2 의 '지금 사람이 해야 할 것' 3번이 그것이다. 확인할 것은 **"PC에서 등록 → 폰에 나타나는가"** 다.

**★ 단추 순서도 바꿨다 (2026-08-25, 사람이 정함).** 폰에서 `구글 캘린더로 구독`이
가장 큰 단추였는데 **아이폰도 갤럭시도 그것으로는 등록이 안 된다** — 가장 눈에 띄는 길이
막다른 길이었다. 그래서 폰에서만 순서를 바꿨다:

| | 가장 크게 | 그다음 | 작게 |
|---|---|---|---|
| 휴대폰 | `아이폰 · 캘린더 구독` | `주소 복사` | `구글 캘린더로 구독` |
| PC | `구글 캘린더로 구독` | `주소 복사` | — |

색·모양은 **기존 토큰(`btn--primary`/`btn--line`/`btn--ghost`)만** 썼다. 새로 만든 것은 없다.

### 웹 푸시는 지금 구조로는 안 한다 (2026-08-16 결정)

알림은 **캘린더 구독**으로 풀었다. 웹 푸시를 하려면:
- **푸시 보낼 서버**가 필요하고 (누가 구독했는지 저장해야 한다)
- 구독 주소는 기기 식별자에 가까워 **개인정보 0건이 깨진다**
- 아이폰은 **홈 화면에 추가해야만** 작동한다 (iOS 16.4+)

→ 서버 0원·개인정보 0건이 이 서비스의 강점이라 **데모 뒤 발주처와 상의**한다.

### 회원가입도 안 한다 (2026-08-16 결정)

담은 사업이 기기에만 남는 한계는 알고 있다. 그런데 계정을 만들면
개인정보처리방침·보관기간·파기·안전조치 의무가 생기고, **코딩 경험 거의 없는 3명이 감당 못 한다.**
정부 서비스에서 **개인정보 최소수집은 감점이 아니라 미덕**이다.
발표에서 "일부러 안 만들었다"고 말하는 편이 낫다.

### 🔴 사람이 결정해야 할 것 — 공공누리 '변경금지' (2026-08-16 확인)

**EIASS 자료는 공공누리 제4유형이다.** 푸터에서 직접 확인했다
(`img_opentype04.png` = 4유형). 제4유형은 **출처표시 + 상업적 이용금지 + 변경금지**다.

| 조건 | 우리 상태 |
|---|---|
| 출처표시 | ✅ **넣었다** (푸터 2곳 · 이용안내 · `projects.json` 의 `source`) |
| 상업적 이용금지 | ✅ 지킴 (광고·유료 없음) |
| **변경금지** | ⚠️ **걸린다.** AI가 요약문을 옮기는 것은 형식상 '개작'이다 |

EIASS 저작권정책 원문에도 "무단 변경, 복제·배포, 개작 등의 이용은 금지"가 있다.

**코드로 풀 수 있는 문제가 아니다.** 다만 이 프로젝트는 **발주처가 그 자료의 운영기관
(기후에너지환경부 국립환경과학원)** 이라 물어보면 끝나는 문제이기도 하다.

- **할 일: 발주처에 "AI 요약 표시가 공공누리 조건에 맞는지" 서면으로 확인받는다.**
  9월 데모 전에 메일 한 통이면 된다. 답을 받으면 여기와 `CLAUDE.md` 에 적는다.
- 그때까지의 완화책은 이미 넣어 두었다 — 해석은 공람 기간에만 · "원문이 아니다" 명시 ·
  원문 링크 상시 제공 · 비영리 · 판단이나 평가를 덧붙이지 않음(번역만).
- 참고: 평가서 초안 자체는 **사업자가 쓴 문서**다. EIASS 도
  "기후에너지환경부가 저작권 전부를 갖고 있지 아니한 자료도 제공된다"고 밝히고 있다.

### 캘린더 기능은 다 만들었다 (2026-08-16)

`.ics` 마감 알림도, 지역 구독도 끝났다. **남은 후보는 §2 "다음에 만들 것" 표에 있다.**
아래는 그때 정해 둔 원칙이라 남긴다 — 캘린더를 손볼 때 다시 읽을 것.

- **넣는 날짜는 `의견제출 마감일`이다.** 공람 종료일이 아니다 (공람이 끝나도 7일 더 낼 수 있다)
- **미리 알림은 사흘 전 + 당일 아침.** 마감 당일에만 울리면 늦다 — 의견을 쓰는 데 시간이 걸린다
- **기기마다 편한 길이 다르다** — 아이폰은 `.ics` 파일, 갤럭시는 구글 캘린더. **둘 다 준다**
- 설명회 일정은 아직 안 넣었다. `briefInfo()` 가 회차별 날짜를 읽지만 **자유 형식이라
  못 읽는 것이 섞여 있다.** 넣는다면 **읽어낸 것만 넣고 못 읽은 것은 조용히 뺀다**
- 남은 후보: 위치 계속 따라가기(`watchPosition`) — 걸어 다니며 보는 화면이 아니라 급하지 않다

### 손댈 수 없다고 결론 난 것 (다시 시도하지 말 것)

> ⚠️ 여기 있던 **"GitHub Actions에서 EIASS 수집 — 해외 IP 차단"은 틀린 항목이었다.**
> 2026-08-06에 실측으로 뒤집혀 지웠다(§4). **이 목록에 무언가를 넣을 때는
> 한 번 실패한 것으로 단정하지 말고, 재현되는지 두세 번은 확인하고 넣을 것.**

- **사업 구간만 잘라낸 하천 노선** — 지금은 하천 전체가 나온다. 화면에 그 사실을 표시해 둠
- **★ 소하천 사업의 노선 자동 조회** (2026-08-25 실측으로 확정) — VWorld 하천망도
  (`LT_C_WKMSTRM`)는 **국가하천·지방하천만** 담고 있어 **소하천은 한 줄도 없다.**
  `옹진군 소하천정비종합계획(재정비)` 는 요약문에서 하천 이름을 48개나 뽑았는데 도형은 0건이었다.
  신도·백령도·영흥도 둘레를 통째로 물어봐도 0건이고, 같은 키로 서울 한강은 9건이 나온다
  (**키·중계 문제가 아니다**). 다시 수집해도 달라지지 않는다.
  → 관리자 화면이 이런 사업을 **따로 묶어 까닭과 함께** 보여주게 고쳤다(`renderAdminRoutes()`).
  → **하천 도형을 받는 길만 막혔을 뿐, 노선을 그릴 길은 있다** — 같은 날 만든
  **'구간표의 시점·종점으로 만들기'** (바로 아래 ⑤).
- **도로·철도 선형 사업의 노선 자동 추출** — 관리자가 직접 그리는 것 외에 방법 없음
- ~~**`<select>` 펼친 목록의 글꼴·색** — 윈도우가 그리는 것이라 CSS로 못 바꾼다~~
  → **CSS 로는 여전히 못 바꾼다.** 다만 2026-08-17 에 **펼친 목록만 우리가 그리는 방식**으로 풀었다.
  닫힌 칸은 native `<select>` 그대로 두어 값 표시·키보드·스크린리더를 native 가 계속 맡는다
  (`app.js` 의 `openSelPop()`). 마우스일 때만 바꾸고 **손가락 기기는 기본 선택기를 쓴다**

---

### ⑤ 구간표의 시점·종점으로 노선 만들기 (2026-08-25 추가) — **사람 손이 남았다**

`route_editor.html` 에 넣었다. 구간표의 **시점·종점 주소를 좌표로 바꿔 곧게 잇는다.**
하천망도에 없는 **소하천 사업은 이 길밖에 없다.**

- **예전 `시점·종점 지도에 표시` 는 한 곳도 못 찾고 있었다.** 주소를 괄호까지만 잘라 넣었는데
  그건 지번주소도 도로명주소도 아니다. 실측(옹진군 46구간 92곳): **옛 0곳 / 새 92곳 전부.**
- 구간마다 **직선/고시 비율**을 보여 준다. 하천은 굽어 있으니 직선이 좀 짧은 것이 정상(60~110%).
- **점을 끌면 노선이 바로 따라 바뀐다.** 체크를 풀어 구간을 뺄 수도 있다.
- 저장하면 도형에 `note:"segment-ends"` 가 붙고, 주민 화면이
  **"대략의 위치와 방향만, 굽은 모양은 반영되지 않음"** 이라고 밝힌다.

> **★ 다음에 사람이 할 일 — `옹진군 소하천정비종합계획(재정비)` 마무리**
>
> 1. 관리자 화면 → **노선 그리기 도구 열기** (또는 `route_editor.html?id=HG20260499-105294`)
> 2. **VWorld 키를 넣는다** (도구 맨 위 칸. 배포 사이트에서는 이미 채워져 있다)
> 3. `구간 시점·종점 찾기` → 46구간이 뜬다
> 4. **주황·빨강으로 표시된 7구간만** `보기` 를 눌러 지도에서 확인하고, 틀렸으면 점을 끌어 고친다
>    (2026-08-25 기준: 3·14·16·32·**38**·40·44번. 38번은 244% 라 가장 의심스럽다)
> 5. `고른 구간을 노선으로 담기` → `이 사업에 저장` → **6번에서 `data/routes.json` 저장**
> 6. 커밋·푸시 (GitHub Desktop)
>
> 저장하지 않아도 화면은 그대로 동작한다. 노선이 없을 뿐이다.

---

### 끝난 기록은 `docs/HISTORY.md` 로 옮겼다 (2026-09-04)

아래 항목의 상세는 **`docs/HISTORY.md` 의 “2026-09-04 · HANDOFF 에서 옮겨 온 끝난 항목”**에 있다.
**여기서 다시 볼 일은 거의 없다** — 결정을 뒤집으려 할 때만 열면 된다.

- ⑥ 천미천이 자동으로 안 그려진 까닭 (2026-08-25) — `is_river_project()` 에 계획 이름 규칙 추가
- ⑦ 관리자 화면 아코디언 · 노선 판단을 자료로 (2026-08-30)
- ⑧ 구간 노선에 중간 점 (2026-08-30)
- ⑨ 46구간 노가다 줄이기 — 임시저장 · 도면 자동 따오기 (2026-08-30)
- ⑩ 도면 자동 따오기를 만들었다가 되돌린 일 (2026-08-30)
- ⑪ 갈래 있는 사업은 '구간표로 만들기'가 답이다 (2026-08-30)
- ⑫ 갈래를 목록에서 고른다 (2026-08-30)

### ⑭ 감사 후속 — 보안·성능 3건 수정 (2026-09-04)

감사 보고서의 '높음' 중 **화면 로직을 안 건드리는 것 셋**을 먼저 했다. 데모 위험이 0이다.

**① Leaflet 을 저장소 안으로** (`assets/leaflet-1.9.4.js·css` + `leaflet-images/`)
unpkg 에서 받으면서 무결성 검증이 없었다. 6곳의 주소를 바꿨고 **unpkg 참조가 0이 됐다.**
마커가 전부 `divIcon` 이라 기본 마커 png 는 원래 안 쓰지만, 3.3KB뿐이라 같이 넣었다.

**② 한글 폰트를 `dynamic-subset` 으로** — 주소 한 군데.
```
옛 방식 (통짜)        2,009 KB
새 방식 · 첫 화면       512 KB   (75% 감소)
새 방식 · 사이트 전부    777 KB   (61% 감소)
```
> ★ 감사 보고서는 "2MB 를 통째로" 라고만 적었는데, **실제 절약은 61~75%** 다.
> subset CSS 자체가 13KB 이고 한글은 조각 19~29개를 받기 때문이다. 어림이 아니라 재서 넣었다.

**③ `no-store` → `no-cache`** — `app.js` 4곳 + `route_editor.html` 2곳.
실측: 같은 파일을 다시 받을 때 **104KB 전송 → 0KB 전송**(내용은 그대로).
GitHub Pages 가 ETag 를 준다는 것을 먼저 확인하고 바꿨다.

확인: 두 화면 모두 Leaflet v1.9.4 정상·지도 생성됨·콘솔 오류 0.
바깥 도메인이 **3곳 → 2곳**(jsdelivr 폰트, unsplash 배경사진)으로 줄었다.

---

### ⑬ 전체 코드 감사 + 10건 수정 (2026-09-04)

저장소 전체를 7개 영역으로 감사했다 — **55건**(높음 13·보통 26·낮음/참고 16).
보고서는 공유 페이지로 냈다(항목마다 무엇을 어떻게 확인했는지 적어 두었다).
그중 **10건을 고치고 1건을 일부 고쳤다.** 고친 것은 전부 **브라우저에서 실제로 돌려 확인**했다.

**① 날짜를 한국 시간으로 고정** (`today_kst()`)
GitHub 러너가 UTC 라서 `date.today()` 가 한국보다 하루 늦었다.
공람 종료 사업의 해석을 지우려고 걸어 둔 **한국 0시 10분 수집이 무력화**돼 있었고,
해석이 **한국 0시~13시 약 13시간** 공개 파일에 남았다.
`build_data.py` 의 세 곳(1298·1418·1609)을 모두 바꿨다 — 시험으로 확인:
UTC 15:10(한국 00:10) 실행에서 **옛 방식은 안 지움 / 새 방식은 지움 ✓**.

**② `normalizeProject()` 에 빠져 있던 두 키** (`firstSeen` · `summaryState`)
자료에는 다 있는데 화면 객체로 안 옮겨서 **안전장치 두 개가 죽은 코드**였다.
- 스캔본 사업이 "요약문에 내용이 없습니다"라고 **사실과 다르게** 말했다
  → 이제 "사진으로 되어 있어 읽지 못했습니다. **내용이 없다는 뜻이 아닙니다**"
- '새 사업' 판정이 공람 시작일로 되돌아가 있었다
  → 실측: 발견일 기준 16건 vs 공람시작일 기준 13건, **3건을 놓치고 있었다**
  (포항융합기술산업지구는 공람 8/20 시작인데 우리가 8/31 에 처음 봤다 — 11일 차이)
- 관리자 화면의 "46건은 발견일 기록이 없습니다" **거짓 경고도 사라졌다**

> **★ 덤으로 실제 사고를 하나 잡았다 — Anthropic 키가 8/30 에 만료됐었다.**
> 7/31 에 **30일짜리**로 만든 키였다. 8/28 이 마지막 사용이고, 그 뒤 들어온 4건에
> 해석이 없었다. **수집은 계속 '성공'으로 끝나 6일 동안 아무도 몰랐다** —
> 안전장치가 **건수만** 보기 때문이다(감사 보고서 `collect.yml:177`).
> 키는 교체됐고 9/4 수집에서 해석이 다시 만들어지는 것을 확인했다.
> **키를 다시 만들 때 만료일을 두지 말 것.**

**③ 해석을 '만들지 못한' 경우를 만들었다** (`eiaSection()`)
코드에 그 경우가 **아예 없어서** 위 키 만료 엿새 동안 화면이 "요약문에 내용이 없습니다"라고
**사실과 다르게** 말했다. 지금은 꼬리표 **`만들지 못함`** + "요약문은 읽었지만 옮기는 데
실패했습니다. **내용이 없다는 뜻이 아닙니다**". 네 상태를 다 재서 확인했고,
**공람 종료 안내가 먼저 나오는 순서**까지 확인했다.

**④ 화면과 캘린더가 같은 마감일을 보게** (`normalizeProject()`)
`D-2` 라면서 **12일 지난 날짜로** 캘린더 일정을 만들어 **미리 알림이 영영 안 울렸다.**
`opinionEnd` 를 **`max(의견마감, 공람종료)` 하나**로 통일하고 EIASS 원문은 `opinionEndRaw` 로 남겼다.
네 경우를 재서 확인: 이르면 공람종료(09-08) / 늦으면 그 값(09-15) / 없으면 공람종료 / 둘 다 지나면 `open:false`.

**⑤ '지금 내 위치로 보기' 가 0건만 내던 것** (`hasHome()`)
주소를 못 찾으면 좌표가 있어도 거리를 안 재서 **반경 안 사업이 하나도 안 나왔다.**
흩어져 있던 판정을 `hasHome()` **하나로 모았다(7곳)**. GPS 상태를 만들어 확인:
거리 43/43건 · 반경 5km 안 1건(3.8km) · **반경 단추가 선택되고 잠기지 않는다**(전에는 선택된 채 잠김).
고치다 `renderScope()` 도 같은 판정을 쓰는 것을 찾아, **구역 안은 `isNation` / 반경은 `hasHome`** 으로 갈랐다.

**⑥ 담당자 작업이 망가지던 것 2건** (`route_editor.html`)
- 저장한 구간 노선을 다시 열면 **점 하나만 끌어도 노선이 두 벌**이 됐다
  (실측 3구간 → 5줄, 46구간이면 91줄). 같은 자리에 겹쳐 그려지니 **눈으로는 알 수 없었다.**
  이제 열 때 `segBranchN = segs.length` · `pts = []`. 뒤에 손으로 그린 갈래는 그대로 지켜진다(4줄 확인).
  **덤**: 이대로만 고치면 '찾기' 전에 노선이 아예 안 보여서, `redraw()` 의 건너뛰기 기준을
  **`segEnds` 가 실제로 깔렸나**로 바꿨다.
- **'노선 지우기' 가 절반만 지웠다** — `pts` 만 비워서 저장하면 갈래가 되살아났다.
  이제 전부 비우고, 되돌릴 수 없으니 **한 번 묻는다.**

**⑦ 보안·성능 3건**
지도 라이브러리를 **저장소 안으로**(`assets/leaflet-1.9.4.*`, 6곳) · 한글 웹폰트 조각별 받기
(**2,009KB → 첫 화면 512KB, 75% 감소**) · 자료를 `no-cache` 로(안 바뀌면 **304**).

**⑧ 남은 높음 3건도 고쳤다** (같은 날 이어서)
- **모달에 포커스 가둠** — `lockBehind()` 가 `<body>` 바로 아래 형제를 `inert` 로 잠근다.
  예전에는 Tab 네댓 번이면 초점이 뒤 화면으로 빠져 **모달을 닫을 수도 없었다.**
  확인: 모달 밖으로 **새는 요소 0개** · Esc 닫힘 · 누른 단추로 **제자리 복귀** · `inert` 뒷정리 0개.
  **모달이 겹쳐 열리는 것**(상세 안에 '의견 제출')도 확인 — 안쪽을 닫으면 **바깥 모달만** 다시 살아난다.
- **초점 표시** — `outline:0` 이 전역 `:focus-visible` 까지 꺼 버리던 **여섯 칸**을 되살렸다
  (첫 화면 동네 3칸 · 검색창 × 두 화면). 색은 `--primary` 를 쓴다 —
  전역 규칙의 옅은 링은 **흰 바탕에서 2.72:1** 로 기준(3:1) 미달이고 `--primary` 는 7.24:1 이다.
  링이 알약 밖으로 안 나가는 것까지 재서 확인했다(여유 16~20px).
- **`run_daily.bat` 이 캘린더를 커밋하게** — `git add -A data/cal` 을 더하고, 바뀐 게 있는지도
  `git diff --cached --quiet` 로 **담은 것 전체**를 본다. **버리는 저장소에서 네 경우를 다 돌려 확인**:
  캘린더만 바뀐 날 커밋 ✓ / 안 바뀐 날 안 올림 ✓ / 수집 실패한 날 안 올림 ✓ / 지역이 없어진 날 삭제 반영 ✓.
  (옛 판정은 캘린더만 바뀐 날 **종료코드 0 = '올릴 것 없음'** 으로 끝났다 — 그것이 이 버그였다)

**⑨ 드롭다운 펼친 목록이 스크롤되지 않던 것** (같은 날, 사용자 제보)
`addEventListener("scroll", closeSelPop, true)` 가 **`capture` 라서 목록 자신의 스크롤까지**
잡아 버려, 굴리는 순간 목록이 사라졌다. 시·도 칸은 17개 항목에 **333px 이 넘치는데**
**아래쪽 항목(세종~충청북도)을 아예 고를 수 없었다.**
`selPop.el.contains(e.target)` 이면 닫지 않게 했다. 다섯 경우를 재서 확인 —
목록 안 휠 ✓ / 끝까지(충청북도) ✓ / 굴린 뒤 골라짐 ✓ /
**뒤 화면 스크롤에는 여전히 닫힘**(원래 의도) ✓ / 모달 안 드롭다운 ✓.
> 같은 함정을 `mousedown` 에서는 이미 막아 두었는데 `scroll` 에만 빠져 있었다.
> **`capture` 로 문서 전체를 들을 때는 '내 것'과 '남의 것'을 반드시 가른다.**

**⑩ 화면 이름을 `우리동네 개발 알리미` 로 바꿨다** (사용자 결정)
로고 5곳 + `<title>` 2곳. 굵게=`우리동네 개발` / 옅게=`알리미`.
정식 명칭(`우리동네 개발사업 알리미`)과 캘린더 파일 안 글자는 그대로 둔다 —
예전에는 화면만 `우리동네 알리미` 여서 **캘린더 파일과 어긋나 있었다.**
네 가지 창에서 머리말이 안 깨지는 것을 재서 확인했다(320px 폰에서도 여백 24px).

**⑪ 그 김에 찾은 것 — 휴대폰 첫 화면 로고가 안 보이고 있었다**
`mobile.css` 가 `.onb .brand-logo` 의 **배치만 베끼고 색(`#fff`)을 빼먹어서**,
**남색 히어로 위에 남색 글자**가 얹혀 있었다. 실측: `알리미` 대비 **1.07** ·
`우리동네 개발` **1.74** → 흰색으로 **12.60**. 이름을 바꾸기 전에도 있던 문제다.

**⑫ 화면을 세 군데 손봤다** (사용자 요청)
- **주민 설명회 안내를 따로 있는 화면으로** (`#scr-brief`). 예전에는 홈 안의 섹션이라
  메뉴를 누르면 홈 아래로 **스크롤만** 했다. 칸 이름은 그대로 두어 `app.js` 는 안 바뀌었고,
  배치는 담은 사업 화면의 `.saved-body` 를 그대로 썼다(새 CSS 0줄).
  휴대폰은 아래 탭의 **설명회**가 여기로 온다.
- **`내가 담은 사업` → `관심사업 / 알림`** — 이름에 '알림'을 넣어 **안에 구독 설정이 있다는 것**을
  이름으로 알린다. 휴대폰 탭은 자리가 좁아 `관심·알림`.
- **PC 구독 단추를 하나로** — `구글 캘린더로 구독` 만 남기고 `주소 복사` 는 설명글 속
  작은 링크로 내렸다. **없애지는 않았다** — 구글 계정이 없거나 네이버·아웃룩을 쓰는 주민은
  PC 가 유일한 길이기 때문이다(2026-08-25 절 참고). 복사 결과는 **따로 있는 줄**에 쓴다 —
  설명글에 덮어쓰면 링크가 사라져 다시 복사할 수 없다.
- 담은 사업 문구도 다듬었다. 예전에는 **별표 설명이 화면에 두 번** 나왔다.

확인: 두 화면 × 다섯 상태에서 **스크롤 못 하는 자리 0곳 · 콘솔 오류 0건**,
설명회 화면 목록·페이저·뒤로가기·휴대폰 탭 불 ✓, 주소 복사를 두 번 눌러도 링크가 남음 ✓.

**⑬ 화면을 또 세 군데 손봤다** (사용자 요청)
- **주요 메뉴에서 '이용안내' 제거** — 맨 위 유틸바와 푸터에 이미 있어 중복이었다.
- **이름을 `관심사업 / 알림` 으로** (휴대폰 탭은 자리가 좁아 `관심/알림`).
- **설명회에서 '자세히 보기'가 모달이 아니라 그 화면에서 펼쳐진다** —
  `#briefList` ↔ `#briefDetail` 를 번갈아 쓴다(지도 화면과 같은 방식).
  설명회는 '다른 사업은 언제인가'를 견주어 보는 자리라 모달로 덮으면 목록을 잃는다.
  **본문은 `detailBodyHtml()` 한 곳**이라 모달과 늘 같은 것을 보여준다.
  **홈·관심사업 화면은 그대로 모달**이다 — 가르는 것은 `bindProjectActions` 의 옵션 하나뿐이다.

**⑭ 설명회 상세를 눈에 갈리게 다듬었다** (사용자 요청)
- 상세 전체를 **흰 카드**로 감쌌다 — 목록과 같은 칸을 쓰는데 배경색이 같아
  **어디서 시작해 어디서 끝나는지 알 수 없었다.** 실측: 카드 `rgb(255,255,255)` vs
  화면 `rgb(246,248,251)`(휴대폰은 `body.m` 배경이 그 색이라 실제 픽셀로 확인).
- `설명회 목록으로` 에 **테두리**를 주고 제목과 16px 띄웠다. 글자만 있으면
  넓은 화면에서 누를 수 있는 것으로 안 보였다.
- **★ `#briefDetail` 안에서만 덮어썼다** — `.gis-detail-*` 을 직접 고치면
  **지도 화면 상세도 같이 바뀐다.** 고친 뒤 지도 상세가 그대로인지 재서 확인했다
  (테두리 0px · 안쪽 0px · 배경 투명).
- 제목 여백을 인라인 `style` 에서 `.bd-ttl` 클래스로 뺐다 — 인라인은 CSS 를 이겨서
  화면마다 조절할 수 없다.

확인: 두 화면 × 여섯 상태에서 **스크롤 못 하는 자리 0곳 · 콘솔 오류 0건**.
홈 '자세히 보기'는 여전히 모달 ✓ / 설명회는 그 화면에서 펼쳐지고 목록으로 돌아옴 ✓ /
상세 안 '담아 두기'가 실제로 담김 ✓ / 초점은 '설명회 목록으로'로 옮겨지고
**마우스로 누르면 링 없음, 키보드로 누르면 링 표시** ✓ / **지도 화면 상세는 안 바뀜** ✓.

**⑮ 뒤로가기가 방금 나온 화면으로 다시 들어가던 것** (사용자 제보)
`설명회 → 우리 동네로 돌아가기 → 뒤로가기` 를 누르면 **설명회로 다시 들어갔다.**
'돌아가기' 가 `show()` 를 불러 **기록을 하나 더 쌓았기** 때문이다
(실측: 홈 len3 → 설명회 len4 → 돌아가기 **len5**). 첫 화면까지 뒤로가기 **세 번**이 필요했다.
- **네 곳 다 같은 문제였다** — 설명회 · 지도 · 관심사업 · 관리자. 셋을 실측으로 확인했다.
- `goBackScreen()` 이 `history.back()` 을 부른다. 기록에 **깊이(`i`)** 를 적어 두고,
  깊이가 0이면(주소로 바로 들어온 경우) **사이트를 벗어나지 않게** 그 자리를 바꾼다.
- 모달을 뒤로가기로 닫는 자리에도 `i` 를 넣었다 — 빠뜨리면 그 뒤로 깊이가 0이 되어 또 어긋난다.

확인: 세 화면 모두 **돌아가기 뒤 기록 길이가 그대로(4)** 이고 **뒤로가기 한 번에 첫 화면** ✓ /
`?admin=1` 로 바로 들어와 '나가기' 를 눌러도 **사이트를 벗어나지 않음** ✓ /
모달을 뒤로가기로 닫은 뒤에도 '돌아가기' 정상 ✓ / 휴대폰 ✓ /
지도 나가기 뒤 **미니 지도 330×190 정상**(`history.back()` 이 한 박자 늦어 확인이 필요했다) ✓.

**⑯ 다른 화면들의 뒤로가기도 훑었다** (사용자 요청)
**열세 가지 경우를 재 봤고 세 가지가 틀려 있었다.** 공통 원인은 하나다 —
화면 **안에서** 겹쳐 놓은 것은 기록에 안 쌓이는데, 뒤로가기가 그것을 걷어내지 않고
**화면을 통째로 벗어났다.**
- **설명회 상세** → 뒤로가기 → 홈으로 나가 버렸다 → 이제 **목록으로**
- **지도 상세** → 같은 문제 → 이제 **목록으로**
- **휴대폰 지도의 시트·마커 카드** → 열어 둔 채 뒤로가기 → 홈으로 나가 버렸다
  → 이제 **시트만 접히고 / 카드만 닫힌다**(지도에 머문다)
- 덤: **앞으로가기**로 돌아왔을 때 상세가 그대로 남아 목록이 안 보였다 →
  `show()` 가 화면을 옮길 때 접는다

**정상이던 것(잘못 짚었다가 확인한 것 포함)**
- 모달은 **실제로 겹치지 않는다** — 상세의 '의견 제출'이 상세를 먼저 닫는다.
  처음에 "겹친 모달이 둘 다 닫힌다"고 봤는데 **내 오독이었다.**
- 모달을 X로 여러 번 열고 닫아도 **기록이 안 쌓인다** ✓
- 앞으로가기 ✓ / 첫 화면에서 더 뒤로가면 사이트를 벗어난다(**정상** — 기록의 처음이다) ✓
- 첫 화면에는 모달을 열 단추가 **없다**(그래서 기록의 처음에서 모달이 열릴 일은 없다)
- 동네 변경 모달이 안 닫히는 것은 **로컬에 VWorld 키가 없어** 좌표를 못 찾은 것이고,
  화면이 그 까닭을 정확히 안내한다 — 의도된 동작이다

**⑰ 휴대폰 화면을 세 군데 정리했다** (사용자 요청)
- **머리말 별표를 없앴다** — 아래 탭에 이미 '관심/알림' 이 있어 길이 둘이었고,
  셋이 나란히 있어 동네 이름이 가운데 끼여 있었다. 이제 [로고] ─── [동네].
  담은 개수 배지는 잃지 않게 **아래 탭 아이콘 위**로 옮겼다(동작 확인).
- **알림설정 글을 줄이고 `주소 복사` 를 없앴다** — 안내글 **227자 → 81자(64% 감소)**.
  단추는 폰 2개(아이폰·구글) / PC 1개(구글).
  **잃는 것**: 구글 계정이 없거나 네이버·아웃룩·삼성 캘린더를 쓰는 주민은 등록할 길이 없다.
  다만 **갤럭시는 폰에서 안 된다는 사실은 문구에 남겼다** — 되는 것처럼 안내하면 안 된다.
- **홈 검색 칸의 배경 사진을 없애고 섹션을 흰 블록으로 갈랐다.**
  사진을 없애면 그 위의 **흰 글자·반투명 칩이 아예 안 보인다** — 글자색·강조색·검색창·칩
  네 가지를 함께 바꿨다. 재서 확인: 제목 11.77 · 강조 7.24 · 입력 21.00 · 칩 9.26.
  첫 화면과 데스크톱 히어로는 사진을 그대로 쓴다.

확인: 머리말 자녀 2개(알약이 오른쪽 끝 14px) ✓ / 담은 개수 배지 동작 ✓ /
알림 지역을 바꾸면 구독 주소가 따라옴 ✓ / 홈 검색 동작 ✓ /
두 화면 × 여섯 상태 스크롤·콘솔 **오류 0건** ✓.

> **★ 지금 남은 것 — 높음은 없다**
>
> **① 일부만 고친 1건**: Leaflet 은 저장소로 들여왔지만 **한글 웹폰트는 여전히 jsdelivr**,
> **CSP 는 아직 없다.** 폰트는 스크립트만큼 위험하지 않으나 `@import` 경로라 스타일 주입은 가능하다.
>
> **② 새로 찾았지만 — 고칠 필요 없다고 결론 냈다.**
> `run_daily.bat` 을 손으로 돌리면 화면에 `'아서' is not recognized` 가 한 줄 뜬다.
> cmd 가 **17번째 줄(주석)** 을 특정 바이트 위치에서 잘라 읽어 뒤쪽을 명령으로 실행하려 드는 것이다.
> **고치기 전에도 있었다**(HEAD 2줄 → 지금 1줄).
> **해가 없음을 실측으로 확인했다** — `REM` 이 `>` 를 막아 파일이 덮어써지지 않고,
> 메시지는 **`build_log.txt` 에 안 들어가며**(화면에만), 작업 스케줄러로 돌면 창이 없어
> **아무 데도 안 보인다.** 수집·판정·커밋은 그대로 돈다.
> **고치려 들지 말 것** — 한 글자만 밀어도 사라지고 BOM 을 붙이면 오히려 늘어난다(0→2).
> 28가지 배치에서 **진짜 명령이 망가진 경우는 0건.** 자세한 것은 `CLAUDE.md` 의
> `run_daily.bat` 항목에 있다.
>
> **③ 보통 25건 · 낮음 16건** — 보고서에 근거와 위치가 다 있다.

---

## 3. 지금 상태

### 동작 확인된 것 (배포 사이트 기준)

| 기능 | 상태 |
|---|---|
| EIASS 수집 (전략 + 환경영향평가) | 동작 — **41건** (공람 중 + 의견제출 유예기간 포함). 건수는 매일 바뀐다 |
| 요약문 PDF → AI 환경영향분석 (8개 고정 항목) | 동작 — 41건 중 39건. 못 채운 것은 공람이 끝나 첨부가 내려갔거나 스캔 PDF |
| VWorld 지오코딩 (주소 → 좌표) | 동작 — **41건 중 39건**. GitHub 에서는 서울 중계를 거친다. 남은 2건은 VWorld 에 주소가 없다 |
| 지도 (Leaflet + VWorld 타일), 마커·반경 원 | 동작. 마커 색·모양 8/3 변경(원본 확인 필요) |
| 하천 사업 노선 자동 표시 | 동작 — 5건 |
| 선형 사업 반경 판정 (노선까지 최단거리) | 동작 — 8/3 추가. 대표주소만으론 반경 밖으로 잘못 판정되던 문제 수정 |
| 주민 설명회 안내 (일시·장소·의견받는곳) | 동작 — 8/3 신규. 회차별 짝짓기, 지남/생략/미정 구분 |
| 전국 협의 진행 중 건수 (전략/환경/소규모) | 동작 — 8/3 신규. EIASS에서 매일 자동 수집, 비용 0원 |
| 보는 범위 3단 전환 (동네 안 / 반경 / 전국) | 동작 — 8/6 개편. 고른 동네 모양에 맞는 기본값이 자동으로 잡힌다 |
| 시·도만 골라도 보기 · 시·도 칸의 `전국` | 동작 — 8/6 신규 |
| 지금 내 위치로 보기 (GPS → 동네 자동 설정) | 동작 — 8/6 신규. https·localhost 에서만 |
| 읍면동·시군구 경계 표시 + 경계 기준 사업 가리기 | 동작 — 8/6 신규. 노선이 경계를 지나는 사업도 잡는다 |
| **시·도 경계 표시** | 동작 — 8/8 신규. `data/sido.json`(미리 단순화, 367KB). VWorld 키 없이도 나온다 |
| **휴대폰 전용 화면 (`m.html`)** | 동작 — 8/6 신규. 짧은 변 ≤600px + 손가락 기기면 자동 이동. `?pc=1` 로 탈출 |
| **내가 담은 사업 / 관심목록** | 동작 — 8/8 데스크톱, **8/16 휴대폰까지**. 카드 별표로 담는다. 휴대폰은 아래 탭 `관심목록` 또는 머리말 별표 |
| **새로 올라온 사업 알림** | 동작 — 8/16 신규. 마지막 본 날짜(`wdn.lastSeen`)만 기억. 서버·권한 0 |
| **마감일 캘린더에 넣기** | 동작 — 8/16 신규. `.ics` 파일 + **구글 캘린더 링크**(갤럭시용) |
| **지역 캘린더 구독** | 동작 — 8/16 신규. `data/cal/` **5,339개**(읍·면·동까지). 새 사업이 뜨면 폰 캘린더에 저절로 들어온다 |
| **위성지도 전환** | 동작 — 8/16 신규. 기본은 일반지도(**기억하지 않는다**). 마커는 핀, 색은 위성용으로 따로 |
| **노선 그리기 도구** (`route_editor.html`) | 동작 — 8/16 신규. 손그리기 + **도면 반자동 추출**(기준점 3~4개만 사람이) · **8/17: 해제 2개 · 시작·종점 지정 · 갈래(MultiLineString) · `?id=` 바로 열기 · AI 기준점 후보** |
| **휴대폰 지도 작은 카드** | 동작 — 8/17 신규. 마커·노선을 누르면 **지도를 덮지 않는 작은 카드**, `자세히 보기`로 전체 화면 |
| **드롭다운 펼친 목록** | 동작 — 8/17 신규. 운영체제가 그리던 목록을 우리 것으로(`openSelPop`). 손가락 기기는 기본 선택기 |
| **관리자 — 날짜별 새 사업 · 진입로 3개** | 동작 — 8/17 신규. `?admin=1` · 첫 화면 단추 · 홈 푸터 기관명 |
| **관리자 — 노선 그릴 사업 짚어 주기** | 동작 — 8/17 신규. `선형` 표시 없는 환경영향평가는 **이름으로 짐작**해 후보로만(자료엔 안 넣음) |
| 반경을 주민이 직접 고르기 (1~20km) | 동작 — 8/6 신규. 기본 5km. 내 동네 카드의 `반경` 칸 |
| 다크 모드에서 색 뒤집힘 차단 | 동작 — 8/7 신규. `color-scheme:light` 두 줄. **지우면 안 된다** |
| 동네 변경 모달 | 동작 — 첫 화면으로 안 튄다 |
| 브라우저 뒤로가기 | 동작 — 8/3 추가. 화면 전환 기록을 남겨 이전 화면으로 복귀 |
| 의견 제출 → EIASS 안내 후 이동 | 동작 |
| 의견제출 마감 기준 필터링 + AI 해석은 공람기간만 차단 | 동작 — 8/3 규칙 변경 (§4 CLAUDE.md 참고) |
| 관리자 노선 직접 그리기 | 동작 — 8/3부터 지도 화면에서 완전히 분리(관리자 전용) |
| 휴대폰 반응형 (글자·버튼 크기, 지도/상세 전환) | 동작 — 8/3 개선 |
| 페이지 나누기 (사업 카드/설명회 목록) | 동작 — 8/3 신규 |
| **푸시 → 자동 배포** | 동작. 배포마다 파일 버전 자동 갱신(캐시 문제 해결) |
| **GitHub Actions 자동 수집** | **동작 — 8/15 확정.** 매일 07·13시. EIASS 는 서울 중계를 거친다 |
| 손으로 수집 (`run_daily.bat`, 한국 PC) | 동작 — 한 번도 실패한 적 없음. **급할 때 쓰는 길로만 남겨 둔다** |

| 수집 실패 시 안전장치 | 동작 — 8/6 신규. 재시도 4회+1회 / 목록 실패 시 저장 안 함 / 건수 급감 시 커밋 중단 |
| 실패 원인을 밖에서 읽기 | 동작 — 8/6 신규. `collect.yml`이 로그 끝부분을 GitHub 주석으로 내보낸다 |

### 데이터 (`data/projects.json` — git에 포함된다)

- 생성일 **2026-08-16**, **41건** (공람 중 + 의견제출 유예기간)
- ★ **`firstSeen` 41/41 · `summaryState` 31건**(ok 30 / **scanned 1** / 나머지는 공람 종료라 대상 아님)
  — 2026-08-16 밤 수집분부터 실제로 채워졌다. 관리자 화면의 '발견일 기록 없음' 경고는 이제 안 뜬다
- AI 해석 **29건** (공람 끝난 10건은 저장 직전에 지운다 — 8/16 규칙) / 좌표 **39건** / 노선 **7건**
- `data/routes.json` — 사람이 그린 노선 **4건** (광양 공업용수도 · 대구1호선 영천연장 · 면목선 · **345kV 신해남~신장성 송전선로**)
- `data/cal/*.ics` — 지역별 캘린더 **5,339개** (전국1+시도16+시군구255+읍면동5,067, 내용 1.99MB)
- `stats.underReview`: 전략 117 · 환경 113 · 소규모 367 = **전국 597건** (수집할 때마다 갱신)
- **건수는 매일 바뀐다.** 위 숫자는 2026-08-15 수집분 기준이다
- 좌표 39/41 — 못 찾은 2건은 VWorld 에 그 주소가 없다(한국에서 불러도 같다). 정상이다

> 반경 안에 사업이 0건으로 나오는 건 **정상일 수 있다.** 우리 동네 근처에 마침
> 없을 뿐이다. 상단 "전국" 스위치를 누르면 다 보인다.

### 자동화 구조 (2026-08-15 현재 — 수집까지 자동)

```
  ■ 지금 실제로 도는 길

  매일 07시·13시(한국)
        │
        ▼
   collect.yml
        │
        ├─▶ [가는 길 확인] ── 서울 중계로 EIASS 접속 ✅
        │        (지역이 ap-northeast-2 가 아니면 여기서 멈춘다)
        │
        ├─▶ [수집] EIASS ── 서울 중계 ──▶ 목록·상세·PDF ✅
        │          VWorld ── 서울 중계 ──▶ 좌표 ✅
        │          Anthropic ─ 직접 ────▶ 해석 ✅
        │
        ├─▶ [안전장치] 건수 0 이거나 절반 아래면 커밋 안 함
        │
        └─▶ 커밋 ──▶ deploy.yml ──▶ 사이트 배포 ✅


  ■ 손으로 돌리는 길 (그대로 남아 있다. 급할 때만)

  [담당자 PC — 한국]  run_daily.bat 더블클릭
    수집(EIASS 직접) → 커밋 → 푸시 ──▶ deploy.yml → 배포 ✅
```

- **수집은 사람이 한다.** 매일 아침 `tools\run_daily.bat` 더블클릭.
- **배포는 자동이다.** 푸시만 되면 `deploy.yml`이 알아서 올린다. 이쪽은 문제없다.
- **자동 수집이 실패해도 사이트는 안 죽는다.** 어제 자료로 정상 동작한다
  (화면이 날짜를 다시 판단하므로 기한 지난 사업이 새어 나가지 않는다).
- 실패하면 GitHub이 저장소 주인에게 메일을 보낸다. 하루 두 번 온다.
  귀찮으면 `collect.yml`의 `- cron:` 두 줄을 주석 처리한다.

### 비용 (`claude-haiku-4-5`, 실측 기반)

| | 비용 |
|---|---|
| 사업 1건당 | 약 $0.02 |
| 매일 증분 (신규 1~3건 + 협의건수 조회 0원) | 월 $1~2 (3,000원 이하) |

증분 수집이 비용의 90% 이상을 줄인다. 서버 비용은 0원(사람 PC에서 돌리므로).

> **2026-08-06에 소넷(`claude-sonnet-5`)을 시험했다가 되돌렸다.**
> 품질은 좋았다 — 시험에서 8개 항목 중 원문에 없는 4개를 정확히 `null` 처리했다.
> 되돌린 이유는 값이 3배($0.02 → $0.05~0.08/건)인데, 같은 날 수집이 계속 실패하면서
> 재시도로 비용이 더 나갈 위험이 있었기 때문이다. 수집이 안정된 뒤에 다시 검토할 만하다.
> **다시 바꿀 때는 `max_tokens`를 6000 → 16000으로 반드시 같이 올려야 한다**(§5 함정 참고).

증분 수집이 비용의 90% 이상을 줄인다. 돈 문제는 사실상 없다.
**서버 비용은 0원이다** — GitHub Actions 무료 사용량 안에서 돈다(공개 저장소는 무제한).
하루 두 번 돌아도 마찬가지다. 검토하던 AWS Lightsail 월 7천원은 안 쓰게 됐다.

---

## 4. 중요한 결정과 이유 (되돌리지 말 것)

**아래 결정들은 다 이유가 있어서 그렇게 된 것이다. 뒤집기 전에 `docs/HISTORY.md` 를 읽을 것.**
근거·측정치·실패 경위가 전부 거기 있다. 여기는 **무엇을 정했는지 한 줄씩**만 적는다.

| 결정 | 한 줄 요약 |
|---|---|
| **기준은 공람 종료일이 아니라 의견제출 마감일** (8/3) | 시행령 제38조 — 공람 후 7일 이내까지 의견 가능. 공람 종료로 끊으면 아직 낼 수 있는 사업이 사라진다 |
| **★ 단, AI 해석만은 공람 기간에만** | 평가서 초안은 법적으로 공람 기간에만 열람 가능. `viewOpen`(공람 중)과 `open`(의견 가능)을 **절대 합치지 말 것** |
| **AI 는 번역만 한다** | 항목 8개 고정, 없으면 `null`. 판단·홍보 문구 금지 |
| **도면(그림)을 읽어 노선을 뽑지 않는다** | 좌표 격자가 없어 픽셀→위경도 기준점이 없다. 대신 하천 이름으로 VWorld 하천망도에서 받는다 |
| **선형 사업은 노선까지의 최단거리로 반경 판단** (8/3) | 대표 주소가 노선에서 수십 km 떨어진 경우가 흔하다 |
| ~~휴대폰 사이트를 따로 만들지 않는다~~ → **뒤집었다** (8/6) | 폭 기준으로 데스크톱을 줄여 쓰다 계속 깨졌다. 화면만 나누고 **`app.js`·`texts.js` 는 하나로 공유** |
| **★★ EIASS 접속 — "해외 IP 차단"은 사실이 아니다** (8/6) | 전 세계 17/20곳 정상. 그런데 **GitHub 러너에서만** 안 된다(6번 중 1번 성공). WARP 우회도 3번 연속 실패해 탈락 |
| **관리자 잠금에 진짜 보안을 붙이지 않는다** (8/16) | 서버가 없어 원리적으로 불가능하고, **지금은 지킬 자산도 없다** — 관리자 설정은 전부 그 브라우저에만 저장되고 브라우저에서 서버로 쓰는 코드가 0건이다. 뒤집히는 조건은 `CLAUDE.md` "관리자 잠금은 보안장치가 아니다" 참고 |

> ### ★ 이 문서를 쓰는 사람이 꼭 기억할 것 (EIASS 건에서 얻은 교훈)
> 1. **한 번 실패한 것을 "확인함"이라고 쓰지 말 것.** 그 결론이 돈 쓰는 결정으로 이어질 때는 특히.
>    실제로 "해외 IP 차단"이라는 **틀린 제약**이 문서 3곳에 박혀 AWS 이전 검토까지 갔다.
> 2. **몇 번 성공한 것을 "된다"고 단정하지도 말 것.** 3번 중 1번 성공을 보고 자동화를 켰다가 3번 연속 실패했다.
>
> 전말은 `docs/HISTORY.md` §4 "EIASS 접속" 에 측정치까지 그대로 남겨 두었다.

---

## 5. 알려진 함정 (다 실제로 겪은 것)

| 함정 | 증상 | 대응 |
|---|---|---|
| **EIASS 접속이 간헐적으로 안 됨** | 어떤 날은 DNS 조회 실패, 어떤 날은 TCP 무응답 (**차단 아님**, §4) | `build_data.py`의 `SESSION`이 4·8·16초 쉬며 4번 재시도. 하루 두 번 실행 |
| **"연결이 되나"와 "실제로 되나"를 헷갈림** | 점검이 `curl https://api.vworld.kr/` 에 응답만 오면 "OK"라고 답했다. **정작 지오코딩은 하나도 안 되고 있었다.** VWorld 는 키를 거부할 때도 **HTTP 200** 에 `status=ERROR` 를 담아 준다 | 점검은 **실제로 쓰는 방식 그대로** 부른다. 상태 코드가 아니라 **본문의 `status` 를 읽는다** |
| **실패를 조용히 삼킴** | 새 사업 좌표가 하나도 안 채워졌는데 **로그가 깨끗했고 수집도 '성공'으로 끝났다.** `geocode_address()` 가 '주소를 못 찾음'과 '거부당함'을 구분 없이 `(None,None)` 으로 넘겼다 | `NOT_FOUND` 가 아닌 응답은 `[지오코딩 거부] status=...` 로 남긴다. **'실패'가 아니라 '반쪽 성공'은 어떤 안전장치에도 안 걸린다** |
| **HTTP 헤더에 한글** | 중계가 오류 까닭을 한글로 헤더에 담았다가, **오류를 알리려다 그 자리에서 또 죽었다**(헤더 값은 라틴 문자만 된다) | 까닭은 **본문**에, 헤더에는 **짧은 아스키 코드**만 |
| **셸 heredoc 이 백슬래시를 먹음** | `collect.yml` 에 `tr -d '\r'` 을 넣었더니 **실제 CR 바이트**가 파일에 박혀 YAML 이 깨졌다. 줄 이음(`\`)도 사라졌다 | 셸 heredoc 으로 코드 파일을 쓰지 않는다. **편집 도구나 파이썬 파일**로 쓴다 |
| **셸에 한글을 박은 판정 스크립트** | 인코딩이 깨져 **8번 성공한 것이 8번 실패로 찍혔다.** 판정을 뒤집을 뻔했다 | 측정·판정 코드는 **파일로 빼서** 돌린다. 셸 한 줄로 판정하지 않는다 |
| **저장소와 배포본이 조용히 어긋남** | Supabase 대시보드는 **버전 관리가 없다.** `tools/relay/index.ts` 만 고치고 배포를 안 하면 옛 판이 계속 돈다(허용 목록을 늘렸는데 `403 not-allowed` 로 막힘) | 고쳤으면 **반드시 다시 배포**하고 `check.yml` 로 확인. 이제 **Supabase 커넥터로 AI 가 직접 배포·대조할 수 있다** |
| **한 번 실패를 "확인함"으로 단정** | 틀린 제약이 문서 3곳에 박혀 반년 갈 뻔했다 (§4) | 제약을 문서에 넣기 전에 **두세 번 재현**해 볼 것 |
| **목록 받다 끊기면 통째로 죽음** | 2026-08-06 수집 실행이 100초 만에 실패. 재시도 4번(≈78~100초)을 다 쓰고 예외가 그대로 터졌다 | `fetch_open_items` 를 try 로 감싸 **아무것도 저장하지 않고** 안내 후 중단. 반쪽 저장이 더 위험하다 |
| **반쪽 수집이 안전장치를 통과함** | 전략 43건만 받고 환경 16건을 놓치면 48→43건. 10% 감소라 `collect.yml` 의 "절반 이하면 중단"을 통과해 버린다 | 목록 실패 시 아예 저장하지 않는 것으로 막았다 |
| **소넷은 `max_tokens`를 생각+답변이 나눠 쓴다** | 모델만 소넷으로 바꾸고 `max_tokens=6000`을 그대로 두면 생각하다 자리를 다 써서 JSON이 잘린다 | 소넷으로 바꿀 때는 **16000으로 같이 올린다**. `b.type=="text"` 필터도 필수(생각 덩어리가 섞여 옴) |
| **Actions 로그는 저장소 주인만 본다** | 도와주는 사람이 실패 원인을 못 봐서 진단이 계속 막혔다 (`403 Must have admin rights`) | `::error::` / `::notice::` **주석**으로 내보내면 누구나 API로 읽는다. `collect.yml`·`check.yml`이 그렇게 한다 |
| **한 번 성공을 "된다"로 단정** | 3번 중 1번 성공을 보고 자동화를 켰다가 3번 연속 실패 | 접속이 안 될 땐 몇 분간 계속 안 된다. 재시도로 안 덮인다 (§4) |
| **시험용 `--limit`이 진짜 데이터를 덮어씀** | `--limit 3`으로 돌리면 48건이 6건으로 줄어 그대로 커밋됨 | `collect.yml`이 `limit` 있으면 커밋 안 함 + 건수 급감 시 중단 |
| **VWorld 키 앞뒤 공백** | 지도 타일은 나오는데 지오코더만 `INVALID_KEY` | 코드 3곳에서 `strip()`. 거부되면 길이부터 세어 본다 |
| **★ `assets/config.js` 에 키를 적어 두고 커밋** | 2026-08-07 실제 발생. 화면 확인하려고 키를 잠깐 넣었는데 그대로 커밋·푸시되어 **공개 저장소에 키가 남았다**(`514496a`). git 은 지운 뒤에도 옛 커밋에 값을 계속 가진다 | **키를 새로 발급받는 것 외에 되돌릴 방법이 없다.** 예방: 내 PC 에서 지도를 볼 땐 `config.js` 말고 **관리자 화면**에 넣는다(브라우저에만 저장됨). 커밋 전 `config.js` 가 `vworldKey: ""` 인지 항상 확인 |
| **시험용 임시 html 이 커밋에 섞임** | `_m_test.html`(화면 확인용 사본)이 `514496a` 에 같이 들어갔다 | `.gitignore` 에 `_*.html` 추가. 임시 파일은 **밑줄로 시작**하는 이름만 쓴다 |
| **`.bat` 줄바꿈이 LF** | 더블클릭해도 조용히 안 열림 | 반드시 CRLF. `.gitattributes`로 고정 |
| **배치 `ERRORLEVEL`** | 멀쩡한 실행이 "실패"로 끝남 | `echo` 뒤 말고 명령 직후 바로 확인 |
| **목록 표가 두 개 / 날짜순 아님** | 중복 수집·누락 | 사업번호로 중복 제거, 빈 페이지 5쪽까지 넘겨봄 |
| **AI 응답 잘림** | JSON 파싱 실패로 해석 통째 유실 | `max_tokens` 6000, 잘리면 `stop_reason` 로그 |
| **증분 수집이 옛 값을 붙잡음** | `미정`으로 한 번 저장되면 영영 `미정` | `needs_detail_refresh()` — 안 정해진 값 있으면 상세 재조회(PDF·AI는 건너뜀) |
| **브라우저가 옛 app.js를 붙잡음** | 배포는 됐는데 화면은 옛 코드 | 자산 주소에 `?v=커밋SHA`를 배포 때마다 자동으로 심음(`deploy.yml`) |
| **★ 커밋을 연달아 하면 배포가 죽는다** | 7분 간격으로 두 번 커밋 → 앞 배포가 **올리는 도중 잘림** → 뒤 배포가 10분 기다리다 `Timeout reached, aborting!` 으로 실패. **사이트는 옛 판 그대로**라 "고쳤는데 그대로네?"가 된다 (2026-08-06 겪음) | `deploy.yml` 의 `cancel-in-progress` 를 **false** 로. 올리는 중인 배포는 끝까지 두어야 한다 (GitHub 공식 Pages 워크플로도 false) |
| **배포 실패를 모르고 화면만 의심함** | 폰에서 안 바뀌길래 판정 규칙·브라우저를 의심했는데, 실제로는 배포가 실패해 파일이 안 올라가 있었다 | **먼저 파일이 살아 있는지 본다** — 브라우저에서 `.../alrimi/m.html` 을 직접 연다. 404면 배포 문제지 화면 문제가 아니다 |
| **격자 `1fr`이 상자보다 넓어짐** | 휴대폰에서 오른쪽이 잘려 보임(스크롤도 안 생김) | `minmax(0,1fr)` + `min-width:0` |
| **`hidden`이 안 먹힘** | 클래스 `display`가 브라우저 기본을 이김 | `[hidden]{display:none!important}` 리셋에 추가 |
| **일시·장소 순서로 짝지으면 틀림** | 기장군 사업: 일시·장소 순서가 다름 | 지역 이름으로 매칭, 안 되면 원문 통째로 표시 |
| **`file://`로 열기** | 표본 데이터로 대체됨 | 반드시 `python -m http.server 8000` |
| **EIASS 상세는 POST 전용** | 링크로 못 엶 | 숨은 폼 제출 (`openEiassSource`) |
| **윈도우 cp949 콘솔** | 한글 로그에서 죽음 | 파이썬은 `log()` 사용 |
| **시도가 16개** | 17개가 아님 | 광주+전남이 VWorld 최신 자료 기준 통합됨 |

---

## 6. 파일 지도

| 파일 | 역할 |
|---|---|
| `index.html` | **데스크톱** 화면 4개 + 모달 5개. 맨 위에서 휴대폰이면 `m.html` 로 보낸다 |
| `m.html` | **휴대폰 전용** 화면. 탭 막대 + 전체화면 지도 + 올라오는 시트 |
| `assets/app.js` | ★ 모든 판단·동작. **두 화면이 함께 쓴다. 복제 금지** |
| `assets/mobile.js` | 휴대폰 배치만 (탭 막대·시트·범례). 판단하는 코드 금지 |
| `assets/texts.js` | 이용안내·문의 본문. **두 화면이 함께 읽는다. 복제 금지** |
| `assets/app.css` / `mobile.css` / `tokens.css` | 공용 스타일 / 휴대폰 배치 / 디자인 토큰 |
| `assets/config.js` | **빈 채로 커밋.** 배포 때 VWorld 키가 채워진다 |
| `tools/build_data.py` | EIASS 수집. `--full` / `--retry-analysis` / `--skip-stats` / `--limit`.<br>`_RelayAdapter` 가 **EIASS·VWorld 만** 서울 중계로 보낸다 (Anthropic 은 직접) |
| `tools/relay/index.ts` | **서울 중계 함수의 정본.** Supabase 에 배포해 쓴다. 고치면 **반드시 다시 배포** |
| `tools/build_regions.py` | 행정구역 목록 (한 번만 돌리면 됨) |
| `tools/build_boundaries.py` | **시·도 경계** 를 미리 단순화해 `data/sido.json` 생성 (개편 때만) |
| `tools/run_daily.bat` | 수집 → 커밋 → 푸시. **반드시 CRLF**. 지금 실제로 쓰는 운영 경로 |
| `.github/workflows/deploy.yml` | 푸시하면 배포. 키 심기 + 캐시 버스팅(버전 번호 삽입) |
| `.github/workflows/collect.yml` | **매일 07시·13시(한국) 자동 수집** → 커밋 → 배포 |
| `.github/workflows/check.yml` | 직접/WARP 두 경로로 EIASS에 닿는지 시험. `[판정]` 주석에 결론. 데이터 안 건드림 |
| `data/projects.json` | 수집 결과 + `stats.underReview`. **git 포함** |
| `data/regions.json` | 전국 행정구역 |
| `data/sido.json` | 시·도 경계 도형 (미리 단순화, 367KB). **git 포함** |
| `data/routes.json` | 관리자가 그린 노선 (없어도 정상) |
| `docs/SETUP.md` | 배포 설정 설명서 (처음 한 번) |
| `docs/HISTORY.md` | **왜 그렇게 결정했나 + 날짜별 작업 기록.** 매번 읽을 필요 없다 |
| `.env` | API 키 (**git 제외**) |

---

## 7. 지금까지 한 일

**날짜별 상세 기록은 `docs/HISTORY.md` 로 옮겼다.** (여기 있던 290줄)
아래는 큰 줄기만이다. "이거 예전에 해봤나?" 싶으면 HISTORY 를 찾아볼 것.

| 날짜 | 무엇을 했나 |
|---|---|
| **08-01** | 실제 EIASS 데이터 연동, 지도·노선, 전국 수집 자동화, GitHub Pages 배포 |
| **08-02~03** | 실사용 관점 전면 개선 — 환경영향분석 가독성, 페이지 나누기, 주민 설명회 안내 개편, **기준을 의견제출 마감일로 전환** |
| **08-06 (낮)** | 수집 자동화 시도 → **실패**. 다만 **"EIASS 해외 IP 차단"이 틀렸음을 밝혀냈다** |
| **08-06 (저녁)** | 동네를 세 칸 다 안 골라도 되게, `내 위치로 보기`(GPS), 보는 범위 3단, 읍면동·시군구 경계 |
| **08-06 (밤)** | **★ 휴대폰 전용 화면 분리** (`m.html`) — 구조가 바뀐 큰 변경 |
| **08-07** | 휴대폰 화면 다듬기(5·6차), 다크 모드 색 반전 차단, **VWorld 개발용 키 유출 → 교체 완료** |
| **08-08** | 지도 축척 기준 정리, 반경 원 규칙, **시·도 경계**(`data/sido.json`), 배경 사진 교체 |
| **08-08 (밤)** | **★ 내가 담은 사업 화면(화면 5) 추가**, AI 해석 빈 7건 원인 규명 |
| **08-09** | 이 문서를 `HANDOFF.md` / `HISTORY.md` 로 갈라 냄 (840줄이라 한 번에 안 읽혔다) |
| **08-09~15** | **★ 수집 자동화 완성** — EIASS·VWorld 를 서울 중계(Supabase)로. 자동 수집 4회 연속 성공, 좌표 30/41→39/41 |
| **08-15** | 코드 점검(담은 사업 '반경 밖' 배지 버그·죽은 코드 정리), 화면 측정(대비 5.48 등), Supabase 커넥터 연결 |
| **08-16** | 휴대폰 마무리(관심목록·새 사업 알림·캘린더 구독 272개→5,339개·위성지도·노선 도구), 공람 끝난 해석을 파일에서도 지우기, 공공누리 4유형 출처 표기 |
| **08-16 (밤)** | 워크플로 액션 판 올림(Node 20 종료 대비), **이용안내 법 조문 대조 — 전략환경영향평가 조문(제14·15조) 누락 2건 수정**, **`firstSeen` 도입**(새 사업 판정을 '우리가 처음 본 날'로), **스캔본 요약문 구분**(`summaryState`), 도로·철도 노선 3건 사람이 그려 넣음, **캘린더 읍·면·동까지 확장**(272→5,339개) · PC 캘린더 칸 정리 · 푸터 가로 배치 |

---

## 8. 진행 기록은 Notion에도 있다

팀·멘토 공유용 진행 기록은 Notion "1차 작업 기록 · 첫 화면 디자인 컨셉 선정" 페이지
맨 아래 "진행 기록" 섹션에 날짜별로 쌓고 있다. 이 문서(HANDOFF.md)는 **다음 세션이 이어받기
위한 기술 인수인계**이고, Notion은 **왜 그렇게 결정했는지의 팀 공유 기록**이라 목적이 다르다.
큰 작업을 끝내면 두 곳 다 갱신할 것.
| **08-17** | 휴대폰 지도 **작은 카드**(마커→카드→자세히) · 뒤로가기 단추 통일 · 시트 머리줄 잘림 수정 · **드롭다운 펼친 목록 교체**(openSelPop) · 담은 사업 배치 · 푸터 가로 배치 · 관리자 **진입로 3개**+날짜별 새 사업+비밀번호 개편 · 캘린더 **읍면동까지 5,339개** · **AI 도면 실험(B안 실패로 확정)** + `tools/plan_gcp.py`(A안) · 노선 도구 3건(해제·시작종점·갈래) |
