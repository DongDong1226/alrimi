/* ============================================================
   배포할 때 채워지는 설정
   ============================================================
   이 파일은 **빈 채로** 저장소에 들어 있다.
   GitHub Actions 가 사이트를 배포할 때 저장소 Secret 에 넣어 둔 값으로
   이 파일을 덮어써서 올린다. 그래서 키가 git 이력에는 남지 않는다.

   여기 들어가는 VWorld 인증키는 **도메인 제한이 걸린 키만** 쓴다.
   (vworld.kr 에서 서비스 URL 을 배포 주소로 등록해 둔 키)
   페이지 소스에서 보이긴 하지만 등록한 주소 밖에서는 동작하지 않는다.

   ※ LLM API 키(ANTHROPIC_API_KEY)는 절대 여기 넣지 않는다.
     그건 파이썬이 GitHub 서버 안에서만 쓰고, 브라우저로 가지 않는다.

   ※ 키 앞뒤 공백은 배포 워크플로와 app.js 가 둘 다 걷어낸다.
     공백이 하나만 붙어도 VWorld 지오코더가 INVALID_KEY 로 거부하는데,
     지도 타일은 멀쩡히 나와서 원인을 찾기가 아주 어렵다 (2026-08-01 겪음).

   내 PC 에서 열 때는 이 값이 비어 있으므로,
   전처럼 관리자 화면에서 키를 넣으면 된다.
   ============================================================ */
window.WDN_CONFIG = {
  vworldKey: ""
};
