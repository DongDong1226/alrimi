# 파이썬
__pycache__/
*.pyc
.venv/

# API 키 등 비밀값 (절대 커밋하지 않음)
.env

# 화면을 확인하려고 잠깐 만드는 사본 (밑줄로 시작하는 파일).
# 2026-08-07 에 _m_test.html 이 실수로 커밋됐다. 이름 규칙으로 막는다.
# ★ 2026-08-17 에 확장자 제한을 풀었다 — 도면 시험용 _plan_test.png 처럼
#   html 이 아닌 임시 파일도 만들게 되어서다. 맨 위 폴더의 '_' 로 시작하는 것은 다 무시한다.
/_*

# data/projects.json 은 이제 커밋한다.
# GitHub Pages 는 저장소에 있는 파일만 방문자에게 전달하므로,
# 이 파일이 빠져 있으면 배포된 사이트에서 사업 정보가 아예 안 보인다.
# (매일 아침 .github/workflows/collect.yml 이 갱신해서 커밋한다)
data/build_log.txt

# OS/에디터
.DS_Store
Thumbs.db
# 윈도우에서 "> nul" 로 출력을 버릴 때 git-bash 가 진짜 파일로 만들어 버린 찌꺼기
nul
tools/_shots/
tools/_plans/
