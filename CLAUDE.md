# 우리동네 개발사업 알리미

내가 사는 동네의 개발사업과 환경영향평가를 한 화면에서 확인하고,
공람 기간 안에 의견을 낼 수 있게 하는 주민용 웹 서비스.

- 발주: 기후에너지환경부
- 사용자: 개발사업 관련 지식이 없는 일반 주민 한 명
- 이 프로젝트를 만드는 팀: 2~3명, 코딩 경험 거의 없음
- 목표: 9월 데모에서 실제로 돌아가는 화면
- 저장소: `DongDong1226/alrimi` (**공개**). 커밋·푸시는 사람이 GitHub Desktop에서 직접 한다.
- 배포: https://dongdong1226.github.io/alrimi/ — 푸시하면 자동으로 다시 올라간다.

> **작업을 시작하기 전에 `docs/HANDOFF.md`를 읽는다.**
>
> | 문서 | 무엇이 있나 | 언제 읽나 |
> |---|---|---|
> | `CLAUDE.md` (이 문서) | 바뀌지 않는 **규칙과 구조** | 자동으로 들어온다 |
> | `docs/HANDOFF.md` | **지금 어디까지 왔고 다음에 뭘 할지** | **작업 시작할 때 항상** |
> | `docs/HISTORY.md` | **왜 그렇게 결정했나 · 날짜별 작업 기록** | 결정을 뒤집으려 할 때만 |
>
> 작업이 한 단계 끝나면 `HANDOFF.md`를 갱신한다.
> **지나간 기록은 `HISTORY.md`로 옮긴다** — HANDOFF.md가 길어지면 한 번에 안 읽혀서
> 정작 다음에 할 일을 놓친다 (2026-08-09에 840줄까지 자라 실제로 잘렸다).

---

## 절대 하지 말 것

1. **디자인 토큰을 새로 만들지 않는다.**
   색·글꼴·radius·그림자는 `assets/tokens.css`에 있는 변수만 사용한다.
   `#2F6B4A` 같은 하드코딩 hex를 새로 쓰지 않는다.
   원본은 `docs/reference/css3.txt`(eiass.css)에 있으니 필요하면 대조할 것.

   **★ 자바스크립트에서 지도 색을 정할 때도 마찬가지다 — `cssVar("--eco")` 로 읽어 쓴다.**
   hex 를 그대로 적으면 토큰이 두 벌이 되어, 나중에 토큰 색을 바꿔도 지도만 옛 색으로 남는다.
   (2026-08-16 점검에서 실제로 그런 자리 8곳을 찾아 고쳤다)

   **★ 이미 들어가 있는 예외 5개 — 건드리지 말 것 (2026-08-16 측정).**
   `#9fc0ff` `#dbe6fb` `#c4d3ee` `#8fa6d4` `#c9d7f2` 는 토큰에도 EIASS 원본에도 없는 색인데,
   **전부 남색 배경 위 글자색**이다(히어로 강조, 휴대폰 보조 글자).
   가장 가까운 토큰으로 바꿔 보려고 재 봤더니 **5개 다 눈에 띄게 색이 달라지고**
   (dE 5.4~10.9 — 구별 못 하는 한계는 3), **4개는 대비까지 떨어졌다.**
   지금 값들의 대비는 6.37~12.43 으로 **전부 기준(4.5)을 넉넉히 넘는다.**
   → 바꾸는 것은 '규칙 지키기'가 아니라 **디자인 변경**이다. 규칙 5에 걸리므로 사람에게 먼저 묻는다.
   나머지 하드코딩 hex 는 전부 EIASS 원본에서 온 값이라 문제없다.

2. **클래스명을 바꾸지 않는다.**
   `.btn--primary`, `.badge--navy`, `.dash`, `.civic`, `.kicker` 등은
   EIASS 원본 시스템의 이름이다. 나중에 본 시스템에 합칠 수 있어야 한다.

3. **외부 라이브러리·빌드 도구를 추가하지 않는다.**
   npm, webpack, vite, React, Tailwind 전부 금지. 순수 HTML/CSS/JS만.
   **예외: 지도용 Leaflet 1.9.4 (CDN)** — 사용자 승인 후 도입했다. 그 외는 물어볼 것.
   파이썬 쪽(`tools/`)은 프론트엔드가 아니므로 이 규칙과 무관하다.

4. **API 키를 프론트엔드 코드에 넣지 않는다.**
   AI 해석은 파이썬으로 미리 생성해 `data/projects.json`에 넣어둔다.
   **Anthropic 키는 절대 브라우저로 가면 안 된다.** GitHub Secret 에만 두고 파이썬이 쓴다.
   VWorld 키는 지도 타일·지오코딩에 브라우저가 직접 써야 해서 예외다.
   저장소가 공개라 **커밋하지 않고**, `deploy.yml`이 배포 직전에만 `assets/config.js`에 심는다.
   **서비스URL(도메인)이 배포 주소로 등록된 키만** 쓴다.
   키 앞뒤 공백은 세 곳(`deploy.yml`·`app.js`·`build_data.py`)에서 걷어낸다 — 아래 함정 참고.

5. **디자인 방향을 임의로 바꾸지 않는다.**
   "이게 더 예쁠 것 같아서" 팔레트나 레이아웃을 갈아엎지 않는다.
   개선안이 있으면 코드를 고치기 전에 말로 먼저 제안한다.

6. **AI가 요약문을 해석할 때 없는 말을 만들지 않는다.** (아래 "AI 해석 원칙" 참고)

7. **공람 기간이 지난 사업의 AI 해석 결과를 보여주지 않는다.**
   평가서 초안은 법적으로 공람 기간에만 볼 수 있다. 의견 제출 기한(공람 종료 +7일)이
   남아 있어도 **해석은 공람 종료와 함께 닫는다.** (아래 "기간 규칙" 참고)
   **★ 화면에서 가리는 것만으로는 부족하다.** `data/projects.json` 은 누구나 주소로
   곧장 내려받을 수 있는 공개 파일이라, 화면만 잠그면 글자는 그대로 읽힌다
   (2026-08-16 실측: 41건 중 10건이 그런 상태였다).
   그래서 `build_data.py` 가 **저장 직전에 해석을 파일에서 지운다.**

---

## 작업 방식

- 코드를 고치기 전에 **무엇을 왜 바꾸는지 한 줄로** 먼저 말한다.
- 한 번에 파일 하나씩 고친다. 요청하지 않은 파일은 건드리지 않는다.
- 큰 변경 전에는 커밋을 권한다.
- 설명은 비개발자 기준으로. 용어를 쓸 거면 한 번은 풀어서 쓴다.
- 안 되는 것은 "된다"고 하지 않는다. 제약이 있으면 바로 말한다.
- **★ "연결이 되나"와 "실제로 되나"는 다른 질문이다.** 점검은 **실제로 쓰는 방식 그대로**
  불러 봐야 뜻이 있다. 2026-08-15에 겪었다 — 점검이 `curl https://api.vworld.kr/` 에
  응답만 오면 "OK"라고 답하는 동안 **지오코딩은 하나도 안 되고 있었다.**
  (VWorld 는 키를 거부할 때도 **HTTP 200** 에 `status=ERROR` 를 담아 준다)
- **★ 실패를 조용히 삼키지 않는다.** '못 찾음'과 '거부당함'은 반드시 갈라서 로그에 남긴다.
  좌표가 하나도 안 채워졌는데 **로그가 깨끗했고 수집은 '성공'으로 끝났다.**
  **'실패'가 아니라 '반쪽 성공'은 어떤 안전장치에도 안 걸린다.**
- **판정하는 코드를 셸 한 줄로 쓰지 않는다.** 한글이 섞이면 인코딩이 깨져
  **8번 성공한 것이 8번 실패로 찍힌다**(실제로 겪음). 측정·판정은 **파일로 빼서** 돌린다.
- 사용자의 API 키를 대화창에 출력하지 않는다. 검증이 필요하면 `.env`를 읽는
  스크립트를 돌리거나 임시 키로 코드 경로만 확인한다.

## 코드 규칙

- 들여쓰기 2칸, 한국어 주석
- 파일 저장은 UTF-8
- CSS는 `tokens.css`(변수) → `app.css`(컴포넌트) 순서로만 로드
- JS는 라이브러리 없는 바닐라. `const`/`let`만, `var` 금지
- 데이터에서 온 문자열을 화면에 넣을 때는 `esc()`로 감싼다
- 접근성: 키보드 포커스 보이게, 아이콘 버튼에 `aria-label`,
  `prefers-reduced-motion` 존중
- 파이썬 로그는 `log()`를 쓴다 (윈도우 cp949 콘솔에서 죽지 않게 처리돼 있음)

---

## 파일 구조

```
index.html                     데스크톱 화면 5개(동네 설정 / 홈 / 관리자 / 지도 / 담은 사업) + 모달 5개
m.html                         휴대폰 전용 화면 (아래 "화면 파일이 두 개다" 참고)
route_editor.html              ★ 담당자용 노선 그리기 도구 (관리자 화면에서 링크로 연다)
                               **반드시 맨 위 폴더에 둔다** — deploy.yml 이 맨 위 *.html 만 올린다
assets/tokens.css              디자인 토큰 (EIASS 원본, --dscale 만 0.7로 조정)
assets/app.css                 컴포넌트 스타일 (두 화면 공용)
assets/mobile.css              휴대폰 전용 배치 (m.html 에서만 읽는다)
assets/app.js                  ★ 모든 판단·동작. 두 화면이 함께 쓴다
assets/mobile.js               휴대폰 배치만 (탭 막대·시트·범례). 판단하는 코드 금지
assets/texts.js                이용안내·문의 본문. 두 화면이 함께 읽는다
assets/config.js               배포 때 VWorld 키가 채워지는 자리 (빈 채로 커밋)
data/projects.json             수집 결과 (git 포함 — 배포 사이트가 이 파일을 읽는다)
data/regions.json              전국 행정구역 목록 (git 포함 — 자주 안 바뀜)
data/sido.json                 시·도 경계 도형, 미리 단순화 (git 포함, 367KB — 자주 안 바뀜)
data/routes.json               관리자가 직접 그린 노선 (git 포함, 없어도 정상)
data/cal/*.ics                 지역별 캘린더 (전국1+시도16+시군구255+읍면동5,067 = 5,339개, 1.99MB).
                               build_data.py 가 만든다. 주민이 구독하면 새 사업이 저절로 들어온다
tools/build_data.py            EIASS 수집 → projects.json 생성
tools/relay/index.ts           EIASS 중계 함수 원본 (Supabase Edge Function, 서울)
tools/shot.py                  화면을 그림으로 찍는 개발 보조 도구 (아래 참고)
tools/build_regions.py         VWorld → regions.json 생성
tools/build_boundaries.py      VWorld → sido.json 생성 (행정구역 개편 때만 다시 돌린다)
tools/run_daily.bat            손으로 급히 수집할 때 (반드시 CRLF). 평소 수집은 collect.yml 이 한다
.github/workflows/deploy.yml   푸시하면 사이트 배포. 이때만 VWorld 키를 심는다
.github/workflows/collect.yml  매일 07시·13시(한국) 자동 수집 → 커밋 → 배포
.gitattributes                 *.bat 를 CRLF 로 고정
requirements.txt               파이썬 패키지 목록
.env                           API 키 (git 제외). .env.example 참고
docs/HANDOFF.md                지금 상태와 다음에 할 일 (작업 시작 전에 읽는다)
docs/HISTORY.md                왜 그렇게 결정했나 + 날짜별 기록 (필요할 때만)
docs/SETUP.md                  배포 설정 설명서 (처음 한 번 따라 하는 것)
docs/reference/                최초 화면 시안과 EIASS 원본 CSS
```

**배포**: https://dongdong1226.github.io/alrimi/ (GitHub Pages, Source = GitHub Actions)

---

## 기간 규칙 (가장 중요)

이 서비스의 존재 이유는 **"의견을 낼 수 있는 동안 알려 주는 것"** 이다.
따라서 기한이 지난 사업을 현재 진행형으로 보여주면 서비스 목적을 해친다.

**기준은 공람 종료일이 아니라 `의견제출 마감일`이다.**
환경영향평가법 시행령 제38조에 따라 주민은 **공람이 끝난 뒤 7일 이내**까지 의견을 낼 수 있다.
실측(2026-08-02, 36건): 26건이 공람 종료 +7일, 4건이 +9~10일(+7일이 주말이라 밀림), 6건이 같은 날.
공람 종료일로 끊으면 **아직 의견을 낼 수 있는 사업이 화면에서 사라진다.**

1. **수집**: `build_data.py`는 목록에서 공람 종료 후 `OPINION_GRACE_DAYS`(14일)까지 담아 두고,
   저장 직전에 `의견제출 마감일` 기준으로 최종 판정한다.
   `opinionPeriod`("2026.07.30 ~ 2026.09.23")에서 뒤 날짜를 뽑아 `opinionEnd`로 저장한다.
   읽지 못하면 `null` — 그때는 공람 종료일을 대신 쓴다.
2. **화면**: `app.js`가 파일을 읽을 때 **날짜를 다시 판단한다.**
   파일을 만든 날과 보는 날이 다를 수 있기 때문이다.
   - 의견 낼 수 있음 → `PROJECTS` (목록·지도·통계에 표시)
   - 기한 지남/시작 전 → `CLOSED_PROJECTS` (표시하지 않음)
   - 공람은 끝났지만 의견은 받는 중 → `viewClosed = true`,
     카드에 `공람 종료 · 의견 접수 중` 배지를 붙인다
3. **D-n 은 의견제출 마감까지 남은 날이다** (공람 종료가 아니다).
   단, 설명회 목록의 D-n 은 **다음 설명회까지** 남은 날이라 뜻이 다르다.
4. **★ 환경영향분석(AI 해석)만은 `공람 기간` 안에서만 보여준다.** (`viewOpen`)
   **평가서 초안은 법적으로 공람 기간에만 열람할 수 있다.**
   공람이 끝난 뒤에도 그 내용을 옮긴 해석을 계속 띄우면
   **볼 수 없는 문서를 대신 보여주는 셈**이 되므로 절대 그러면 안 된다.
   - 의견 제출 기한(`open`)이 아니라 **공람 기간(`viewOpen`)** 으로 잠근다. 헷갈리기 쉬우니 주의.
   - 공람은 끝나고 의견만 받는 중이면 해석 대신
     "공람 기간이 끝나 평가서 초안을 볼 수 없으므로 해석 결과도 표시하지 않습니다.
     의견은 ○○까지 낼 수 있습니다" 를 낸다.
   - 사업명·기간·기관·설명회 같은 **사실 정보는 그대로** 보여준다. 막는 것은 해석뿐이다.
   - 해석이 나가는 곳은 `eiaSection()` 한 곳뿐이다. 다른 데서 `analysis` 를 직접 꺼내 쓰지 않는다.
   - **★ 자료에서도 막는다 (2026-08-16).** `build_data.py` 가 저장 직전에
     공람이 끝난 사업의 `analysis` 를 `null` 로 지운다(`view_closed()`).
     화면 잠금은 "안 그린다"일 뿐이고, `data/projects.json` 은 주소만 알면 누구나 읽는다.
     **여기 기준은 `opinionEnd`(의견제출 마감)가 아니라 `periodEnd`(공람 종료)다.**
     `--retry-analysis` 도 공람이 끝난 사업은 건너뛴다 (지운 것을 되살리면 안 되므로).
     남는 구멍: **자정~다음 수집 사이**. 그래서 한국 0시 10분 수집을 하나 더 걸어 두었다.

---

## AI 해석 원칙 — "환경영향분석"

AI는 **번역만** 한다. 분석·판단·보충을 하지 않는다.

- 항목을 고정했다 (`app.js`의 `EIA_FIELDS` = `build_data.py`의 `EIA_FIELDS`).
  **두 곳의 키가 반드시 같아야 한다.**
  현재 8개: 사업개요 / 대기·악취 / 소음·진동 / 물 / 동식물·생태 /
  토양·경관 / 폐기물 / 그 밖에
  (화면에 쓰는 **이름**은 `app.js`에서만 바꾼다. **키**를 바꾸면 `--full` 재수집이 필요하다)
- 요약문에 해당 내용이 없으면 그 항목은 `null`로 두고 화면에
  "요약문에 관련 내용이 없습니다"라고 표시한다. **억지로 채우지 않는다.**
- **★ '내용이 없다'와 '읽지 못했다'를 절대 섞지 않는다** (2026-08-16 추가).
  앞의 문구는 **요약문을 읽었는데** 그 항목이 없었다는 뜻이다. 못 읽은 것을 그렇게 쓰면
  **읽어 보고 없다고 한 것처럼** 들려 사실과 다르다.
  자료의 `summaryState` 로 가른다 — `ok` / `scanned`(사진으로 된 문서) / `none`(요약문 없음).
  판정은 **쪽당 글자 수**(`MIN_CHARS_PER_PAGE = 100`)로 한다.
  실측: 스캔본 쪽당 11자 vs 정상 문서 744~882자 — 70배 차이라 기준이 흔들릴 여지가 없다.
  **★ 애매하면 '읽었다'로 본다.** 멀쩡한 문서를 "못 읽었다"고 하는 쪽이 더 나쁜 실수다.
  **★ 스캔본은 AI 에 넘기지 않는다** — 목차와 쪽 번호만 든 글은 지어낼 위험만 있다.
  화면은 해석이 **통째로** 비었을 때만 이 문구를 쓴다. 일부라도 채워졌으면 기존 문구가 맞다.
  **공람 종료 안내가 언제나 먼저다.**
- 사업이 좋다/나쁘다 판단하거나 홍보하거나 "걱정할 필요 없다"는 식의 말을 넣지 않는다.
- 원문에 저감 대책이 있으면 "~하기로 되어 있다"처럼 사실로만 전달한다.
- 모델은 **`claude-haiku-4-5`**, JSON으로 받아 파싱한다. `max_tokens=6000`.
  - 2026-08-06에 소넷(`claude-sonnet-5`)으로 바꿔 봤다가 **비용 때문에 되돌렸다.**
    품질은 좋았다(시험에서 8개 항목 중 원문에 없는 4개를 정확히 `null` 처리).
    되돌린 이유는 값이 3배(사업 1건당 $0.02 → $0.05~0.08)인데,
    같은 날 수집이 계속 실패하면서 재시도로 비용이 더 나갈 위험이 있었기 때문이다.
  - **★ 다시 소넷으로 바꾸려면 `max_tokens`를 반드시 같이 올려야 한다 (16000).**
    소넷은 '생각하기'가 기본으로 켜져 있고, `max_tokens`가 **생각한 양 + 답변을 합쳐서**
    제한한다. 6000을 그대로 두면 생각하다 자리를 다 써서 JSON이 중간에 잘린다.
  - 응답에서 답변만 골라내는 `b.type == "text"` 필터는 **소넷일 때 반드시 필요하다**
    (생각 덩어리가 섞여 오기 때문). 지금도 그대로 두었으니 모델만 바꾸면 된다.
  - 모델을 바꿔도 `EIA_FIELDS` 키는 그대로라 `--full` 재수집은 **필요 없다**.

---

## 데이터 파이프라인

### EIASS 수집 구조 (직접 확인한 내용)

수집 대상은 **전략환경영향평가 + 환경영향평가**의 "평가서 초안 공람" 두 유형뿐이다.
(소규모환경영향평가·사후환경영향조사는 범위 밖)

| 단계 | 방법 |
|---|---|
| 목록 | `POST /searchApi/search.do` — 화면의 검색엔진 AJAX를 그대로 흉내낸다.<br>`collection=draft`, `urlString=&alias=1(전략)/2(환경)&orgnCd=&sido=`,<br>`viewName=eiass/user/partcptn/choan/choan{Sperss|Eiass}List_searchApi`<br>**함정 2개 (둘 다 겪음)**: ① 한 페이지에 `tbl01` 표가 **두 개** 있고 내용이 같다 → 사업 번호로 걸러내지 않으면 전부 두 번씩 처리된다. ② 목록이 **시작일 순으로 정렬돼 있지 않다** → "오래된 게 나오면 그만" 방식은 공람 중인 사업을 놓친다. 공람 중인 사업이 없는 페이지가 5쪽 이어질 때까지 넘긴다. |
| 상세 | `POST /partcptn/choan/choan{Sperss|Eiass}View.do`<br>전략: `BIZ_CD, BIZ_SEQ, CCIL_STEP1_CD_CK` / 환경: `BIZ_CD, BIZ_SEQ` |
| **협의 진행 중 건수** | 같은 `search.do`에 `collection=business`.<br>`viewName=/eiass/user/biz/base/info/searchList{Per|Eia}_searchApi`,<br>`urlString`에 `completeFl=진행`·`businessExquery`(진행구분 5개)·`perssGubn=S/M/E`.<br>건수는 응답 HTML의 `detailPage` 문구에서 뽑는다.<br>**사업을 하나도 받지 않고 건수만** 가져오므로 요청 3번·비용 0원.<br>2026-08-02 실측: 전략 115 / 환경 121 / 소규모 391 (화면 숫자와 일치) |
| 첨부파일 | 상세 HTML의 `generalView('FILE_SEQ','파일명')` 링크에서 뽑는다 |
| 파일 받기 | `GET /common/file/downloadFileByFileSeq.do?FILE_SEQ=..&SYSTEM_NAME=PERSS`<br>**로그인·세션 없이 그대로 내려온다** (확인함) |

> ### ★ EIASS 는 서울 중계를 거친다 (2026-08-09 도입)
>
> GitHub 서버에서 EIASS 접속이 안 되는 문제를 **EIASS 로 가는 요청만 서울에서 대신 걸어**
> 해결했다. 중계 함수 원본은 `tools/relay/index.ts` (Supabase Edge Function, 서울 리전).
>
> - **켜고 끄는 것은 환경변수 두 개뿐이다** — `EIASS_RELAY_URL`·`EIASS_RELAY_KEY`.
>   없으면 지금까지처럼 직접 접속한다. **한국 PC(`run_daily.bat`)는 직접 가는 게 빠르므로 안 쓴다.**
> - **중계를 타는 곳은 EIASS 와 VWorld 두 곳이다.** `requests` 가 주소별로 통로를 나눠 쓰는
>   성질을 이용해 그 두 주소일 때만 갈아끼운다(`_RelayAdapter`). **부르는 코드는 한 줄도 안 바뀌었다.**
>   - VWorld 는 2026-08-15에 추가했다. GitHub 러너에서 **아예 답을 하지 않아**
>     (502·연결 끊김, 틀린 키도 똑같음 = 키 문제 아님) 새 사업 좌표가 하나도 안 채워졌다.
>   - **Anthropic 은 중계를 타지 않는다.** 미국에서 잘 붙고, **API 키가 중계를 지나가지
>     않게 하는 편이 낫다.**
> - **★ `tools/relay/index.ts` 를 고쳤으면 Supabase 에 다시 배포해야 한다.**
>   대시보드는 버전 관리가 없어 저장소와 조용히 어긋난다. 허용 목록을 늘렸는데 배포를
>   안 하면 새 주소가 `403 not-allowed` 로 막힌다.
> - **★ `x-region: ap-northeast-2` 헤더가 없으면 미국에서 돈다.** 중계는 "부르는 사람과 가까운
>   곳"에서 도는 것이 기본이다. 이 헤더가 빠지면 조용히 예전 상태로 돌아간다.
> - **EIASS 는 중간 인증서를 안 보낸다.** 브라우저·파이썬은 알아서 보충하지만 Deno 는 안 해서
>   `UnknownIssuer` 로 끊긴다. 그래서 중계에 그 한 장을 넣어 두었다(검증을 끄는 게 아니다).
>   **EIASS 인증서는 2027-03-29 만료** — 갱신 때 인증기관이 바뀌면 중계가 멈춘다.
>   새로 받는 방법은 `tools/relay/index.ts` 맨 위 주석에 있다.
> - **HTTP 헤더에 한글을 넣지 말 것.** 헤더 값은 라틴 문자만 된다. 중계가 오류를 한글로
>   헤더에 담았다가 오류를 알리려다 또 죽었다(2026-08-09에 겪음). **까닭은 본문에, 헤더에는 짧은 아스키 코드만.**
> - ~~WARP 터널~~ 은 걷어냈다. 3회 연속 실패했고 그때 VWorld 까지 같이 죽었다. **되살리지 말 것.**

- **★ GitHub 서버(미국 Azure)에서는 EIASS 직접 접속이 거의 안 된다. 한국 PC에서는 100% 된다.**
  (2026-08-06 실측. 자세한 경위와 결정 대기 사항은 `docs/HANDOFF.md` §2·§4)
  - **"해외 IP 차단"은 사실이 아니다.** 전 세계 20개 노드 중 17곳 정상
    (미국 LA 0.49초, 댈러스 1.09초, 독일·프랑스·네덜란드·러시아·인도·베트남).
  - 그런데 **GitHub 러너에서만 유독 실패한다.** 6번 시도해 1번 성공(약 17%).
    실패 모양도 제각각이다 — DNS 조회 실패 / TCP 443 무응답 / ConnectTimeout.
  - **가장 중요한 단서**: 같은 러너·같은 순간에 **VWorld는 0.43초에 붙는데 EIASS만 14.7초 만에 시간 초과**했다.
    Azure↔한국 경로 문제가 아니라 **EIASS 쪽 문제**라는 뜻이다.
  - 한국 PC에서는 EIASS가 **매번 0.08초**에 붙는다. 여태 `run_daily.bat`이 실패한 적이 없다.
  → 재시도 통로(`SESSION`)를 두었다(4·8·16초 쉬며 최대 4번, `EIASS_RETRY`로 조절).
    다만 **재시도로는 안 덮였다** — 접속이 안 될 때는 몇 분 동안 계속 안 되기 때문이다.
  - ~~WARP 우회~~ 도 3회 연속 실패해 탈락했고, 그때 VWorld 까지 같이 죽었다. **되살리지 말 것.**
  - **★ 결론: 서울 중계로 풀었다 (2026-08-15).** 위 "EIASS 는 서울 중계를 거친다" 참고.
    **VWorld 도 같은 문제였다** — GitHub 러너에서 아예 답을 하지 않았다(502·연결 끊김).
    틀린 키로 불러도 똑같이 끊겼으니 **키 문제가 아니었다.** 그래서 둘 다 중계를 태운다.
- 사이트가 `frameset` 구조라 겉 URL로는 내용을 못 본다. 위 엔드포인트를 직접 부른다.
- 요약문 파일은 첨부 목록에서 이름에 "요약문"이 든 것을 찾는다.
- PDF 글자 추출은 `pdfplumber`를 쓴다 (한글 표 처리가 안정적).

### 위치 유형 — 면형 / 선형 구분

- **전략환경영향평가**는 사업위치에 면형/선형/점형이 표시된다. **환경영향평가는 표시가 없다**
  (확인함). 표시가 없으면 `locationTypes: []`로 두고 **추측하지 않는다.**
- **면형**: 주소 한 점만 쓴다. 노선을 찾지 않는다.
- **선형**: 구간 표에서 시점·종점·연장을 뽑는다(`segments`).
- **선형 + 하천 사업일 때만** 하천 노선을 자동으로 찾는다.
  판단은 `사업구분`·`근거법령`에 "하천"이 있는지로 한다 (`is_river_project()`).
  **도로·철도 사업에 이걸 적용하면 안 된다** — 요약문에 나오는 하천 이름은
  그냥 "지나가는 하천"이라 노선으로 그리면 틀린 그림이 된다.
  이런 사업은 사람이 직접 그린다 — **`tools/route_editor.html`** (아래 참고).

### 도로·철도 노선은 사람이 넣는다 (`route_editor.html`)

**아직 없는 길**이라 어떤 지도에도 도형이 없다. 그래서 세 가지 길을 둔다.

> **어디에 있나** — 저장소 **맨 위**다. `deploy.yml` 이 맨 위 `*.html` 만 올리므로
> `tools/` 로 옮기면 배포에서 빠져 관리자 화면의 단추가 404 가 된다.
> 관리자 화면(비밀번호) → **노선 그리기 도구 열기** 로 들어간다.
> VWorld 키는 배포 때 `assets/config.js` 에 심어지므로 배포 사이트에서는 따로 안 넣어도 된다.
>
> **★ 저장은 반드시 사람이 한다.** GitHub Pages 는 정적 파일을 올려두는 곳이라
> 브라우저에서 서버 파일을 못 쓴다. 쓰려면 GitHub 토큰이 필요한데 그것을 브라우저에 두면
> **저장소 쓰기 권한을 공개하는 셈**이라 절대 안 된다 (VWorld 키와 달리 도메인 제한도 안 걸린다).
> 흐름: 도구에서 그림 → `routes.json` 내려받기 → `data/routes.json` 에 덮어쓰기 → 커밋.

**① 도면에서 반자동으로 따기 (2026-08-16 추가, 가장 빠르다)**

도면은 그냥 그림이라 **어떤 픽셀이 위도 몇인지가 어디에도 없다**(좌표 격자가 없다).
그 한 단계만 사람이 메우면 나머지는 전부 자동이다.

| 단계 | 누가 |
|---|---|
| 기준점 3~4쌍 짝짓기 (도면 ↔ 지도) | **사람** |
| 선 색 픽셀 골라내기 → 가장 큰 덩어리 → 가장 긴 경로 | 자동 |
| 픽셀 → 위경도 변환 → routes.json | 자동 |

- 기준점 2개=닮음 / 3개=어파인 / **4개 이상=투영**(캡처가 기울어져도 보정)
- 계산은 위경도가 아니라 **웹 메르카토르 평면**에서 한다 (`map.project`, 줌 20).
  위경도를 바로 선형변환하면 위도 방향이 찌그러진다
- **남는 오차(RMS)를 미터로 보여 준다.** 기준점을 잘못 짚으면 여기서 바로 드러난다
- 선 따기는 **AI 가 아니라 색 걸러내기**다. 결과가 항상 같고, 공짜고, 틀리지 않는다
- **★ 덩어리 하나만 쓰면 안 된다.** 역 표시(원)·글자가 노선 위를 덮으면 **선이 거기서 동강 난다.**
  '가장 큰 덩어리'만 쓰면 나머지 절반이 통째로 빠지는데 **화면에는 그럴듯하게 보인다.**
  그래서 덩어리를 다 찾은 뒤 **끝점이 가까운 것끼리 이어 붙인다**(기본 80px).
  실측: 역 표시 두 개가 겹치면 틈이 **59px** 였다 — 40px 로 뒀더니 절반(50%)만 따왔다
- **뭉툭한 덩어리는 건너뛴다.** `106` 같은 노선번호 딱지가 선과 같은 빨강이다.
  `몸통길이 / 칸수 >= 0.12` 인 것만 선으로 본다 (딱지는 0.06 언저리)
- **무엇을 했는지 다 알린다** — 이은 곳 수와 틈 크기, 못 붙인 조각, 건너뛴 덩어리.
  조용히 반쪽만 따오는 것이 이 도구에서 가장 위험하다
- 실측(2026-08-16, 정답을 아는 시험 도면 800×600 / 실제 8.1km):
  **길이 8.14km(정답 8.117km) · 위치 오차 중앙값 18m · 최대 22m.**
  범례의 빨간 조각·회색 도로선·검은 글씨는 걸러졌다

> **★ AI 시각 인식으로 좌표를 만들지 않는다.** 비전 모델은 정확한 픽셀 좌표를 못 내면서
> **모른다고 하지 않고 그럴듯한 숫자를 낸다.** 지도에서는 그 오차가 수백 m~km 다.
> 정부 서비스 지도에 틀린 노선이 그려지는 것은 **노선이 없는 것보다 나쁘다.**
> (도면에 적힌 지명을 읽어 기준점 후보를 추천하는 정도는 안전하다 — 아직 안 함)

**② 지도에 직접 점 찍기** — 도면이 없거나 기준점을 못 찾을 때

**③ GeoJSON·KML 붙여넣기** — geojson.io · Google 내 지도 · QGIS 에서 그린 것을 가져온다
- **길이 검산이 이 도구의 핵심이다.** 그린 길이와 EIASS 에 적힌 연장(`segments[].length`)을
  견줘 ±15% 밖이면 경고한다. 손으로 그린 선이 맞는지 기계가 알 수 있는 **유일한 단서**다
- 결과는 `data/routes.json` 으로 내보낸다. 형식은 `{ "routes": { "사업번호": GeoJSON } }`
- **배포되지 않는다** — `deploy.yml` 이 `*.html`(맨 위)·`assets/`·`data/` 만 올린다
- VWorld 키는 화면에서 넣고 그 브라우저에만 저장된다. **파일에 적어 커밋하지 않는다**

### 노선 도형을 얻는 방법

도면(그림)에는 노선이 빨간 선으로 그려져 있지만 **좌표 격자가 없어서
픽셀을 위경도로 바꿀 기준점이 없다.** 그래서 도면을 읽어내는 방식은 쓰지 않는다.
(요약문 PDF에는 도면조차 없다. 다른 첨부의 경위도 표는 노선이 아니라
행정구역의 동서남북 끝 좌표다 — 확인함.)

대신:
1. 요약문 글자에서 하천 이름을 찾는다 (`extract_river_names()`)
2. VWorld 하천망도 `LT_C_WKMSTRM`에서 `riv_nm`으로 실제 도형을 받는다
3. Douglas-Peucker로 약 40m까지 단순화한다 (46,799점 → 599점, 13.9KB로 확인)
4. 사업 위치에서 60km 넘게 떨어진 동명이천은 버린다

한계: **하천 전체가 나온다.** 사업 구간만 잘라내지는 않는다. 화면에 그 사실을 표시한다.

### 노선 우선순위

`관리자가 직접 그린 것(data/routes.json + localStorage) > 수집한 하천 도형`

### 필요한 API 키

| 키 | 용도 | 신청한 활용 API |
|---|---|---|
| `VWORLD_KEY` | 지도 타일, 지오코딩, 행정구역, 하천 도형 | 2D 지도 / 지오코더 / 2D데이터 |
| `ANTHROPIC_API_KEY` | 환경영향분석 생성 | — (크레딧 충전 필요) |

`.env`에 넣는다. VWorld 데이터 API는 **`domain` 파라미터**(발급 때 등록한 서비스 URL)가
없으면 `INCORRECT_KEY`가 난다. `VWORLD_DOMAIN`으로 바꿀 수 있다(기본 `http://localhost:8000`).

**VWorld 키는 두 개를 나눠 쓴다:**

| 어디 | 서비스URL | 어디에 두나 |
|---|---|---|
| 내 PC 수집 | `http://localhost:8000` | `.env` |
| 배포 사이트 | `https://dongdong1226.github.io/alrimi/` | GitHub Secret `VWORLD_KEY` |

활용API는 **2D 지도 / WMTS·TMS / 2D데이터 / 지오코더** 네 개를 모두 체크해야 한다.

브라우저에서 부르는 VWorld API는 **전부 JSONP**다(`app.js`의 `jsonp()` 한 곳으로 모았다).
CORS를 피하려는 것이다. 지금 쓰는 것은 세 가지:

| 무엇 | 요청 | 쓰는 곳 |
|---|---|---|
| 주소 → 좌표 | `address / getCoord` | 동네를 고르면 우리 집 좌표 |
| 좌표 → 주소 | `address / getAddress` | **내 위치로 보기** (GPS 좌표 → 동네 이름) |
| 읍면동·시군구 경계 | `data / GetFeature` (`LT_C_ADEMD_INFO`, `LT_C_ADSIGG_INFO`) | 지도에 경계선, "이 동네 안 사업" 판정 |

경계 조회는 `domain` 없이 먼저 부르고(브라우저는 Referer로 확인된다), 거부되면
지금 열려 있는 주소를 `domain`으로 붙여 한 번 더 시도한다. 두 번 다 실패해도
경계만 안 그려질 뿐 나머지 화면은 그대로 동작한다.
**시·도 경계는 받지 않는다** — 도 하나가 수 MB라 화면이 느려지고, 그 범위는 주소 글자만으로 충분하다.
`attrFilter`의 `like`는 **앞에서부터** 맞춘다(`의창구`로는 안 찾아지고 `창원시`로는 찾아진다).
같은 이름의 다른 동네가 섞여 오므로 `full_nm`으로 한 번 더 걸러낸다.

> **VWorld 키는 브라우저에서 한 곳에만 둔다 — `wdn.settings.vworldKey`.**
> 알리미 화면(관리자 설정)과 `route_editor.html` 이 **같은 칸**을 쓴다.
> 따로 두었더니 "도구에는 지도가 나오는데 알리미 화면에는 안 나오는" 일이 생겼다(2026-08-16).
> 로컬에서 지도가 안 나오면 **거의 항상 이 칸이 비었거나 지워진 것**이다
> (`assets/config.js` 는 로컬에서 비어 있는 게 정상이라 기댈 곳이 이 칸뿐이다).

> **함정 — 지도 타일을 시험할 때 좌표를 아무 숫자나 넣지 말 것.**
> 한국 밖 타일을 부르면 `FileNotFound / 서비스 제공영역이 아닙니다` 가 온다.
> 이것을 **키가 거부된 것으로 오해하기 쉽다**(2026-08-16에 그럴 뻔했다).
> 위경도에서 타일 번호를 제대로 계산해서 부를 것. 줌 5 이하는 `InvalidParameterValue / tilematrix`
> 가 오는데, 그건 진짜로 VWorld 가 줌 6~19 만 지원하기 때문이다.

> **함정 — 키 앞뒤 공백.** 공백이 한 칸만 붙어도 VWorld가 `INVALID_KEY`로 거부한다.
> 그런데 **지도 타일(WMTS)은 멀쩡히 나오고 지오코더만 막힌다** (타일은 키가 URL 경로에,
> 지오코더는 질의 파라미터에 들어가기 때문). 원인을 찾기 아주 어렵다 — 2026-08-01에 겪었다.
> **키가 거부되면 값을 의심하기 전에 길이부터 세어 본다.**

### 실행

```bash
python tools/build_data.py            # 전체 (이미 받아 둔 사업은 재사용)
python tools/build_data.py --limit 5  # 유형별 5건만 (테스트)
python tools/build_data.py --full     # 캐시 무시하고 전부 다시 받기
python tools/build_regions.py         # 행정구역 목록 (한 번만)
python tools/build_boundaries.py      # 시·도 경계 도형 (한 번만)
```

옵션: `--skip-geocode`, `--skip-summary`, `--skip-route`, `--max-pages`, `--delay`, `--full`

> **★ 되받기(backfill)가 없는 값은 조용히 영영 빈다.**
> 증분 수집은 이미 받아 둔 사업을 통째로 건너뛴다. 그래서 **처음 수집할 때 못 받은 값은
> 다시 시도하는 자리를 따로 만들어 두지 않으면 영원히 빈 채로 남는다.**
> 2026-08-16에 겪었다 — VWorld 가 죽어 있던 날 수집된 하천 사업 2건이 노선 없이 굳어 있었다.
> 좌표는 되받기가 있어서 8/15에 회복됐는데(30→39), **노선은 그 코드가 없어 아무도 안 받았다.**
> `refresh_cached()` 가 되받는 것: **좌표 · 노선 도형 · 아직 안 정해진 상세값**.
> 되받지 않는 것: **AI 해석**(비용 때문. `--retry-analysis` 로 따로 한다).

**증분 수집**: 기본 동작은 `data/projects.json`을 먼저 읽어 **이미 받아 둔 사업은 건너뛰는** 것이다.
돈과 시간이 드는 건 목록 조회가 아니라 사업 1건당 상세+PDF+AI 해석이다.
공람이 끝난 사업은 마지막 필터에서 자동으로 빠진다.
**`EIA_FIELDS`를 바꿨으면 반드시 `--full`로 돌려야 한다** (옛 키가 그대로 남는다).

로컬에서 화면을 볼 때는 **반드시 서버로 열어야 한다** (`file://`은 json을 못 읽음):

```bash
python -m http.server 8000
```

### ★ 화면을 그림으로 찍어 보기 (`tools/shot.py`)

**치수를 재는 것만으로는 "답답한지 넉넉한지"를 알 수 없다. 눈으로 봐야 한다.**
브라우저 창을 안 띄우면 캡처가 막히므로, 창 없이 찍는 길을 둔다.

```bash
python tools/shot.py                                          # 휴대폰 첫 화면
python tools/shot.py --click "#setForm button[type=submit]"   # 눌러 본 뒤 찍기
python tools/shot.py --scroll bottom                          # 아래까지 내려서
python tools/shot.py --pc                                     # 넓은 화면
```

- **playwright 가 있으면 그것을 쓴다** — 진짜 휴대폰 흉내가 된다
  (viewport meta 를 따르고 배율 3배, 손가락 기기로 인식 → `m.html` 판정이 맞게 돈다)
- 없으면 크롬/엣지를 headless 로 부른다(설치 불필요). 단 **데스크톱 취급**이라
  viewport meta 를 무시해 **좁은 화면이 잘린 것처럼 찍힌다** — 2026-08-16 에 그걸 보고
  없는 버그를 찾을 뻔했다. 휴대폰 화면을 볼 때는 playwright 쪽을 쓸 것
- 가로로 삐져나간 것이 있으면 찍은 뒤 `⚠` 로 알려 준다 (그림만 보면 잘린 줄 모른다)
- 결과는 `tools/_shots/` 에 쌓이고 `.gitignore` 에 넣어 두었다

---

## ★ 화면 파일이 두 개다 (2026-08-06 결정)

**휴대폰은 데스크톱을 줄인 화면이 아니라 따로 설계한 화면을 쓴다.**

| | 파일 | 언제 열리나 |
|---|---|---|
| 데스크톱·태블릿 | `index.html` + `app.css` | 기본 |
| 휴대폰 | `m.html` + `mobile.css` | **짧은 쪽 변 ≤600px** 이고 손가락 기기(`pointer:coarse`)일 때 자동 |

**★ 판정은 폭이 아니라 `짧은 쪽 변`으로 한다.**
처음에 `폭 ≤820px` 으로 만들었다가 바로 고쳤다 — 요즘 폰은 **가로로 돌리면 폭이
844~932px**(아이폰14 844 / S23U 915 / 14 ProMax 932)이라, 폭으로 재면
**돌릴 때마다 휴대폰 화면 ↔ 데스크톱 화면이 왔다 갔다 한다.**
짧은 변은 돌려도 그대로다 — 폰 320~430, 태블릿 744~834 로 깔끔히 갈린다.

```js
var shortSide = Math.min(innerWidth, innerHeight);
var phone = shortSide <= 600 && matchMedia("(pointer:coarse)").matches;
```

`pointer:coarse` 를 같이 보는 이유는 **데스크톱 창을 좁게 줄인 것과 진짜 휴대폰이 다르기 때문**이다.
(실측: 폰은 세로·가로 모두 휴대폰 화면 / 아이패드·폴드 편 상태·PC 좁은 창은 모두 데스크톱)

> **★ 끄는 스위치.** `index.html` 맨 위 스크립트의 `var MOBILE_ON = true;` 를 `false` 로 바꾸고
> 푸시하면 휴대폰도 `index.html` 을 쓴다. **파일을 지우거나 되돌리기(revert)를 할 필요가 없다.**
> 휴대폰 화면이 감당이 안 되면 여기만 만진다. 다시 켜려면 `true` 로 되돌린다.

`index.html` 맨 위 스크립트가 판단해 `m.html` 로 보낸다.
**폭만 보지 않는다** — 데스크톱 창을 좁게 줄인 것과 진짜 휴대폰은 다르다.
`?pc=1` 로 들어오면 보내지 않고 그 선택을 기억한다(`sessionStorage`). 두 화면에 서로 가는 링크가 있다.

> 이 결정은 **원래 하지 않기로 했던 것을 뒤집은 것이다.**
> 되돌린 이유: 폭 기준 미디어쿼리로 데스크톱 배치를 줄여 쓰다 보니 휴대폰에서 계속 깨졌다
> (가로로 든 화면에서 목록 높이 0px, 세로에서 37px). 덧대는 방식으로는 새는 것을 못 막았다.

### 그래서 반드시 지킬 것 — 두 벌이 되면 안 되는 것

**복제하면 두 화면이 서로 다른 답을 내놓는다.** 아래는 한 곳에만 둔다.

| 무엇 | 어디에 | 규칙 |
|---|---|---|
| 판단·동작 전부 | `assets/app.js` | 기간 판정, AI 해석 잠금, 범위 필터, 지도 로직. **`mobile.js` 에 절대 옮기지 않는다** |
| 이용안내·문의 본문 | `assets/texts.js` | 법 조문 인용이라 특히 위험하다. `<div data-text="guide">` 자리에 채워진다 |
| 색·글꼴·모서리 | `assets/tokens.css` | `mobile.css` 도 변수만 쓴다. 새 값을 만들지 않는다 |
| 컴포넌트 스타일 | `assets/app.css` | `mobile.css` 는 **배치만** 덮어쓴다 |

`assets/mobile.js` 에는 탭 막대·시트 여닫기처럼 **배치에만 관한 것**을 둔다.

### app.js 는 없는 칸을 만나도 죽지 않는다

두 화면은 칸 구성이 다르다 (휴대폰에는 관리자 설정·노선 그리기가 없다).
그런데 없는 칸을 만지면 자바스크립트가 그 자리에서 멈춰 **화면이 통째로 빈다.**
그래서 `$()` 가 없는 칸을 찾으면 아무 일도 하지 않는 `NO_EL` 을 돌려준다.

- 무엇이 없었는지는 개발자도구 콘솔에 `[알리미] 이 화면에는 없는 칸:` 으로 한 번씩 찍힌다
- **화면에 값이 안 나오면 먼저 이 로그를 본다.** 오타로 id 가 틀려도 조용히 넘어가기 때문이다
- Leaflet 은 id 로 요소를 직접 찾으므로 `#gisMap`·`#miniMapCanvas` 는 `getElementById` 로 따로 확인한다

### 휴대폰 화면 구성 (m.html)

- **아래쪽 탭 막대** — 홈 / 지도 / 설명회 / **관심목록** / 내 동네. 엄지로 닿는 자리.
  **'안내'는 뺐다** — 자주 볼 것이 아니라 머리말·푸터로 충분하고, 담아 둔 사업은
  마감을 지켜보려고 담은 것이라 자주 들어와야 한다 (2026-08-16 교체).
  **★ 막대가 안 보인다는 말을 들었다.** 흰 배경 + 옅은 회색 글자 + 1px 선이라
  본문과 안 갈렸다. 세 가지를 함께 고쳐야 했다 — 위쪽 **그림자**,
  글자 `--ink-4`→`--ink-2`(대비 2.6→9.3), 고른 탭 아이콘 뒤 **알약 배경**
- **지도** — 처음 들어오면 **지도만** 보여준다(닫힘). 화면 아래 가운데 "목록 보기" 배지를
  눌러야 목록 시트가 올라온다(닫힘 → 작게 → 반 → 크게 4단계, 손잡이로 순환).
  **★ 손잡이만으로는 지도로 못 돌아간다.** 손잡이는 한 단계씩 도는 물건이라
  '작게'에서 지도를 다시 보려면 세 번을 눌러야 했고, 상세를 열면 나갈 길이 그것뿐이었다.
  그래서 손잡이 줄 오른쪽에 **`지도 보기` 단추**를 따로 뒀다(`#btnSheetClose`) —
  한 번에 닫힘으로 가고, 상세를 보고 있었으면 목록으로도 되돌린다.
  사업을 고르면(마커든 목록이든) 자동으로 끝까지 올라간다. 지도 화면에 새로 들어올
  때마다(다른 화면에서 돌아오는 것 포함) 닫힘으로 되돌아간다 — "지도만 켜면 지도만"
- **모달** — 아래에서 올라오는 전체 화면
- 첫 화면은 `지금 내 위치로 보기` 를 맨 위 큰 단추로 둔다 (휴대폰에서 가장 빠른 길)

---

## 화면 구조

- **화면 1 · 동네 설정** — 전국 시도/시군구/읍면동 선택 + `지금 내 위치로 보기`.
  **세 칸을 다 고를 필요는 없다** (아래 "동네 고르기" 참고).
  **기본값은 `전국`이다** — 처음 온 사람은 자기 동네를 아직 안 정했으므로 전국을 먼저 보여준다
- **화면 2 · 우리 동네 홈** — 대시보드 숫자, 사업 카드 목록, 의견 낼 수 있는 공람 목록,
  미니 지도(동네 경계 + 반경 원).
  두 목록은 **페이지로 나눠** 보여준다 (`PAGER` — 카드 9개, 공람 10개가 기본).
  필터·검색·범위가 바뀌면 `resetPages()`로 1쪽으로 돌아간다
- **화면 3 · 관리자** — VWorld 키, 반경, 문의처 등. 비밀번호는 보안장치가 아니다.
  맨 위에 **날짜별로 새로 들어온 사업**(`renderAdminFeed()`)이 있다 — 담당자가 가장 자주 보는 칸이다.
  세는 기준은 **`firstSeen`(우리가 처음 본 날)** 이고, 없으면 공람 시작일로 대신하되
  **그 사실을 화면에 밝힌다**(조용히 섞으면 "이 날 이만큼 들어왔구나"를 잘못 읽는다).
  **★ 들어가는 길이 세 가지다** — `?admin=1`(어느 화면에서든) · 첫 화면 아래 단추 · 홈 푸터 기관명.
  예전에는 첫 화면 단추 하나뿐이라 **동네를 고르면 못 들어갔다**(단추가 0×0 이 된다).
  `?admin=1` 처리는 **`app.js` 맨 끝**에 둔다 — 앞에 두면 시작 코드가 첫 화면으로 되돌린다
- **화면 4 · 지도** — 전체화면. 왼쪽 패널(주소 변경 + 목록 ↔ 상세) + 지도
- **화면 5 · 내가 담은 사업** — 주민이 카드의 별표(☆)로 담아 둔 사업만 모아 본다.
  데스크톱은 헤더 메뉴, **휴대폰은 머리말 오른쪽 별표 단추**로 들어간다
  (본문 스크롤이 아니라 **화면 전환**이다). 아래 "담은 사업" 참고

### ★ 휴대폰 대응 — 폭만 보면 안 된다

아래는 **휴대폰 화면을 따로 만들기 전(`index.html` 하나로 버티던 때)에 겪은 것들**이다.
지금은 휴대폰이 `m.html` 로 가지만, `index.html` 도 태블릿·좁은 창에서 같은 함정을 밟을 수 있어 남긴다.
**그리고 이 교훈 자체가 화면을 나눈 이유다.**

| 무엇 | 왜 깨졌나 | 어떻게 막았나 |
|---|---|---|

| 무엇 | 왜 깨졌나 | 어떻게 막았나 |
|---|---|---|
| **가로로 든 휴대폰** (812×375) | 폭이 넓어 "데스크톱"으로 판정 → 지도 화면을 위아래로 나눔 → 위쪽 띠 99px + 지도 173px 쓰고 나면 왼쪽 패널이 104px 인데 주소칸 하나가 117px. **사업 목록 높이가 0** 이었다 | `@media (max-height:560px) and (max-width:1024px)` — 좌우 배치로 되돌리고 띠를 납작하게. 목록 0 → 212px |
| **세로 휴대폰** (375×812) | 위쪽 띠가 두 줄(108px)이 되고 주소칸에 `내 위치`가 붙으면서 **목록이 37px** 로 눌림 | 띠를 **반드시 한 줄**로(돌아가기 글자 일부를 감춤), 지도 62dvh → 52dvh, 왼쪽 패널을 통째로 스크롤 + 주소칸 sticky. 37 → 210px |
| **주소창에 가려 아래가 안 보임** | 휴대폰 브라우저는 주소창·도구막대 높이까지 `100vh` 에 넣는다 | `height:100vh; height:100dvh` (모르는 브라우저용으로 vh 를 먼저) |
| 띄어쓰기 없는 긴 사업명 | `word-break:keep-all` 이 한글 낱말을 지키느라 줄을 못 바꿔 상자 밖으로 삐져나감 | `overflow-wrap:anywhere` 를 같이 건다 |

> **화면을 손보면 폭뿐 아니라 높이도 재 볼 것.**
> 특히 `.gis-side-body`(지도 화면 목록)의 높이가 **140px 아래로 내려가면 안 된다.**
> 위쪽 띠나 주소칸에 무언가를 더할 때마다 그만큼 목록이 줄어든다.
> `m.html` 의 시트도 마찬가지다 — 열린 단계(작게/반/크게) 모두에서 목록이 남아 있어야 한다
> (실측: 세로 258/426/752px). **닫힘 단계는 예외다** — 그때는 목록이 없는 것이 의도다.

> **브라우저가 색을 뒤집는 것도 '깨짐'이다.** 휴대폰이 다크 모드면 삼성 인터넷·크롬이
> 페이지 색을 제멋대로 반전시키는데, 요소마다 판단이 달라 어떤 칸만 검게 된다.
> 두 화면 모두 `<meta name="color-scheme" content="light">` 와 `:root{color-scheme:light}` 로 막았다.
> **이 두 줄을 지우면 안 된다.**

### ★ 배경 사진을 바꾸면 글자 대비를 다시 재야 한다

히어로(첫 화면·홈 위쪽)는 **사진 위에 흰 글씨**를 얹는다. 그래서 사진을 바꾸면
덮개(`.hero-bg::after` 의 gradient, 휴대폰은 `.m-onb`·`.m-lead` 의 첫 배경 레이어)를
**반드시 같이 조정해야 한다.** 장식이 아니라 글자를 읽히게 하는 장치다.

2026-08-08에 밝은 초록 사진으로 바꿨을 때 실측:

| | 히어로 전체 최악 대비비 |
|---|---|
| 옛 덮개 그대로 | **1.87** ← 기준 미달 |
| 덮개 조정 후 | **4.77** ✓ (본문 기준 4.5) |

**핵심은 불투명도가 아니라 덮개 '색'이다.** 옛 덮개의 오른쪽 끝이 밝은 파랑
(`rgba(90,140,190)`)이라 불투명도를 70%까지 올려도 2.64밖에 안 나왔다.
어두운 `--p90`(#0c1e59) 계열로 바꾸니 56%만으로 4.89가 됐다.

- 재는 방법: 사진을 캔버스에 `center/cover` 로 그려 놓고, 덮개를 합성한 뒤
  실제 글자 위치에서 흰색 대비비를 구한다. 기준은 **큰 글씨 3:1 / 본문 4.5:1**
- 사진을 못 받아도 `--p90` 바탕색이 깔려 글자는 읽힌다 (그 색을 지우면 안 된다)
- 휴대폰은 같은 사진의 **작은 판(`w=900`, 184KB)** 을 받는다. 1920 판은 823KB다

지도 화면 규칙 (`index.html` 기준. 휴대폰은 `m.html` 의 시트 방식을 쓴다):
- 왼쪽 패널 한 곳에서 목록과 상세를 번갈아 보여준다 (오른쪽 패널 없음)
- **좁은 화면(≤560px)에서는 지도와 사업 내용을 같이 두지 않는다.**
  들어오면 지도(62vh), 사업을 고르면 지도·주소칸이 사라지고 내용만.
  되돌아가는 `지도 보기` 단추가 있다. 표시는 `#scr-map.on-detail` 클래스,
  거기에 반응하는 CSS는 휴대폰 media query 안에만 둔다 (넓은 화면은 그대로)
- 목록은 "우리 동네" / "그 밖의 지역"으로 나눈다.
  나누는 기준은 지금 고른 범위를 따른다(`localTest()`)
- 상세의 긴 내용은 눌러서 펼치는 묶음으로 나눈다:
  공람·설명회 안내 / 구간(선형만) / 환경영향분석
- **노선 그리기 단추는 지도 화면에 없다.** 주민에게 보일 기능이 아니다.
  관리자 화면(비밀번호 해제) → "지도에서 노선 그리기" → 지도로 넘어가
  목록에서 사업을 고르면 그때 그리기가 시작된다(`routeArmed`).
  지도를 눌러 점을 잇고 저장하면 localStorage에 남고,
  "JSON 복사"로 받아 `data/routes.json`에 넣으면 모두에게 보인다.
  지도 화면을 나가면 그리기 모드도 끝난다

### 마감일을 캘린더에 넣기 — 2026-08-16 추가

**서버 없이 '알림'에 가장 가까운 길이다.** 넣어 두면 **폰 캘린더가 대신 알려 준다** —
우리는 아무것도 안 보내고, 계정도 권한도 필요 없다.
(웹 푸시는 보낼 서버가 필요하고, 아이폰은 홈 화면 추가까지 해야 한다.
 그러면 서버 0원·개인정보 0건이 깨진다. 그래서 안 쓴다.)

**★ 두 가지 길을 반드시 함께 준다. 기기마다 편한 쪽이 다르다.**

| | 어디에 좋나 | 왜 |
|---|---|---|
| `.ics` 파일 받기 | **아이폰·PC** | 받은 파일을 열면 바로 들어간다. **미리 알림도 넣을 수 있다** |
| 구글 캘린더 링크 | **갤럭시(안드로이드)** | 파일을 받아 '어떤 앱으로 열까' 고를 필요 없이 일정 추가 화면이 바로 뜬다 |

- 넣는 날짜는 **의견 제출 마감일**(`opinionEnd`). 없으면 공람 종료일을 대신 쓴다
- 미리 알림은 **사흘 전·당일 아침 9시** 두 번 (`VALARM`, 온종일 일정 기준 `-P3DT15H`/`-PT15H`)
- **★ `.ics` 한 줄은 75바이트를 넘으면 안 된다.** 한글은 한 글자가 3바이트라
  사업명이 대부분 넘는다. 안 접으면 까다로운 캘린더 앱이 **파일을 통째로 못 읽는다.**
  `icsFold()` 가 글자 단위로 세어 접는다(한글이 중간에 잘리면 안 되므로).
  줄바꿈은 **CRLF** 여야 한다
- 온종일 일정은 **끝날짜를 하루 뒤로** 적는 규칙이다 (`DTEND` = 마감일 +1)
- 자리: 사업 상세(한 건) · 담은 사업 화면(한 번에). 두 화면이 같은 칸(`#savedCal`)을 쓴다

### 지역 캘린더 구독 — 2026-08-16 추가

위의 '캘린더에 넣기'는 **한 번 받으면 끝**이다. 구독은 다르다 —
폰 캘린더가 주소를 기억하고 주기적으로 다시 받아 오므로,
그 지역에 **새 사업이 뜨면 일정이 저절로 들어오고 알림도 온다.**
**이것이 서버 없이 '알림'에 가장 가까운 답이다.**

- `build_data.py` 의 `build_calendars()` 가 **5,339개**를 만든다
  (전국1 + 시도16 + 시군구255 + **읍면동5,067**). 실제 내용은 **1.99MB**, 평균 373바이트.
  일정이 실제로 든 것은 91개뿐이고 나머지는 빈 달력이다 (아래 참고)
- **★ 고른 단계가 곧 받는 범위다** (2026-08-16 확장). 시·군·구까지만 고르면 그 시·군·구 전체가,
  읍·면·동까지 고르면 그 동네 것만 온다. 그래서 화면도 세 칸을 다 보여 준다.
  **★ 파일 이름 규칙이 두 곳에 있다 — `app.js` 의 `calFeedName()` 과 `build_data.py` 의 `cal_slug()`.**
  어긋나면 화면이 없는 파일을 가리켜 **구독이 조용히 실패한다**(캘린더 앱은 아무 말도 안 한다).
  고칠 때는 반드시 둘 다 고치고, 이름을 맞춰 보는 시험을 돌릴 것
- **★ 사업이 0건인 지역도 파일을 만든다.** 구독해 둔 주소가 사라지면
  캘린더 앱이 오류를 내거나 구독을 버린다. 빈 파일은 335바이트뿐이다
- **★ `DTSTAMP` 를 '오늘'로 하면 안 된다.** 내용이 안 바뀌어도 파일이 매일 달라져
  **5,339개가 날마다 커밋된다.** 사업의 공람 시작일로 고정한다.
  내용이 같으면 아예 다시 쓰지 않는다
- **★ `collect.yml` 이 `git add -A data/cal` 을 해야 한다.**
  안 그러면 주민이 구독한 주소의 내용이 영영 안 바뀐다 (실제로 빠져 있었다)
- 판정은 **주소 글자**로만 한다. 화면(`inHood()`)은 노선이 경계를 지나는 것까지 보지만
  여기서는 안 한다 — 구독은 '대충 우리 동네'면 충분하고 정확한 판정은 화면이 한다
- **★ 알림 받을 지역은 보고 있는 동네와 따로 고른다** (`wdn.calRegion`, 2026-08-16).
  읍·면·동까지 좁혀 보는 사람도 **알림은 시·군·구 전체로 넓혀 받고 싶을 수 있다**
  (옆 동네 사업도 우리 생활권이다). 처음 값만 지금 동네를 따라가고, 그 뒤로는 따로 간다.
  **시·도 / 시·군·구 / 읍·면·동 세 칸을 다 고를 수 있다.**
  위 칸을 바꾸면 아래 칸은 '전체'로 되돌린다 — 다른 시·도의 시·군·구나
  다른 시·군·구의 읍·면·동이 남으면 **없는 파일을 가리키게 된다**
- **★ PC 에는 '구글 캘린더로 구독' 하나만 둔다** (2026-08-16).
  아이폰 구독(`webcal:`)은 PC 에서 열 앱이 없고, 주소 복사는 구글 단추가 하는 일을 손으로 하는 것이라
  셋을 나란히 두면 어느 것을 눌러야 할지 헷갈린다. 휴대폰은 기기마다 편한 길이 달라 셋을 다 둔다.
  가르는 기준은 `document.body.classList.contains("m")` 이다 (`m.html` 의 `<body class="m">`)
- **★ '담은 사업 마감일 캘린더 파일 받기'도 휴대폰에만 둔다** (2026-08-16).
  알림을 주는 것은 **폰 캘린더**라, 파일을 PC 로 받아 봐야 정작 알림이 울려야 할 기기에는 안 들어간다
- 자리는 **관심목록(담은 사업) 화면**이다. 구독이 위, 담은 사업 파일 받기가 아래 —
  '한 번 받고 끝'보다 '앞으로 계속 받는' 쪽이 더 값어치 있다
- **홈 사업 목록 아래에는 한 줄짜리 링크(`.cal-link`)만 둔다.** 관심목록까지 안 들어가는
  사람이 있어서다. 설정 칸을 통째로 홈에 두면 화면이 길어지므로 데려가기만 한다
- 아이폰은 `webcal://`, 갤럭시·PC 는 구글 캘린더 `render?cid=`, 그리고 주소 복사

### 새로 올라온 사업 알림 — 2026-08-16 추가

주민이 다시 들어왔을 때 **그 사이에 새로 뜬 사업**을 놓치지 않게 한다.
**서버도 계정도 알림 권한도 쓰지 않는다** — 마지막으로 본 날짜(`wdn.lastSeen`)만
그 브라우저에 적어 두고, 그보다 늦게 **우리 화면에 나타난** 사업을 새것으로 본다.

- **★ 기준은 `firstSeen`(우리가 처음 본 날)이지 `periodStart`(공람 시작일)가 아니다.**
  공람 시작일은 **EIASS 쪽 사정**이라 우리 화면에 나타난 날과 다르다. 어긋나는 경우가 둘 있다 —
  ① EIASS 가 공람 시작 뒤에 목록에 늦게 올린다(실측: 공람 8/3 시작인데 우리가 처음 본 건 8/14)
  ② **우리 수집이 하루라도 실패한다**(실제로 7일 구멍이 있었다).
  늦게 나타난 사업일수록 남은 기간이 짧아서, 공람 시작일로 재면
  **가장 급한 사업이 가장 조용히** 들어온다.
- `firstSeen` 은 `build_data.py` 의 `stamp_first_seen()` 이 **저장 직전 한 곳에서만** 찍는다.
  **★ 한 번 적힌 날짜는 절대 덮지 않는다** — 매번 오늘로 덮으면 모든 사업이 날마다 새것이 된다.
  **★ `existing` 이 아니라 파일을 다시 읽는다** — `--full` 은 `existing` 을 비우므로,
  거기에 기대면 `--full` 한 번에 값이 전부 날아가 **다시 온 주민에게 전부 NEW** 가 된다.
- 값이 없으면 공람 시작일로 대신한다(`p.firstSeen || p.periodStart`).
  이 기능을 넣기 전에 수집된 사업이 그렇다 — 그때는 예전과 똑같이 동작한다.
  **없다고 새것으로 몰지 않는다.**
- **첫 방문에는 아무것도 새것으로 표시하지 않는다.** 41건이 전부 NEW 면 뜻이 없다.
  그때는 오늘 날짜만 적어 둔다
- 건수는 **지금 보고 있는 범위**를 따른다 (전국 7건 → 당정동 안 1건)
- 알림 줄(`#newBanner`)과 카드의 `NEW` 딱지, 두 곳에 나온다. 두 화면이 같은 칸을 쓴다
- `✕`(닫기)를 누르면 확인한 것으로 치고 날짜를 오늘로 옮긴다.
  **`보기`만 눌러서는 안 지운다** — 목록을 보다 말 수도 있기 때문이다
- 남은 한계: **2026-08-16 이전에 수집된 사업은 여전히 공람 시작일 기준**이다.
  우리가 언제 처음 봤는지는 git 기록에만 있어 되받을 수 없다. 새로 들어오는 것부터 정확해진다

### 담은 사업 (화면 5) — 2026-08-08 추가

주민이 사업 카드 오른쪽 위의 **별표**를 눌러 담아 두면 화면 5에 모인다.
"공람 마감을 놓치지 않게 지켜보는 것"이 목적이라 정렬은 **마감 임박 순**이다.

- 저장하는 것은 **사업 번호뿐**이다 (`localStorage`의 `wdn.saved`).
  내용은 매번 `data/projects.json`에서 다시 읽는다 —
  내용까지 저장하면 **기간이 연장돼도 옛 날짜를 계속 보여주게 된다.**
- **계정이 없으므로 이 목록은 그 브라우저에만 남는다.** 폰에서 담은 것이 PC에 안 나온다.
  고칠 수 있는 문제가 아니다(회원가입·서버가 필요하다). 그래서 **화면에 그 사실을 반드시 밝힌다** —
  안 알려주면 주민이 "사라졌다"고 생각한다. (`#savedNote`)
- **★ 기한이 지난 사업도 지우지 않는다.** 일부러 담아 둔 것이 말없이 사라지면
  담은 사람은 고장으로 여긴다. 아래쪽에 `.proj--closed`(점선·흐림)로 따로 모으고
  "기한이 지난 사업"이라고 밝힌다. 한꺼번에 비우는 단추를 준다.
  - 그래서 `findProject()`는 `PROJECTS`뿐 아니라 **`CLOSED_PROJECTS`까지** 찾는다.
    `openDetail()`과 `data-eiass` 처리가 이걸 쓴다.
  - **단 기간 규칙에는 예외가 없다.** 마감된 사업은 의견 제출 단추가 안 나오고(`dday`가 null),
    **환경영향분석은 `eiaSection()`이 공람 기간(`viewOpen`)으로 그대로 잠근다.**
    담아 뒀다고 해서 볼 수 없는 평가서 초안의 해석을 보여주면 안 된다.
  - EIASS 목록에서 아예 내려간 사업은 표시할 내용이 없으므로 건수만 알린다
- **카드 HTML은 `projCardHtml()` 한 곳에서만 만든다.** 홈 목록과 이 화면이 같이 쓴다.
  복사해 두면 한쪽만 고쳐져 두 화면이 서로 다른 것을 보여준다
- 담기 단추가 있는 곳: 사업 카드(별표) · 상세 모달 · 지도 상세. 처리는
  `bindProjectActions()`의 `[data-save]` 한 곳뿐이다
- **입구가 두 곳인 이유**: 헤더 메뉴는 `≤980px`에서 통째로 숨는다(기존 규칙).
  그러면 갈 길이 없어지므로 사업 목록 줄에도 같은 단추를 뒀다(`#btnSavedGo`)
- **휴대폰(`m.html`)에도 있다 (2026-08-16 추가).** 입구는 **머리말 오른쪽 별표 단추**다 —
  아래 탭이 이미 5개로 꽉 찼고(애플 권장도 5개), 즐겨찾기를 머리말에 두는 것이 앱들의 흔한 방식이다.
  **화면 안의 칸 이름(id)을 `index.html` 과 똑같이 맞췄다** — 그래야 `renderSaved()` 가
  두 화면을 그대로 함께 쓴다. **여기에 따로 코드를 만들면 두 화면이 갈라진다**

### 동네 고르기 — 세 칸을 다 채우지 않아도 된다

시·도 칸 맨 위에 **`전국`**, 시·군·구·읍·면·동 칸 맨 위에 **`전체`** 가 있다.
`app.js`의 `ALL_SIDO` / `ANY` 가 그 표시값이고, `isNation()` `hoodSgg()` `hoodDong()`
세 도우미로만 읽는다. **두 값을 문자열로 직접 비교하지 않는다.**

- 위 칸을 안 고르면 아래 칸은 잠근다 (시군구 없이 읍면동만 고르면 같은 이름이 전국에 여럿이라 뜻이 없다)
- 세종처럼 시·군·구 단계가 없는 곳은 시·도 → 읍·면·동으로 바로 이어진다
- `지금 내 위치로 보기` — GPS 좌표를 VWorld 역지오코딩으로 동네 이름으로 바꿔 넣는다.
  이때 거리 기준점은 동 중심이 아니라 **실제 서 있는 자리**다(`HOME.gps`).
  주소를 못 찾아도 좌표는 남아 반경 기준으로는 쓸 수 있다.
  **https 또는 localhost 에서만 동작한다** — 브라우저가 막는다

### 보는 범위 세 가지 (`homeScope` / `mapScope`)

| 값 | 뜻 | 판정 |
|---|---|---|
| `region` | 고른 행정구역 **안에 실제로 있는** 사업만 | `inHood()` |
| `near` | 우리 집 반경 N km (경계를 넘어도 가까우면 보인다) | `isNearby()` |
| `all` | 전국 전부 | — |

동네를 새로 고르면 `applyAutoScope()`가 고른 모양에 맞는 기본값을 정한다 —
전국이면 `all`, 읍·면·동까지 골랐으면 `near`, 시·도나 시·군·구만 골랐으면 `region`.
**시·도 한가운데를 기준으로 잡은 반경 3km는 아무 뜻이 없기 때문이다.**
사용자가 단추로 바꾼 값은 동네를 다시 고를 때까지 유지된다.

`inHood()`는 두 가지를 본다:
1. **주소 글자** (`inHoodByAddress`) — 경계 자료 없이도 동작한다. 이게 기본이다
2. **경계 도형 안에 들어오는가** — 사업 위치나 **노선**이 경계를 지나면 우리 동네 사업이다.
   실측: 고양시 덕양구는 주소로 맞는 사업이 0건인데, 한강 고양권역 하천사업의
   노선이 덕양구를 지난다. ①만으로는 놓친다.

> **함정.** `지도에서 보기`로 연 사업이 지금 범위 밖이면 지도에 마커가 없다.
> `openMapScreen(focusId)`가 그런 경우 `mapScope`를 `all`로 넓힌다.

### ★ 지도 축척은 '사업 마커'가 아니라 '보고 있는 범위'에 맞춘다 (`fitGisView`)

노선으로 걸린 사업은 **대표 주소 마커가 수십 km 떨어져 있을 수 있다.**
(실측: 인천 검단구 원당동에서 한강 고양권역 사업 — 노선은 0.9km인데 마커는 파주 18.5km)
그 마커까지 담으려고 `fitBounds` 하면 축척이 확 벌어져 **우리 집이 화면 구석으로 밀린다.**
같은 원인으로 두 번 겪었다(경서동에서 한 번, 원당동에서 또 한 번).

그래서 **지금 고른 범위 그 자체**에 맞춘다:

| 범위 | 무엇에 맞추나 | 실측 결과 |
|---|---|---|
| `region` | 경계 + 우리 집 | zoom 14, 6km |
| `near` | **우리 집 ± 반경** (경계 있으면 함께) | zoom 13, 13km, 우리 집 **정중앙** |
| `all` | 사업 마커 전체 | zoom 8, 415km |

옛 방식으로는 `near`에서 우리 집이 중앙에서 x −94px, y +121px 밀려 있었다.

### ★ 전국을 보는 중에는 '우리 집'이 없다

동네를 안 정했으면 기준 좌표는 관리자 설정의 기본값(하남 근처)일 뿐이다.
그걸로 거리를 재면 **"우리 집에서 19.7km" 같은 거짓말**이 화면에 뜨고,
미니 지도에는 하남에 깃발이 꽂힌다 (2026-08-06에 실제로 그랬다).

그래서 `recomputeDistances()`가 **맨 앞에서 `isNation()`을 보고 거리를 전부 `null`로 둔다.**
한 곳만 막으면 반경 판정·정렬·거리 표시·`가장 가까운 사업`이 전부 조용히 빠진다.
지도 쪽은 따로 막는다 — `renderGisMarkers()`·`renderMiniMap()`이
**우리 집 마커와 반경 원을 아예 그리지 않고**, 축척은 사업들에 맞춘다(`KOREA_CENTER`).

### 반경 필터

반경은 **첫 화면 '내 동네' 카드의 `반경` 칸에서 주민이 직접 고른다** (`RADIUS_STEPS`, 기본 5km).
시골은 10km도 '우리 동네'라 3km 고정은 맞지 않았다. 고른 값은 바로 저장되고
관리자 설정의 `radiusKm`과 같은 값이다.

**선형 사업은 주소 한 점으로 판단하지 않는다.** 사업 대표 주소가 노선에서 멀리 떨어져
있는 일이 흔하다 — 한강 고양권역 하천사업은 주소가 파주시라 계양구에서 22.9km로 나오지만
하천 노선은 **4.1km** 앞을 지난다. 주소만 보면 "우리 동네 사업"에서 빠진다.
그래서 `recomputeDistances()`가 **노선까지의 최단 거리(`routeDist`)도 재고,
둘 중 가까운 쪽(`nearDist`)** 을 반경 판단·정렬·표시에 쓴다.
노선 쪽이 더 가까우면 화면에 `(노선 기준)`이라고 밝힌다.
관리자가 직접 그린 노선도 같은 방식으로 즉시 반영된다.
한계: **노선 도형이 있는 사업만** 해당한다(지금 4건 + 관리자가 그린 것).
도로·철도는 도형이 없어 여전히 주소 한 점으로 판단한다.

우리 집 좌표는 선택한 동네를 **VWorld 지오코더(JSONP)** 로 변환해서 얻는다.
실패하면 관리자 설정의 기준 좌표로 되돌린다.
범위 안에 아무것도 없으면 "반경으로 넓혀 보기" / "전국 사업 모두 보기" 버튼을 준다.

### 배경지도 — 일반 / 위성 (2026-08-16 추가)

**기본은 일반지도다.** 주민은 지명·도로명 **글자**로 위치를 알아본다.
위성만 켜면 글자가 없어 "여기가 어디지"가 되므로 **`Satellite` + `Hybrid` 두 장 세트**로 올린다.

그런데도 위성을 두는 이유는 일반지도가 못 하는 것을 하기 때문이다 —
**"지금 저기가 어떤 땅인가"**(논밭인지 산인지 이미 개발된 곳인지).
개발사업 알리미에서 그것은 장식이 아니라 정보다.

| | 실측 타일 무게 | 비고 |
|---|---|---|
| `Base` (기본) | 4~38KB | 지금 쓰는 것 |
| `Satellite` | 13~41KB | **확장자가 `jpeg` 다** (다른 것은 png) |
| `Hybrid` (지명·도로) | 42~83KB | 위성과 반드시 같이. **무겁다** |
| `midnight` | 2~24KB | 줌 6~18. 이 서비스엔 안 맞는다 |
| `gray` | — | **이 키로는 안 된다** |

- 위성 쪽이 3배 넘게 무거워 **기본으로 켜 둘 물건이 아니다.** 단추로 바꾼다
- **★ 고른 값을 기억하지 않는다. 언제나 일반지도로 시작한다** (2026-08-16 결정).
  한 번 위성으로 바꾼 사람이 다음에도 위성으로 열리면 그것이 기본 화면인 줄 안다.
  위성은 지명 글자가 없어 **첫 화면으로는 맞지 않다.**
  (`route_editor.html` 은 예외로 `wdn.maplayer` 에 기억한다 — 기준점 잡느라 위성을 계속 쓴다)
- **미니 지도(홈 '내 동네' 카드)에는 넣지 않는다.** 작아서 마커·경계가 묻힌다
- 노선 그리기 도구에서는 위성이 특히 좋다 — 건물 모서리·교차로를 기준점으로 쓸 수 있다

> ### ★ 위성 위의 색 — 옅게가 아니라 **선명하게** (2026-08-16)
>
> 타일만 갈면 표시가 안 보인다. 그렇다고 **밝게(파스텔로) 바꾸면 더 나빠진다.**
>
> **★ 여기서 한 번 크게 헛짚었다.** 글자용 잣대(WCAG 명도 대비)를 마커에 갖다 썼다.
> 그 잣대는 '배경과 밝기가 얼마나 다른가'만 보므로 계속 옅은 색으로 밀리는데,
> **마커는 글자가 아니라 지도 위에 올려놓은 물건**이다.
> 실제로 파스텔로 바꿔 놓으니 위성 사진의 **안개·구름처럼 보여서 더 안 보였다.**
> 위성 배경이 어둡고 편차가 크다는 실측(중앙 0.069 · 하위5% 0.021 · 상위5% 0.196)은 맞지만,
> **거기서 끌어낸 결론이 틀렸다.**
>
> **지도 서비스들(구글·네이버·카카오·애플)이 실제로 하는 방식**을 따른다:
> 1. **진한 색**을 쓴다. 파스텔은 안 쓴다
> 2. **흰 테두리 2~3px**
> 3. **그림자**로 '떠 있는 물건'처럼 만든다
> 4. 위성이라고 **마커 색을 바꾸지 않는다** — 학습된 색을 지킨다
> 5. 노선은 **짙은 테두리 + 채도 높은 선** (구글 경로 = 선명한 파랑 + 짙은 파랑 테두리)
>
> | 요소 | 일반 | 위성 |
> |---|---|---|
> | 전략 마커 | `--p80` 짙은 남색 | **`--accent`**(선명한 파랑) — 남색은 어두운 산 위에서 원리적으로 안 보인다 |
> | 환경 마커 | `--warn` | **그대로** (원래 잘 보인다) |
> | 우리 집 | `--eco` | **그대로**, 흰 테두리만 4px |
> | 노선(고름) | `--eco` | `--live` + `--p90` 테두리 |
> | 노선(안 고름) | `#3f9aae` | `--accent` + `--p90` 테두리 |
> | 동네 경계 | 초록 점선 | 흰 점선 (+ `bringToBack()` 안 함) |
> | 반경 원 | 파랑 | `--accent` + `--p90` 원 한 겹 더 |
>
> - 고른 노선을 **흰색으로 하지 않는다** — 동네 경계가 이미 흰 점선이라 겹친다
> - **새 색을 만들지 않는다.** 전부 `tokens.css` 값이고, JS 는 `cssVar("--accent")` 로 **읽어서** 쓴다
> - 마커 색은 `app.css` 의 `.gis-map--sat .mk--*` 에서만 바꾼다 (JS 아님)

### ★ VWorld 배경지도는 줌 6 미만을 지원하지 않는다

전국(42건)을 작은 미리보기 지도에 억지로 다 담으려 하면 `fitBounds`가 줌을 **5**까지
낮춘다. 그런데 VWorld WMTS `Base` 레이어는 **줌 6~19만** 지원해서, 그 아래로 내려가면
타일 대신 오류 XML(`InvalidParameterValue`)이 온다. **마커는 제자리에 잘 찍히는데
바탕지도만 통째로 빈 걸로 보인다** — 2026-08-06에 실제로 겪었다.

`ensureGisMap()`·`renderMiniMap()` 둘 다 지도 생성 시 `minZoom:6`을 건다.
Leaflet이 `fitBounds`로 그 아래를 계산해도 6에서 멈춰 준다.
**지도를 새로 만드는 곳이 생기면 여기도 `minZoom:6`을 잊지 말 것.**

### 동네 경계 표시

읍·면·동(또는 시·군·구) 경계를 VWorld에서 받아 지도와 미니 지도에 초록 점선으로 그린다
(`HOOD_BOUNDARY`, `drawBoundary()`). 반경 원보다 "우리 동네"에 훨씬 잘 맞는다.
미니 지도는 경계가 있으면 반경 원 대신 **경계에 맞춰** 축척을 잡는다.

**★ 반경 원은 `범위 = 반경`일 때만 그린다** (`showRadiusCircle(scope)`).
원은 "지금 무엇으로 거르는지"를 보여주는 표시라 범위를 따라간다 —
`near`면 그리고, `region`·`all`이면 안 그린다.
`region`에서 원을 그리면 큰 점선 원이 화면을 덮어 정작 동네 경계가 얼룩처럼 보인다(주안동에서 겪음).

> 한때 **'경계가 있으면 무조건 숨김'**으로 만들었다가 되돌렸다 —
> 반경을 직접 골라도 원이 안 나와서 5km가 얼마인지 알 수 없었다. 기준은 **범위**지 경계가 아니다.

- **읍·면·동 / 시·군·구**는 VWorld를 그때그때 부른다 (9KB / 84KB로 작다)
- **시·도는 `data/sido.json`에서 읽는다.** VWorld 원본이 너무 커서다 —
  실측 경기도 3.2MB·좌표 79,576점, 전남광주 272,761점.
  `tools/build_boundaries.py`가 미리 200m로 단순화해 둔다 (**전체 16개 367KB**, 원본의 2.2%).
  화면은 시·도를 처음 고를 때 한 번만 받고, 안 고르면 아예 안 받는다.
  **VWorld 키가 없어도 나온다** (파일에서 읽으므로). 단 지도 타일은 여전히 키가 필요하다.
- 전국이면 경계를 받지 않는다
- 경계를 못 받아도(키 없음·거부·시간초과) 화면은 그대로 동작한다. **경계는 덤이다**
- 늦게 도착한 옛 응답이 새 것을 덮지 않게 `boundarySeq`로 순번을 확인한다
- 점이 경계 안인지는 교차 횟수 세기(`ptInBoundary`)로 본다. 구멍(호수 등)도 처리하고,
  먼저 bbox로 걸러 낸 뒤에 계산한다. 노선은 점이 많아 200개만 골라 본다

**★ 동네의 '한가운데'는 지오코더가 주는 점이 아니다.**
VWorld 지오코더에 "인천광역시 서해구 경서동"을 넣으면 **그 동의 대표 지번 한 곳**이 온다.
동사무소도 아니고 도형의 중심도 아니라서, 경계를 그려 놓고 보면 기준점이 한쪽에 치우쳐 있다.
실측으로 **경서동 2.5km · 미사동 2.4km · 사림동 0.7km** 어긋났다 — 반경 5km에서는 큰 차이다.
그래서 경계를 받으면 `boundaryCenter()`로 도형에서 직접 중심을 구해 옮긴다.

- 면적으로 가중한 무게중심을 쓰고, 섬이 딸린 동네는 **가장 넓은 덩어리**를 본체로 삼는다
- 초승달처럼 굽은 동네는 무게중심이 경계 **밖**으로 나갈 수 있다 →
  격자(20×20)를 훑어 안쪽 점 중 무게중심에 가장 가까운 곳을 고른다
- **`내 위치`로 잡은 자리는 옮기지 않는다** (`HOME.gps`). 그건 진짜 서 있는 곳이다

### 의견 제출

EIASS 주민의견등록은 **본인인증 로그인이 필수**다. 접수 페이지(`suggestWrite.do`)를
직접 열면 alert 후 로그인 페이지로 튕긴다(확인함).
그래서 안내 모달에서 3단계를 알려주고, "EIASS로 이동"이 해당 사업 페이지를 새 탭으로 연다.
(EIASS 상세는 GET 링크가 아니라 POST로만 열려서, 숨은 폼을 만들어 제출한다.)

---

## 지어내지 않는 값

EIASS에 없는 정보는 화면에 만들어 넣지 않는다.

- 주민 의견 수, 설명회 참가 신청 현황 → 수집 안 됨. 0 또는 표시 안 함
- **첫 화면 숫자 3칸과 대시보드는 전부 수집한 실제 값이다.** 관리자가 손으로 넣는
  가짜 숫자는 없앴다. 값이 없으면 그 칸을 아예 만들지 않는다
- **소규모환경영향평가는 건수만** 가져온다. 사업 목록·지도에는 넣지 않는다
  (공람·설명회 절차가 달라 지금 화면 구조와 맞지 않는다)
- 설명회 일시는 사업마다 자유 형식(`260814 오전 10시` / `2026.08.06(목) 14:00`)이라
  **원문 그대로** 보여준다. 날짜로 파싱하면 틀릴 위험이 있다.
  **예외**: 다음 설명회까지 남은 날(D-n)과 "지남" 표시를 내야 해서
  `briefInfo()`가 회차 단위로 날짜를 읽는다. **원문은 절대 고치지 않고** 표시만 더한다.
  - 회차 나누기: 괄호 밖의 쉼표·빗금 기준. 시각만 있는 조각(`14:00`)은 앞에 붙인다
  - 읽는 형식: `2026년 8월 6일` `2026.08.06` `20260806` `260806` `2026 8 6(목)`
  - 연도가 생략된 조각(`08월 03일`)은 **앞 조각의 연도를 물려받는다**
  - `30일(목)~31일(금)` 같은 범위는 양 끝 날짜를 모두 담는다
  - `미정` `타법에 의한 생략`은 날짜가 없으므로 뜻만 덧붙인다
- **설명회 일시와 장소를 순서대로 짝짓지 않는다.**
  1:1로 맞는 자료가 많지만(36건 중 32건) **어긋나는 것이 실제로 있다** —
  기장군 사업은 일시가 `장안읍…, 정관읍…` 순인데 장소는 `기장읍…, 일광읍…` 순이다.
  `matchPlaces()`는 ① 일시에 적힌 지역 이름으로 장소를 찾고,
  ② 지역 이름이 아예 없고 회차가 3번 이상일 때만 순서대로 짝짓는다.
  둘 다 안 되면 짝짓지 않고 일시·장소를 원문 통째로 따로 보여준다
- **의견 받는 곳(`deptName`)에 기관명을 붙이지 않는다.**
  `org`는 협의기관(유역환경청), `deptName`은 사업자 부서라서 이어 붙이면
  `낙동강유역환경청 창원시청 도시계획과` 같은 없는 이름이 만들어진다
- **전화번호는 화면에 내지 않는다.** 부서 대표번호이긴 하나 표시하지 않기로 했다.
  연락처가 필요하면 `EIASS 원문 페이지 열기`로 간다

---

## 자료 출처 표기 — 공공누리 제4유형 (2026-08-16 확인)

EIASS 푸터에서 직접 확인했다 (`img_opentype04.png`,
alt="공공누리 공공저작물 자유이용허락 4유형 출처표시,상업적 이용금지").

**제4유형 = 출처표시 + 상업적 이용금지 + 변경금지(2차적저작물 작성 금지)**
(공공누리 공식 정의. EIASS 의 alt 글자에는 '변경금지'가 빠져 있지만 유형 번호가 기준이다)

- **출처 표기는 의무다. 지우지 말 것.** 두 화면 푸터(`.footer-src` / `.m-src`)와
  이용안내(`texts.js` "자료 출처와 이용 조건"), `data/projects.json` 의 `source` 항목.
  **온라인은 출처 사이트로 가는 링크가 함께 있어야 한다.**
- **상업적 이용금지** — 광고·유료 기능을 넣지 않는다. 지금 조건을 지키고 있다.
- **★ 변경금지가 걸려 있다.** 우리는 요약문을 AI로 옮기고 있으니 형식상 **개작**에 해당한다.
  EIASS 저작권정책도 "무단 변경, 복제·배포, 개작 등의 이용은 금지"라고 적어 두었다.
  → **코드로 풀 수 있는 문제가 아니다. 발주처(기후에너지환경부 국립환경과학원) 확인이 필요하다.**
  지금 해 둔 완화책: 해석은 공람 기간에만 · 원문이 아님을 명시 · 원문 링크 상시 제공 ·
  비영리. 진행 상황은 `docs/HANDOFF.md` §2 참고.
- 평가서 초안 자체는 **사업자가 쓴 문서**다. EIASS 정책도
  "기후에너지환경부가 저작권 전부를 갖고 있지 아니한 자료도 제공된다"고 밝히고 있다.

---

## 용어

| 화면에 쓰는 말 | 뜻 |
|---|---|
| 초안 공람 | 평가서 초안을 주민에게 공개하고 의견받는 기간 |
| 의견 제출 | 주민이 그 기간에 의견을 내는 것 |
| 협의 | 환경부·유역청이 사업자와 조율하는 절차 |
| 사후환경영향조사 | 공사 시작 후에 하는 점검 |
| 면형 | 사업 구역이 넓은 면으로 되어 있는 것 (주소 한 점으로 표시) |
| 선형 | 도로·하천처럼 길게 이어지는 것 (구간별 시점·종점이 있음) |

행정 용어를 그대로 쓰지 말고, 위 표처럼 주민이 아는 말로 바꿔 쓴다.
예: "협의 진행 중" → 괜찮지만, "재협의 요청 반려" 같은 건 풀어서 쓴다.

---

## 아직 안 된 것 / 다음에 할 것

- **✅ 수집 자동화 완료 (2026-08-15).** 매일 07시·13시(한국)에 GitHub Actions 가 자동으로
  수집 → 커밋 → 배포한다. **사람이 매일 누를 일이 없다.**
  - **EIASS 와 VWorld 는 서울 중계를 거친다** (위 "EIASS 는 서울 중계를 거친다" 참고).
    미국(GitHub)에서 그 두 곳이 응답하지 않는 것이 오래 막혀 있던 원인이었다.
  - `tools/run_daily.bat`(한국 PC 수동 수집)은 **급할 때 쓰는 길로 남겨 둔다.** 직접 접속이라 빠르다.
  - 수집이 반쪽만 되면 저장하지 않는다 — 목록 받기가 실패하면 아무것도 쓰지 않고 멈추고,
    `collect.yml` 도 수집 전후 건수를 비교해 0건이거나 절반 아래로 급감하면 커밋하지 않는다.
  - 중계가 죽어 있으면 **수집을 시작하지도 않는다.** 실패해도 자료는 안 망가지고 비용도 0원이다.
  - 실패하면 GitHub 이 저장소 주인에게 메일을 보낸다.
  - **AWS 이전·self-hosted 러너는 필요 없어졌다.**
- **★ Supabase 커넥터가 붙어 있다 (2026-08-15).** 중계 함수를 AI 가 직접 다룰 수 있다 —
  배포(`tools/relay/index.ts` 를 그대로 올림), **함수 로그 확인**, 배포본과 저장소 대조.
  예전처럼 대시보드에 손으로 붙여넣지 않아도 된다.
  - **단 `RELAY_KEY` 는 읽지 않는다.** 키를 몰라도 확인할 수 있다 —
    키 없이 부르면 `401 bad-key` 가 나오는 것으로 살아 있음을 안다.
- **전국 전체 수집 (완료)**: 의견제출 마감 기준으로 최신 **41건**이 화면에 뜬다
  (2026-08-15 수집분, 좌표 39건). 건수는 매일 바뀐다.
  예전에 적혀 있던 "수백 건"은 목록 중복 집계 때문에 나온 잘못된 수였다.
- **✅ 휴대폰 화면 완성 (2026-08-16).** 관심목록 · 새 사업 알림 · 마감일 캘린더 넣기 ·
  **지역 캘린더 구독**(5,339개 `.ics`) · 위성지도 · 노선 그리기 도구.
  경위는 `docs/HISTORY.md` §8.
- **웹 푸시·회원가입은 안 한다 (2026-08-16 결정).** 둘 다 **서버와 개인정보 보관**이 생겨
  '서버 0원·개인정보 0건'이 깨진다. 알림은 **캘린더 구독**으로 풀었다.
  아이폰 웹 푸시는 **홈 화면 추가까지** 해야 해서 주민에게 현실적이지도 않다.
- **도로·철도 선형 사업의 노선**: 자동으로는 못 한다. **`route_editor.html`** 로 사람이 넣는다
  (도면이 있으면 기준점 3~4개만 짝지으면 반자동으로 딴다).
  **2026-08-16 에 3건을 그려 넣었다** — 면목선 · 대구도시철도1호선 영천(금호) 연장 · 광양(Ⅳ) 공업용수도
- **사업 구간만 잘라낸 하천 노선**: 지금은 하천 전체가 나온다
- **✅ `firstSeen`(우리가 처음 본 날) 기록 완료 (2026-08-16).**
  '새로 올라온 사업' 판정이 공람 시작일에서 이 값으로 바뀌었다. 위 "새로 올라온 사업 알림" 참고
- **다음에 만들 것은 `docs/HANDOFF.md` §2 표에 우선순위대로 있다.**
- **배경 사진을 저장소 안에 두기** — 지금은 Unsplash 에서 직접 받는다.
  정부 서비스라 외부 CDN 의존이 걸리면 `assets/` 로 옮길 수 있다 (아직 안 함)

> **지금 당장 할 일은 `docs/HANDOFF.md` §2 에 있다.** 이 목록은 큰 방향만 적는다.
