# tools/ 의 파이썬 스크립트가 쓰는 것들
# GitHub Actions 와 내 PC 가 같은 것을 쓰도록 여기 적어 둔다.
#   설치: pip install -r requirements.txt

requests>=2.31          # EIASS·VWorld 에 HTTP 요청
python-dotenv>=1.0      # .env 파일에서 API 키 읽기
lxml>=5.0               # EIASS 상세 페이지 HTML 파싱
pdfplumber>=0.11        # 요약문 PDF 에서 글자 뽑기 (한글 표 처리가 안정적)
anthropic>=0.40         # 환경영향분석 생성
